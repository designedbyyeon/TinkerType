# 시각예술가의 창작 도구 — working notes

프로젝트의 목적, 디자인 언어, 작동 방식. 새 도구를 추가하기 전에 읽을 문서다.
개별 기능의 상세와 설계 근거는 `README.md`에,
**지금 어디까지 왔고 무엇이 열려 있는지는 `HANDOVER.md`에** 있다.

---

## 1. 목적

**그래픽 디자이너가 실무에 실제로 투입할 창작 도구들을 모은 웹사이트.**

데모나 장난감이 아니다. 판단 기준은 늘 같다 — *실제 작업에 쓸 수 있는가.*
그래서 정밀도(폰트 메트릭, 진짜 벡터 패스, 정확한 기하)를 대충 넘기지 않는다.

**산출물은 도구마다 다르고, 각 도구의 `spec`에 그대로 적는다.** 01·04는 편집 가능한
SVG, 03은 **OBJ·STL 입체 모델**, 02는 **형태에 따라 둘 다**다(평면=SVG, 입체=OBJ),
05는 **WAV 오디오 루프**다.
오래 "결과물은 항상 편집 가능한 SVG"였는데, 03이
three.js로 라이브 렌더하기로 결정되면서 사이트 전역 법에서 **도구별 계약**으로
내려왔다. 02가 한 도구 안에서 두 계약을 갖게 되면서 그 결정이 한 번 더 옳았다 —
전역 법이었다면 02의 입체 형태를 만들 수 없었다.

**그리고 05가 그 계약을 그림 밖으로 데려갔다.** 사이트의 첫 비-그래픽 산출물이다.
`CANVAS_GROUND`도 `exportSvg`도 그 파일에 관여하지 않는다 — 대신 같은 규칙이 다른
매체에서 산다(§3의 원리 ①). **스테이지는 그래도 SVG DOM으로 그린다**: 나머지 넷이
그렇고, 악보를 SVG로 내보내기로 하면 `exportSvg` 호출 한 줄이 되는 상태로 남기는
값이 0이기 때문이다. 지금 `spec`은 WAV만 말한다 — 지킬 수 없는 약속을 적지 않는
것이 이 저장소의 규칙이다. 그림만 내보내면 이미지 생성기가 되지만 **모델을 내보내면 오브젝트
생성기**가 된다 — 씬에도, 렌더에도, 프린터에도 가져갈 수 있는 것. 그래서 03의
산출물은 이미지가 아니라 모델이다. 지킬 수 없는 약속을 적지 않는 것이 이 저장소의 규칙이므로, 메인 페이지의
소개 문구에서도 그 문장을 뺐다 — 등록부에 대해 이미 못박혀 있던 것과 같은 이유다.

도구는 앞으로 여러 개가 된다. 지금은 다섯이다 — `diagram-typography`(선 위의 도형
체인에 글자가 앉는다), `plastic-type`(글자를 프라모델 런너로 만든다),
`billboard-typography`(글줄이 상가 건물의 간판이 되어 하나의 글줄로 읽힌다),
`magic-circle-typography`(카메라에 주먹을 뻗고 손을 펼치면 글줄마다 하나의
고리가 되어 손바닥 위에 써진다), `type-the-beat`(원판 세 개를 돌려
음절을 맞추고 격자에 탭해 넣으면 한 마디의 드럼이 된다).

**04의 이름은 비유다.** 레퍼런스는 손 하나와 그 주위를 도는 활자뿐이고 선이 한
가닥도 없다. 그래서 테두리·괘선·눈금·별·살은 **전부 기본값이 꺼짐**이고, 켤 수 있는
목록으로만 있다. 그리는 것은 원 위의 활자다 — 묻지도 않은 펜타그램이 나오면 그것이
주제가 되고, 주제는 문장이다.

`plastic-type`은 **라틴과 한글 둘 다**다 — 이 저장소에서 유일하게 그렇다. 라틴
베리어블 3종에 한글 2종(Gothic A1 네모꼴 · UnJamo Dotum 세벌식 탈네모꼴)이 붙었고,
**그 둘은 한글을 짜는 두 가지 방식이지 한 방식의 두 취향이 아니다**: 네모꼴은 모든
음절을 같은 상자에 맞추고, 세벌식은 자모마다 한 벌씩 주고 음절 높이는 나오는 대로
둔다. 사출 판에서 그 차이는 스타일이 아니라 구조다.
**한글이 이 도구를 가장 세게 시험한다** — 라틴 낱말은 한 덩어리짜리 글자들의 줄이고,
음절은 애초에 한 형태였던 적이 없는 자모 둘·셋이 쌓인 것이다. `한`은 이 파이프라인에서
**조각 다섯 개**로 나온다(ㅎ이 획 셋으로 그려진다). 글자 하나 크기의 프레임에 부품이
다섯이면 게이트를 걸 벽이 모자라기 시작하고, 그래서 게이트 불변식 검사에 한글이 따로
들어가 있다.
**형태가 둘이다** — 평면(SVG DOM)과
입체(three.js). `Page`의 `Form` 한 행이 고르고, 그 위의 모든 컨트롤은 두 형태에서
같은 뜻이다. 같은 플랜을 두 번 보는 것이고, 두 개의 도구가 아니다.
**기본값은 입체다** — 이 도구가 만드는 것이 성형된 물건이고, 그것의 도면이 평면이다.
먼저 보이는 화면이 도면이면 도구를 거꾸로 가르친다.
`billboard-typography`는 반대로 **한글 전용**이다 — 정방형에 가까운 자형이라 행이
조밀하게 맞물리고 세로쓰기가 자연스럽다. 라틴은 둘 다 안 된다.
`magic-circle-typography`는 **영문 전용**이고, 사이트가 이미 갖고 있는 라틴 3종을
그대로 쓴다. `type-the-beat`은 **한글 전용**이고 — 라틴에는 초성·중성·종성이
없으니 성립조차 안 한다 — 03이 갖고 온 조선일보 견고딕을 같이 쓴다. 두 번째
사용자가 생겼으므로 그 서체가 `shared/media/type/hangul/`으로 올라갔다.

**04는 사이트의 첫 입력장치 도구다.** 웹캠으로 손을 읽고 그 손이 문서를 움직인다.
그래도 판단 기준은 같다 — 카메라가 없어도(또는 못 켜도) 도구는 평범한 편집기로
멀쩡히 돈다. 손은 값을 *덮어쓰는* 장치일 뿐이고, 셔터가 그 값을 문서에 굽는다.

**05는 사이트의 첫 소리 도구다.** 그리고 그 아이디어가 서는 근거는 비유가 아니다 —
**음절의 세 자리가 소리의 세 구간과 맞는다.** 초성은 어택이고(조음 위치가 타악기를
정한다: ㅂ 순음=킥, ㄷ 설음=스네어, ㅅ 치음=하이햇, ㅇ은 무음 초성이라 어택이
없다), 중성은 몸통이고(ㅜ는 낮고 어둡다, ㅣ는 높고 밝다), 종성은 릴리스다(ㅁ은
멈추고, ㅇ은 울리고, ㄱ은 불파음이라 끊긴다).

**그리고 초성은 어택만 정하는 게 아니라 몸통이 얼마나 남는지도 정한다.** 이걸 빠뜨렸을
때 모든 소리가 비슷하게 들렸다 — 몸통이 음절에서 가장 큰 소리인데 초성과 무관하게 항상
같은 크기로 나면, 츠와 으는 앞에 긁힌 자국만 다른 같은 톱니파다. 지금은 `voiced`가
있다: ㅇ은 모음의 자리일 뿐이라 전부 목소리(1.0), ㅅ은 성대가 거의 안 붙은 마찰(0.13),
파열음은 그 사이(0.5~0.62). 그리고 파열이 없는 초성(ㅇㅁㄴ)은 때리는 대신 **부풀어
오른다**(`onset`) — 그게 웅이 울리느냐 퍽 하느냐를 가른다.

**종성도 길이만으로는 부족했다.** 붐과 붕이 200ms 차이 말고는 같아서 한 소리를 두 길이로
쥔 것으로 들렸다. 실제로 둘을 가르는 것은 **닫히는 자리**다: ㅁ은 양순이라 맨 앞에서
막히니 울릴 것이 남지 않아 꼬리가 어두워지고, ㅇ은 연구개라 그 앞쪽이 통째로 관으로
남아 밝게 울린다(`close`). 그리고 ㅂ은 불파음이라 페이드가 아니라 **정지**이고, 조음
기관이 맞닿는 소리가 실제로 들린다 — 그 짧은 텀프가 붑!의 느낌표다. **가획이 에너지다** — ㄱ→ㅋ은 획
하나와 공기 한 번이고, 그래서 뚬은 둠보다 센 킥이다.

**그래서 오래 "믹서 페이더가 없다"였는데, 그건 두 가지를 하나로 본 것이었다.** 가획은
*한 소리가 얼마나 센가*이고 레인 페이더는 *한 줄이 다른 줄들 사이에서 어디에 앉는가*다.
앞의 것은 글자의 사실이라 여전히 컨트롤이 아니고, 뒤의 것은 한 마디를 짜는 사람의
결정이다. 둘은 부딪히지 않는다 — 절반으로 내린 레인은 **같은 뚬**을 작게 낼 뿐이다.
지금은 레인마다 페이더가 하나 있고, 그것이 못 하는 일은 뚬을 둠으로 만드는 것이다.
(그리고 그 자리는 패널이 아니라 격자다 — 아래 기계 등록부 11번.)

**그리고 세 개의 휠이 곧 한글 입력기다.** 휠을 돌리면 `compose`가 음절을 다시 만든다.
조합형 문자라서 회전 세 개로 음절을 조립할 수 있고, 그게 이 도구가 한글로만 성립하는
이유다.

**세로모임꼴만이다.** 레퍼런스가 세로로 쌓인 음절이므로 기계도 그 음절을 턴테이블로
만든 것이고, 중성 슬라이더는 그렇게 앉는 다섯 모음(ㅗㅛㅜㅠㅡ)만 든다. 한 번은 모임꼴을
음절에서 읽어 ㅏ에서 옆으로, ㅘ에서 대각으로 기계를 재조립하는 버전을 만들었다.
**돌아갔고 일부러 지웠다** — 가로·섞임 형태는 더 생각해야 하고, 안 나가는 배치를 위한
죽은 코드는 존재하지 않는 도구의 로드맵과 같은 종류의 못 지킬 약속이다. `voice.ts`의
소리 표는 21개 중성을 그대로 다 갖고 있다 — 그건 배치가 아니라 음성학이고, 완전한 표는
값이 0이다.

**시퀀스를 타이핑하지 않는다. 원판을 돌려 만든다.** 한 번은 텍스트 필드가 시퀀스인
버전을 만들었는데(한 줄이 한 트랙), 이야기는 깔끔했고 **악기가 아니었다** — 비트를
타이핑해서 만들게 되고, 원판은 커서 밑에 있는 것을 고치는 편집기로 내려앉는다.
지금은 드럼머신의 몸짓이다: **휠 세 개로 소리를 맞추고, 격자에서 스텝을 탭해 넣는다.**
한 레인이 한 음절이고 한 열이 한 스텝이다. 레인의 이름이 곧 그 음절인 것은 음절이
소리의 전부를 말하기 때문이다 — 따로 채널 이름을 붙일 것이 없다.

**그래서 `Text` 그룹이 없다. 활자는 사라진 게 아니라 기계 안으로 들어갔다.** 손에 든
음절을 크게 보여주는 읽기값, 레인마다 그 소리의 이름, 원판에 파낸 자모 — 화면의 글자가
전부 일하는 자모다. 텍스트 필드가 있는 것보다 더 타이포그래피적이고, 덜하지 않다.

**만들어진 도구만 등록한다** (`src/app/tools.ts`). 존재하지 않는 이름의
로드맵은 사이트가 지킬 수 없는 약속이다.

### 사용자

그래픽 디자이너. 코드보다 타이포그래피·레이아웃 감각과 실무 흐름에 익숙하다.
설명할 때는 디자인 용어를 자연스럽게 쓰고, 구현 디테일은 **결과물에 어떤 영향을
주는지** 중심으로 말한다. 결과는 눈으로 확인할 수 있는 형태로 전달한다.

---

## 2. UI 디자인

계기판·기술 문서의 언어. 레퍼런스는 Swiss 포스터와 부품 카탈로그(ACF-01 류)다.

### 팔레트 — 화면과 작품은 다른 팔레트다

**화면 — `src/shared/styles/tokens.css`**

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--paper` | `#f4f3f0` | 지면 |
| `--paper-sunk` | `#eceae4` | 한 단 낮은 면 (메인 페이지의 미리보기 카드) |
| `--ink` | `#14140f` | 화면 활자·도형 |
| `--ink-mute` | `#8a877c` | 레이블, 그리고 `mark`의 기본 상태 |
| `--rule` / `--rule-strong` | `#d3d0c7` / `#b6b2a6` | 헤어라인 |
| `--accent` | `#ff4a12` | **"지금 켜진 것" 하나에만.** 장식으로 쓰지 않는다 |

**작품 — `src/shared/styles/canvas.ts`**

| 상수 | 값 | 쓰임 |
|---|---|---|
| `CANVAS_GROUND` | `#f4f3f0` | 문서의 기본 지면. 세 도구가 같이 쓴다 |
| `CANVAS_INK` | `#101010` | 출력물의 잉크 |

토큰이 아니라 **리터럴이어야 한다.** 이 값들은 export되는 SVG에 실려 나가고,
`var()`는 앱 밖에서 열면 아무것도 아닌 것으로 풀린다.

`CANVAS_INK`가 `--ink`와 다른 것은 **의도**다. 화면 잉크는 따뜻한 종이 아래
하루 종일 놓여 있으니 따뜻하고(`#14140f`), 작품 잉크는 인쇄로 갈 것이니
중성이다(`#101010`). 통일하지 말 것.

활자: 화면 UI는 Pretendard Variable, 수치·레이블은 고정폭(`--mono`).
레이블은 10px 대문자 자간 0.09em으로 값보다 뒤로 물러난다.

### 지켜야 할 규칙

1. **가로선은 한 가지 의미만 갖는다** — "여기서 섹션이 바뀐다". 그룹 안에는
   선이 하나도 없다. 섹션은 번호 + 잉크색 제목 + 여백으로 구분된다.
2. **슬라이더를 쓰지 않는다.** 파라미터는 `레이블 — 값` 한 줄이고, 숫자 아래
   숫자 폭만큼의 요철이 손잡이다. 요철은 포인터와 1:1로 흐른다. 끌면 숫자가
   손 방향으로 기울고 놓으면 튕겨 돌아온다. (`shared/ui/controls.tsx`의 `Scrub`)
3. **아이콘이 설명을 대신한다.** 선택지의 결과를 그대로 그린다 — 변주 방식은
   점 네 개의 크기 변화로, 이음 방식은 원이 원으로 남는 그림 대 뭉개진 그림으로.
   **숫자 파라미터도 마찬가지다.** 이름과 값만으로 결과가 안 보이는 행에는 그 값이
   바꾸는 것의 축소 도면을 레이블 옆에 붙이고, 드래그와 함께 움직이게 한다
   (`Scrub`/`Switch`의 `mark`, 각 도구의 `marks.tsx`). **26×14 한 칸**으로 크기를
   통일하고 `currentColor`만 쓴다. **스스로 설명되는 행에는 붙이지 않는다** —
   전부에 붙이면 장식이 되고, 그러면 어느 것도 의미를 갖지 않는다. 각 도구의
   `marks.tsx` 머리에 **붙이지 않은 행과 그 이유**가 적혀 있다.
   **숫자 행과 선택 행은 자리가 다르다.** `Scrub`·`Switch`는 레이블 옆의 `mark`를
   받지만 `ChoiceRow`는 안 받는다 — 선택지의 결과는 **선택지 안에** 그려야 하기
   때문이다(`icon`). tool 05의 `Step`(1/4·1/8·1/16)이 그 경우다: 버튼 세 개가 각각
   자기 길이를 그린다. 레이블 옆의 마크 하나로는 이미 고른 것밖에 못 보여준다.
4. **의미 없는 컨트롤은 감춘다.** 비활성 회색 처리 대신 아예 렌더링하지 않는다.
   **tool 04가 이 규칙을 가장 멀리 밀었다.** 카메라가 손을 잡고 있는 동안
   `Bloom`은 손이 가진 값이라 행이 사라지고, `Radius`는 **`Reach`로 바뀐다** —
   "얼마나 큰가"는 여전히 디자이너의 것이지만 이제 그 단위가 자기 손바닥이다.
   지우는 것과 갈아 끼우는 것의 차이가 여기 있다.
   **tool 02가 같은 규칙을 형태 전환에 쓴다.** `Solid`로 넘기면 `Zoom` 행이
   사라진다 — 거기서는 시점이 사람 것이고 휠이 줌이다 — 그리고 깊이 세 행과
   모따기·광택이 나타난다. 평면에서 의미 없는 행을 회색으로 두는 것이 아니라,
   그 형태에 있는 컨트롤만 있다.
   **tool 05는 같은 규칙을 두 번 쓰는데 둘 다 "감춘다"가 아니라 "만들지 않는다"다.**
   *배치* 컨트롤은 모임꼴이 정하므로 없고, *킷* 컨트롤은 휠에 올릴 것이 하나로
   정해지면서 없어졌다 — 선택지가 하나뿐인 컨트롤은 컨트롤이 아니다. 남길 이유가
   "나중에 바꿀지도 모르니까"라면 그건 존재하지 않는 도구의 로드맵과 같은 종류의
   약속이다.
5. **대지 경계가 없다.** 캔버스가 작업 영역 전체를 채우고 viewBox가 픽셀과 1:1.
   테두리도 그림자도 판형도 없다.
6. **패널 순서는 제작 순서고, 칠하기가 다듬기보다 앞이다.** 색을 정하기 전에는
   크기·간격·두께를 제대로 판단할 수 없다. 밝은 도형이 어두운 지면 위에서 더
   무겁게 읽히고, 같은 12px 벽이 검정 프레임과 주황 프레임에서 다르게 읽힌다.
   세 도구가 같은 순서를 따른다 — **거친 형태 → Paint → 다듬기**.

   | | tool 01 | tool 02 | tool 03 | tool 04 | tool 05 |
   |---|---|---|---|---|---|
   | 문서 | 1 Page | 1 Page | 1 Page | 1 Page | 1 Page |
   | 내용 | 2 Text | 2 Text | 2 Text | 2 Text | 2 Type |
   | 거친 형태 | 3 Shape · 4 Join | 3 Split | 3 Arrangement | 3 Circle | 3 Deck |
   | **색** | **5 Paint** | **4 Paint** | **4 Paint** | **4 Paint** | **4 Paint** |
   | 다듬기 | 6 Variation · 7 Letters | 5 Solid · 6 Runner · 7 Gate | 5 Light · 6 Detail | 5 Sigil | 5 Voice |
   | 배치 | 8 Objects · 9 Motion | 8 Sheet | — | 6 Hand | — |

   **tool 05에서 규칙 7이 가장 멀리 간다.** 템포·스윙·스텝 길이가 `Page`에 있다 —
   지면 색과 같은 이유로, 문서 전체에 걸리기 때문이다. 한 트랙만 다른 템포인 것은
   없다. 색이 다듬기보다 앞인 것도 여기서 특히 그렇다: `Voice`의 다섯 값은 전부
   **귀로** 판단하는데, 그 판단을 하는 동안 눈은 화면을 보고 있다.

   그리고 tool 05에 배치 그룹이 없는 것은 tool 03과 같은 이유다 — **배치가 컨트롤이
   아니다.** 이 기계는 쌓이고, 중성 슬라이더는 그렇게 앉는 다섯 모음만 든다.

   **tool 05의 2번 그룹은 `Text`가 아니라 `Type`이다.** 타이핑할 필드가 없으므로 그
   그룹이 정하는 것은 *활자가 어떻게 앉느냐*다 — 손에 든 음절을 보여주는 읽기값의
   크기와, 레인 이름의 크기. 여전히 문서의 내용을 다루는 자리이고, 여전히 두 번째다.

   tool 02의 `Form`(평면/입체)은 다듬기가 아니라 **`Page`의 첫 행**이다. 그것이
   정하는 것은 시트가 어떻게 보이느냐가 아니라 **파일이 무엇이 되느냐**(SVG냐
   OBJ냐)이고, 그건 지면 색과 같은 문서 단위 결정이다(규칙 7). 반대로 세 깊이는
   Paint 뒤에 있다 — 34px 부품은 남색에서 판이고 흰색에서 얇은 조각이다.

   tool 04의 마지막 그룹은 장치다. 손이 무엇을 가져갈 수 있는지만 있고, **카메라를
   켜는 버튼은 패널에 없다** — 스테이지에 있다. 빈 스테이지가 무엇을 해야 하는지
   말해야 하는 자리이고, 레일 맨 밑은 그 자리가 아니다.

   **그리고 tool 04에서는 `Text`가 가장 큰 그룹이다. 그것이 요점이다.** `Sigil`의
   모든 행은 요청받기 전까지 아무것도 하지 않고, `Text`의 모든 행은 첫 순간부터
   일한다. 둘의 무게가 뒤집히면 그 도구는 타이포그래피 도구가 아니게 된다.

   tool 03에 배치 그룹이 없는 것은 **배치가 컨트롤이 아니기 때문**이다 — 글줄이
   행 수를 정하고 행 수가 건물을 정한다. 대신 Arrangement에 **방위각(Angle)이
   있다.** 처음엔 카메라 컨트롤을 두지 않았는데, 그 각도는 시점이 아니라
   **레이아웃의 입력**이라서 그렇다: 벽에서 뜬 간판은 화면에서 옆으로 밀리고
   그 양이 각도로 정해지므로, 패커가 그만큼의 간격을 확보해야 읽는 순서가 지켜진다.
   0°에서는 측면 벽의 화면 폭이 0이 되어 패커가 알아서 거기 아무것도 안 놓는다 —
   특수 처리가 아니라 같은 산술의 결과다. **앙각은 여전히 고정이다.**

7. **문서 단위와 스타일 단위를 섞지 않는다.** 배경색은 문서 전체에 적용되므로
   Paint(스타일)가 아니라 Page에 있다. **세 도구 모두 첫 그룹이 `Page`이고, 그
   안에 지면 색이 있다.**
8. **고르는 곳과 조절하는 곳을 붙인다.** 오브젝트 목록과 그 오브젝트의
   위치·배율이 한 그룹에 있다.
9. **화면에는 한 번에 한 언어만 있다.** 오래 이 규칙은 "화면에 나가는 글자는 전부
   영문이다"였다. 그 규칙이 지키려던 것은 진짜였다 — 고정폭 대문자 레이블이 계기판을
   만들고, 두 언어가 한 패널에 섞이면 둘 다 아니게 된다. 틀린 것은 **처방**이었다:
   한 언어만 두는 방법은 *한 언어만 두는 것*이지 한 언어만 갖는 것이 아니다.
   `지면 GROUND`처럼 병기하는 판이 정확히 톤을 무너뜨리는 판이고, 지금 규칙이
   대신하는 것이 그 판이다.
   그래서 사이트는 **EN / KO 스위치 하나**를 갖는다(`shared/i18n/`). 레이블·그룹명·
   `Note`·오류·알림·도구 설명이 통째로 넘어가고, 도구 이름은 안 넘어간다 — 이름은
   이름이라서 `Poppins`가 `Poppins`인 것과 같은 이유다. 코드 주석과 세 문서는
   여전히 한국어다.
   **언어는 문서가 아니다.** `doc`에 넣으면 `structuredClone` 스냅샷마다 얼어붙어
   Ctrl-Z가 패널을 다른 언어로 되돌린다(버그 유형 15 그대로). 그래서 스토어 하나에
   따로 살고 `localStorage`가 기억한다.
   **번역되지 않는 절반이 있고, 그것이 계기판을 지킨다.** `ON`/`OFF`·`px`·`bpm`·
   `st`·`°`·`×`·`SVG`·`OBJ`·`WAV`·`SPACE`·워드마크·`masthead-meta`의 태그·모든 숫자.
   그건 영어가 아니라 **측정의 언어**이고, 단위를 번역하는 것은 서체 이름을 번역하는
   것과 같은 실수다. 한글 레이블은 그 대신 한 급 커지고 자간이 0으로 붙는다
   (`controls.css`의 `[data-lang='ko']` 블록) — 대문자도 자간도 라틴의 장치이고,
   음절은 지어진 덩어리라 자간이 그것을 벌리는 게 아니라 뜯는다.
   **자모와 음절은 예외가 아니라 다른 범주다.** tool 05의 킷 행은 `ㄷ ㅇ ㅂ`을
   버튼에 쓴다. 그건 UI 언어가 아니라 **내용**이다 — tool 03의 텍스트 필드에 한글이
   들어가는 것과 같고, 아이콘이 결과를 그대로 그리는 규칙(규칙 3)의 가장 곧은
   형태다. 그래서 언어를 바꿔도 자모는 그대로다.
   **그리고 스위치는 샘플까지 간다 — 다만 샘플일 때만.** tool 01의 프리셋·새 획과
   tool 02의 시작 시트는 언어를 따라간다(한국어 tool 02는 자모 분해로 열린다. 그것이
   이 도구의 한국어 주장 자체다). 손댄 문서는 절대 안 건드린다: `isSample`이 먼저
   묻고, 아니면 `EN / KO`는 패널만 바꾸는 스위치로 돌아간다.
   **못 가는 곳은 못 간다고 말한다.** tool 04는 라틴을 전제로 만든 도구이고 샘플이
   어느 언어에서든 라틴이다 — 그 사실을 패널이 한국어로 적는다. tool 02의 인덱스
   카드도 라틴으로 남는다: 한글로 짜면 인덱스가 3.9MB 서체를 받아야 하고, 그건
   `faces.ts`가 통째로 막으려고 쓰여 있는 사고다.

### 악기는 문서와 다른 무게를 갖는다 — tool 05가 연 예외

**규칙 1~9는 그대로 두고, tool 05만 한 가지를 더 갖는다.** 나머지 넷은 화면에 *문서*를
그린다 — 대지가 없는 캔버스에 작품이 있고, 헤어라인과 레이블이 그 위에 얇게 얹힌다.
tool 05가 화면에 그리는 것은 문서가 아니라 **만지는 기계**다. 그걸 1px 헤어라인으로
그렸더니 계기판 톤은 맞았는데 **악기의 무게가 없었다** — 종이 위의 빈 원 세 개다.

레퍼런스(`0827_DJ is my Type /`의 세 장)가 정확히 반대다. 그래서 tool 05는 다음을 쓴다.

1. **한 단 낮은 둥근 사각 패널 위의, 한 단 밝은 부품.** 패널이 지면보다 어둡고
   원판이 패널보다 밝다 — 표면에 끼워 넣은 가공 부품으로 읽힌다. 한 번은 원판을
   거의 검게 하고 글자를 파냈는데(knock-out), **무게는 생겼고 활자가 묻혔다.** 활자가
   요점인 도구에서 그건 진 거래다. `Paint`의 `Disc`는 그래서 `Panel`보다 밝아야 한다.
2. **평평하다. 헤어라인 하나가 입체의 전부다.** 그림자·방사형 조명·말린 테두리를
   전부 붙여 봤고 **렌더링된 버튼**이 됐다 — 레퍼런스는 인쇄된 계기이고, 조명이
   글자를 이기고 있었다. 남은 깊이는 스핀들 하나뿐이고 그건 구멍이다. 음영이 필요한
   자리는 둘밖에 없다: 슬라이드의 양 끝(목록이 무언가를 돌아 사라진다)과 스텝이 앉는
   트랙. 그마저도 `ink`와 지면색을 낮은 불투명도로 덮은 것이라(`render/Surfaces.tsx`)
   `Disc`를 검게 해도 같은 방향으로 작동한다 — 회색을 박아 넣으면 무너진다.
3. **자모는 조판하지 않고 그린다**(`render/jamo.ts`). 기계 위의 글자는 부품이다 —
   한 굵기, 진짜 원, 직각. 시각 보정도 서체도 없다. 그려진 ㅇ은 완전한 고리이고
   서체의 ㅇ은 아니다(옆이 얇고 위가 눌려 있고 다른 글자 옆에 서도록 그려져 있다).
   턴테이블 위, 옆에 아무것도 없이, 네 가지 회전으로 놓이면 고리 쪽이 맞고 조판된
   쪽은 흔들림으로 읽힌다. **악보의 레인 이름은 여전히 서체다** — 기계는 가공된 것,
   기보는 조판된 것.
   **그리고 그 경로들은 `nonzero`로 감는다.** 한 자모가 여러 획으로 된 한 경로이고
   획은 서로 교차하므로(ㅂ의 세로가 허리를, ㅜ의 기둥이 바를), `evenodd`로 칠하면
   **교차부마다 구멍이 뚫린다** — 첫 판이 정확히 그랬다.

   **킷의 일곱 자모는 이제 사용자가 그린 도면이다**(`render/jamoArt.ts`). 위의 판단이
   틀렸다는 뜻이 아니라 맞았다는 뜻이다 — 도면도 한 굵기·진짜 원·직각이고, 우리 것이
   아닐 뿐이다. **적용할 때의 규칙은 하나다: 배율이 자모마다 다르면 안 된다.** 서로
   다른 viewBox 크기가 곧 비례이기 때문이다 — ㅇ이 55.47이고 ㅁ이 50.64인 것은 원형
   오버슛이고, 가로획 8.07·세로획 9.0은 시각 보정이고, ㅡ·ㅜ가 54.8로 둘보다 넓은 것은
   중성이라서다. 각자를 자기 상자에 맞추면 그 숫자들이 전부 자기 자신으로 나뉘어
   **한 가족이 여섯 개의 무관한 그림이 된다.** 배율은 ㅁ 하나에서 뽑아 전부에 같이
   쓰고, 각 자모는 em 중심에 놓는다. 상자를 넘치는 것이 정상이다.
   나머지 알파벳은 계속 여기서 그린다 — 어느 휠에도 없지만 표는 완전해야 한다.
4. **글자는 원판과 함께 돈다.** 읽는 자리의 글자만 똑바로 서고, 나머지는 얼마나
   멀리 돌아가 있는지만큼 기운다 — 레코드 라벨이다. 한 번은 전부 똑바로 세웠는데,
   **바퀴가 바퀴로 안 읽혔다**: 도는 것이 아무것도 없고 글자 셋이 자리만 옮긴다.
5. **회전을 말하는 것은 림이 아니라 칸이다.** 원판 면을 가로지르는 흐린 방사선이
   같이 돈다. 림 눈금이 몇 도 움직이는 것은 안 보이고, 살이 면 전체를 쓸고 지나가는
   것은 보인다. **그 개수는 킷을 따르고 각 살이 글자 사이를 이등분한다.**
   그리고 **그 각도는 림에서 파생시킨다** — 한 번은 살을 원판의 회전으로 돌리고
   글자는 읽는 자리에서 놓았더니, 둘이 읽는 각만큼 어긋나 **모든 살이 글자를 정통으로
   관통했다.** 림에 맞춰야 하는 것은 림에서 계산할 것.
6. **눈금이 텍스처다.** 원판 **바깥**에 60개의 가는 눈금과 사분점의 긴 것 넷. 그
   고리는 **돌지 않는다** — 도는 면과 고정된 눈금자, 그 대비가 계기의 읽기 전부다.
7. **가로 컨트롤은 눈금자다.** 윗변에 매달린 가는 눈금(다섯째마다 길게), 가운데가
   선택, 양옆이 회색, 양 끝이 사라진다. 널링과 광택 띠를 넣은 배럴로 그려 봤고 **너무
   많았다** — 기계가 인쇄물에서 렌더로 넘어가고 글자가 졌다. 원통에서 남긴 것은 양 끝이
   사라지는 것 하나뿐이다.
8. **액센트는 읽는 자리 하나에만.** 각 휠이 읽히는 쪽에 점 하나. 그 점이 놓일 자리를
   확보하는 것이 배치의 산술에 들어간다(§아래). 채워진 액센트는 오디션 패드 하나뿐 —
   기계 위에서 스스로 소리를 내는 유일한 것이다.
9. **가장 큰 활자는 기계가 읽고 있는 음절이다.** 별도의 읽기값을 두지 않는다 — 기계가
   스스로 음절을 쓰기 때문이다.
10. **격자도 파인 트랙이다.** 스텝은 그 안에 선 키이고, 비트마다 한 칸씩 음영을
   번갈아 준다. 열여섯 개의 같은 칸은 셀 수 없고, 두 톤이 번갈면 읽을 수 있다 —
   그래서 비트마다 있던 괘선을 지웠다. 한 질문에 두 개의 답이었다.
11. **레인 페이더는 세운 스텝 키다.** 같은 파인 슬롯, 같은 키 — 스텝 키는 *이 레인이
   여기서 난다*고 말하고 페이더 키는 *이 레인이 이만큼 난다*고 말한다. 같은 종류의
   사실 두 개를 노브와 키라는 두 어휘로 그리면 한 격자에 두 언어가 생긴다.
   **그리고 값은 캡의 위치가 아니라 채워진 양으로 읽는다.** 34px 슬롯에서 캡 혼자는
   글자 옆의 짧은 대시이고, 레인 셋이 다른 값이면 흩어진 자국 셋으로 보였다. 슬롯이
   차오르면 여러 레인을 한눈에 훑을 수 있고, 레인이 여러 줄인 이유가 그것이다.
   **헤더 폭은 글자 크기의 배수가 아니다** — 셋 중 둘(×와 페이더)은 활자 크기를
   따라가지 않으므로, 순수한 비례는 작은 쪽 끝에서 셋을 같은 12px에 겹쳐 놓는다.

**비례는 재서 가져온다.** 레퍼런스에서 자모는 원판 반지름의 **0.38**, 스핀들은 0.2,
림은 0.63, 슬라이드는 원판 지름의 1.36배이고 그 글자는 원판 글자의 1.2배다. 한 번은
자모를 0.57r로 뒀는데 **원 안에 갇힌 글자 셋**이 됐다 — 원판이 물건이고 글자가 그
위의 것이어야 한다.

**이것을 사이트 전역 언어로 올리지 않는다.** tool 01의 캔버스에 눈금 달린 원판을 두면
그것은 작품이 되고, 그 도구는 작품을 그리는 도구다. **화면에 기계를 그리는 도구만 이
등록부를 쓴다** — 그리고 지금 그런 도구는 하나다.

### 기계가 스스로 음절을 쓴다 — 읽는 자리가 마주본다

tool 05에서 가장 중요한 배치 결정이고, 두 번째 패스에서야 나왔다. **위 휠은 6시에서,
아래 휠은 12시에서 읽는다.** 둘이 사이의 슬라이더를 향하므로 선택된 세 글자가 한 축에
글자 높이 정도로 나란히 떨어지고, **기계가 자기가 연주할 음절을 쓴다.**

둘 다 12시에서 읽던 첫 버전은 음절이 기계 높이 전체에 흩어져, 글자 사이마다 원판 하나
분량의 빈 곳이 있었다. 그걸 메우려고 옆에 132px 읽기값을 따로 뒀는데 — 기계가 못 하는
말을 다른 데서 대신 한 것이다. 읽는 자리를 마주보게 하자 그 읽기값이 필요 없어졌다.

**그리고 중성은 세 번째 휠이 아니다.** 모음 다섯은 고리가 아니라 목록을 원하고, 두 원판
사이의 틈이 세로모임꼴이 모음을 두는 자리다. 가로 배럴이 그 틈에 정확히 들어간다.
목록은 고리와 달리 **방향이 있으므로 그 방향이 뜻을 가질 수 있다** — 왼쪽이 낮고 어둡고
오른쪽이 밝다. 음정 순서다. 그래서 미는 것이 메뉴에서 고르는 것이 아니라 튜닝이 된다.

### 메인 페이지 (`app/Home.tsx`)

도구들의 2열 구조를 그대로 반복한다 — 큰 스테이지 + 우측 레일. 목록 페이지가
앱의 인터페이스를 먼저 가르친다. 레일의 현재 항목은 **치수 보조선**(액센트
헤어라인 + 5px 실심 틱)이 레일 경계를 넘어 스테이지를 향해 뻗어 표시한다.

**버튼에 특정 도구의 형태를 쓰지 않는다.** 원+알약을 필렛으로 이은 모양은
tool 01의 출력물이라, 모든 버튼에 쓰면 두 번째 도구가 첫 번째의 얼굴을 쓰게 된다.

---

## 3. 작동 원리와 방식

### 구조

```
src/
  app/        셸 — 해시 라우터 · 도구 등록부 · Home(bench)
  shared/     i18n(lang=언어 스토어 · site=셸과 공용 컨트롤의 말 · LangSwitch)
              ui(컨트롤 원형·아이콘·isTyping)
              geometry(vec·polygon·polyline·transform·coverFit·importSvg)
              text(hangul=자모 분해·조합·세 표)
              media(이미지·UI 폰트 메트릭 · type/=도구가 그리는 서체
                    · type/hangul/=조선일보 견고딕, 인덱스에서 닿을 수 없는 자리) · export
              — 등록부의 한글 서체는 `url`이 문자열이 아니라 **함수**다(아래 폰트 항목)
              styles(tokens.css=화면 색 · canvas.ts=작품 색 · controls.css)
  tools/<id>/ 그 도구만의 것 전부: geometry · render · store · types · Panel · icons
              marks · tool.css · copy.ts(그 도구가 하는 말, 두 언어)
public/     빌드 산출물에 그대로 실리는 것 — 라이선스 원문 · tool 04의 wasm 런타임
.github/    workflows/deploy.yml — main에 푸시되면 검사·빌드·배포 (아래 배포 절)
0819_ref/ 0824_* 0825_* 0827_*
            레퍼런스 이미지. 작업 폴더에는 있고 **저장소에는 없다**(`.gitignore`)
```

`tsconfig`는 셋으로 갈라져 있다 — `app`(브라우저) · `node`(vite.config) ·
`test`(단위 테스트). 테스트는 폰트 파일을 디스크에서 읽어야 해서 node 타입이
필요하고, 그걸 앱 설정에 넣으면 앱 코드에서 `window` 아닌 전역이 조용히 통과한다.

**공유 부품을 추측으로 만들지 않는다.** 도구가 하나일 때 플러그인 시스템을
설계하면 두 번째 도구가 틀에 안 맞아 두 번 일한다. 무엇이 공유인지는 두 번째
도구가 알려줄 때 꺼낸다.

**한 번은 세 번째 도구를 예상해서 꺼냈다.** 지금은 없는 실험(건물 타이포그래피)이
필요로 해서 셋을 `shared/`로 옮겼는데, 그 실험은 접었고 **옮긴 것은 남겼다** —
tool 02가 이미 그 자리에서 쓰고 있고, 세 가지 모두 스프루와 무관하다는 판단은
실험과 무관하게 여전히 맞다.

- **폴리곤 헬퍼**(`signedArea`·`bboxOf`·`pointInPolygon`·`roundedRectRing`
  → `geometry/polygon.ts`). 글리프 배관처럼 보였지만 글리프와
  무관한 닫힌 윤곽 기하다. (다섯째였던 `clipHalfPlane`은 그것을 쓰던 가짜 3D가
  기각되면서 같이 지웠다 — 아래 프로토타입 항목.)
- **서체**(`faces.ts`·`fontStore`·`.ttf` → `media/type/`). 서체는 사이트의
  자산이고 한 도구의 것이 아니다. `media/fonts/`는 *인터페이스가 설정된* 웹폰트,
  `media/type/`은 *도구가 그리는* 서체 — 그 구분이 디렉터리 이름이다.
- **서체 읽기**(`applyAxes`·`drawGlyph`·`Seg` → `media/type/measure.ts`).
  함정 둘이 조용하다: `getPath`를 `advanceWidth`보다 먼저, `applyAxes`는 재는
  일과 한 함수 안에.

**교훈은 그래도 반대 방향이다.** 이건 두 번째 도구가 알려줘서 꺼낸 것이 아니라
세 번째를 예상해서 꺼낸 것이고, 그 예상이 틀렸다. 맞는 자산이었던 것은 운이 좋았다.
다음에도 **실제로 두 번째 사용자가 생길 때** 꺼낸다.

**네 번째 도구가 알려준 것은 셋이다.** 전부 "두 번째 사용자가 생겼다"는 신호였고,
셋 중 둘은 **이미 조용히 빌려 쓰고 있던 것**이었다.

- **`outlineData`**(`Seg[]` → SVG 패스 문자열 → `media/type/measure.ts`). `Seg`가
  거기 있으니 그것을 쓰는 방법도 거기 있어야 한다. tool 02는 자기 래퍼를 유지한다 —
  *윤곽선*에는 없고 *컨투어*에는 있는 규칙이 하나 더 있다(닫혀야 한다).
- **`registerBitmap`**(`media/images.ts`). 카메라 프레임은 파일 임포터를 통과하지
  않지만 같은 레지스트리에 있어야 한다. **`releaseBitmap`은 만들지 않았다** — 실행
  취소 스택이 옛 프레임의 id를 들고 있으므로 "낡아 보이는" 항목도 여전히 참조된다.
  대신 캡처 크기에 상한을 걸었다.
- **`.stage-status`**(→ `app/app.css`). 세 도구가 쓰는데 선언이 **tool 02의 시트에만**
  있었다. `main.tsx`가 모든 시트를 불러오기 때문에 우연히 돌고 있었을 뿐이다.

**다섯 번째 도구가 알려준 것도 셋이다.** 전부 "두 번째 사용자가 실제로 생겼다"였고,
이번에는 하나도 예상이 아니었다.

- **한글 분해**(`isSyllable`·`decompose`·`compose`·`CHO`/`JUNG`/`JONG`
  → `shared/text/hangul.ts`). tool 02의 `geometry/` 안에 있었고 **기하가 아니었다** —
  음절의 세 자리는 문자 체계의 사실이다. 스프루에 관한 절반(`splitText`·`PartUnit`·
  `RunnerUnit`)은 tool 02에 남겼다. 프레임은 한글의 사실이 아니다. 그리고 세 표를
  export했다: 05는 림에 올릴 전체 목록과 가획 계열을 알아야 한다.
  **11,172자 왕복 검사도 같이 옮겼다** — `decompose`를 증명하는 검사가 더 이상 그
  함수를 갖고 있지 않은 도구에 남아 있으면 다음 사람이 증명을 엉뚱한 폴더에서 찾는다.
- **조선일보 견고딕**(`ChosunBg.ttf`·`face.ts` → `shared/media/type/hangul/`).
  03과 05가 같은 3.9MB를 쓴다. **`faces.ts`에 넣으면 안 된다** — 그 파일은 인덱스의
  모듈 그래프에 있어서 `?url` 한 줄이 3.9MB를 `dist`로 끌고 온다. 그게 애초에 이
  폰트를 도구 폴더에 넣게 만든 사고였고, 옛 `face.ts`의 주석이 그것을 기록해 뒀다.
  **검사는 간단하다: `app/`의 어떤 파일도 이 모듈을 import하지 않는다.**
  기준 글자가 둘이 됐다 — `REFERENCE`(`한`, 음절을 앉히는 도구용)와
  `JAMO_REFERENCE`(`ㅁ`, 자모를 앉히는 도구용). 자모 하나에 `한`을 쓰면 Size가 말한
  것보다 3분의 1 작아진다.
- **스테이지 위의 장치 칩**(`.magic-deck`/`.magic-chip` → `app/app.css`의
  `.stage-deck`/`.stage-chip`). 04는 카메라를 켜고 05는 트랜스포트를 켜는데, 같은
  물건이다 — 스테이지 위에 떠 있는 칩 한 줄. 04에 남겨 두고 05가 빌려 쓰는 것이
  정확히 `.stage-status` 사고(버그 유형 9)이므로 **04의 마크업도 같이 고쳤다.**
  `.magic-shutter`는 04에 남겼다(사용자 하나) — 그리고 옮기면서 그 버튼이 공유 칩의
  상자를 조용히 물려받고 있던 것이 드러나, 이제 자기 상자를 자기가 적는다.

두 번째 도구(`plastic-type`)가 실제로 알려준 것은 다섯 가지다 —
**패널 머리**(`masthead`·`wordmark`: 모든 도구가 자기 이름을 걸고 그 이름이 나가는
길이다 → `app/app.css`), **`Rect` 타입**(이미지 맞추기 모듈이 갖고 있었지만 프레임
기하와 무관했다 → `geometry/vec.ts`), **테스트용 tsconfig**, 그리고 정리 때
드러난 둘 — **작품 팔레트**(두 도구가 같은 종이에 인쇄한다 →
`styles/canvas.ts`)와 **`isTyping`**(창 단위 단축키를 갖는 도구마다 필요하다 →
`ui/typing.ts`. 두 번째 도구가 다시 쓰면서 `isContentEditable`을 빠뜨렸고, 그게
함수 하나로 막히는 종류의 어긋남이다).

**합치지 않은 것도 있다.** 두 `ToolPanel.tsx`는 스무 줄이 거의 같지만, 인자
셋(Panel · store · shortcuts)을 받는 팩토리가 그 중복보다 읽기 어렵다. 중복이
더 정직한 자리도 있다.

`app/tools.ts`의 `Tool` 인터페이스는 **도구 다섯을 지나도록 한 줄도 안 고쳤다** —
그중 하나는 WebGL이고 하나는 카메라를 들고 있고 하나는 오디오 클럭을 들고 있는데도
그렇다. **여섯 번째로 그것을 고친 것은 도구가 아니라 언어였다**: `blurb`·`spec`이
언어당 하나가 되면서 `copy: Record<Lang, ToolCopy>` 하나로 접혔다. 인터페이스를
지키는 판도 만들어 봤다 — `shared/`에 도구 id로 색인한 번역표 — 그리고 더 나빴다.
도구의 말이 도구 밖에 살게 되고, 그건 `tools/<id>/`가 가진 유일한 규칙이다.
다섯 도구 동안 그 모양이 맞았던 것은 여전히 맞고, 트로피였던 적이 없을 뿐이다.

**도구의 말은 `tools/<id>/copy.ts`에 있고, 한국어가 영어를 타입으로 받는다**
(`type Copy = typeof en` → `const ko: Copy = {...}`). 한쪽에만 추가한 줄이 빌드
오류가 되지, 화면에 엉뚱한 언어로 뜨는 구멍이 되지 않는다. 렌더 밖에서 나는 말
(카메라 실패·붙여넣기 알림·이미지 임포터)은 `useLangStore.getState()`로 **그 순간**
읽는다 — 오디오 스케줄러가 문서를 읽는 규칙과 같고, 이유도 같다.

**의존 방향은 한쪽뿐이다.** `shared/`는 `tools/`를 절대 참조하지 않는다.
범용 타입(`Transform`, `DocImage`)은 그것을 다루는 shared 모듈이 소유하고,
도구가 재수출한다.

### 도구 하나를 추가하는 방법

1. `src/tools/<id>/` 를 만들고 자기 store · Panel · Stage · 아이콘 · CSS를 둔다
2. `copy.ts`에 그 도구가 하는 말을 두 언어로 적는다 — 영어를 먼저 쓰고
   `type Copy = typeof en`으로 한국어를 묶는다
3. `index.ts`에서 `{ id, name, copy, Preview, Panel, Stage }`를 내보낸다
4. `app/tools.ts`의 `TOOLS`에 추가한다
5. `main.tsx`에 그 도구의 `tool.css`를 추가한다
6. 패널 머리에 `<LangSwitch />`를 단다 — 도구 안에서도 언어를 바꿀 수 있어야 하고,
   자리는 `masthead-side`로 다섯 도구가 같다

Panel은 `EditSessionProvider`로 감싸 자기 스토어의 `pushHistory`/`setInteracting`을
공유 컨트롤에 연결한다 (`shared/ui/editSessionContext.ts`). 함수 두 개짜리
컨텍스트이고, 의존성을 끊기 위한 것이지 프레임워크가 되려는 것이 아니다.

**무거운 의존성을 가진 도구는 자기 안에서 지연 로드한다.** tool 03은 three.js를
쓰는데 그건 나머지 사이트 전체만 한 크기다. 안 여는 사람이 그 값을 치를 이유가
없으므로 `index.ts`에서 `Preview`·`Panel`·`Stage`를 각각 `lazy` + `Suspense`로
감싸고, 겉은 평범한 컴포넌트로 내보낸다. **`Tool` 인터페이스는 여전히 한 줄도
안 고쳤다** — 도구 넷이 지나도록 그대로라는 것이 그 모양이 맞다는 증거다.
결과: 인덱스 청크 541KB(gzip 166KB), three는 별도 청크, 한글 폰트는 별도 에셋.

**단은 필요한 만큼만 가른다.** tool 04는 손 런타임 + 모델이 20MB인데 **`Preview`는
지연시키지 않았다.** 그 카드가 필요한 건 인덱스가 이미 갖고 있는 라틴 3종뿐이고,
지연시키면 첫 화면에 빈 칸만 남는다. `Panel`·`Stage`만 가르고 — 그리고 **그 안에서
한 단 더 가른다**: 도구를 열면 도구 코드(9KB)만 오고, `hand/tracker.ts`가 **카메라를
켤 때** 런타임과 모델을 받는다. 실측: 인덱스에는 한 바이트도 없고, 셔터를 쓸 사람만
`vision_bundle`(gzip 45KB) + wasm 11.8MB + 모델 7.8MB를 받는다.

**tool 05는 가를 것이 서체 하나뿐이다.** Web Audio는 네이티브라 신스·스케줄러·WAV
작성기 전부가 자기 소스 크기 말고는 0바이트다. 그런데 그리는 모든 부분이 3.9MB
서체를 필요로 하므로(인덱스 카드까지) **tool 03의 모양**을 따랐다 — 셋 다 `lazy`.
그 안에서 한 번 더 가른다: `audio/render.ts`는 **Export를 누를 때** import한다.
실측: 인덱스 청크 542.92KB(gzip 166.61)로 tool 04 시점과 사실상 동일하고,
`voiceFor`·`playVoice`·`patternOf`·`deckOf`·`ChosunBg`·`encodeWav`·`AudioContext`가
인덱스 청크에 **0회** 등장한다. 도구를 열면 약 31KB, WAV 작성기는 그 뒤 1.66KB다.

**tool 02는 도구가 아니라 컨트롤 하나를 기준으로 가른다.** 형태가 둘인데 기본값은
평면이고 평면에는 렌더러가 필요 없으므로, 이음매를 **렌더러가 필요한지를 정하는 그
한 컨트롤** 자리(`render/Stage.tsx`)에 뒀다. 모델 내보내기 모듈도 클릭 시점에
import한다 — 패널은 인덱스 청크에 있으므로 최상단 import 한 줄이면 three가 딸려온다.
그리고 **`geometry/`는 three를 몰라야 한다**: 평면 형태도 그 폴더를 쓰기 때문에,
거기 한 줄이 들어가면 전부가 인덱스로 온다. three를 만나는 파일은
`render/shapes.ts` 하나다.

**로더가 자기 형제 파일 경로를 문자열로 만드는 에셋은 `public/`에 둔다.**
`vision_wasm_internal.js`는 자기 `.wasm` 경로를 자기 경로에서 치환으로 만들기 때문에
Vite의 콘텐츠 해시가 그 치환을 깨뜨린다. 모델(`.task`)은 그 제약이 없어서 도구 폴더
안에서 `?url`로 실린다 — 같은 패키지에서 온 두 파일이 다른 자리에 있는 이유이고,
`models/NOTICE.md`에 적혀 있다.

### 프로토타입은 별도 엔트리로 만든다

아직 고를 것이 남은 실험은 **사이트에서 닿을 수 없어야 한다.** 등록부에 올리는 것도,
라우터에 dev 전용 분기를 넣는 것도 아니고, **루트에 자기 `.html`을 하나 둔다**
(`proto-billboard.html` → `tools/<id>/proto/main.tsx`). Vite는 빌드 입력이 `index.html`
하나뿐이라 개발 서버에서만 열리고 프로덕션에는 안 실린다 — 셸·등록부·`main.tsx`를
한 줄도 고치지 않는다는 것이 요점이다.

기하는 도구의 `geometry/`에 정상 모듈로 두고 단위 테스트를 붙인다. 렌더러만
`proto/`에 있다. 그러면 채택될 때 옮길 것이 UI뿐이고, 안 될 때 지울 것도 UI뿐이다.

**채택되면 프로토타입은 지운다.** tool 03이 등록되면서 `proto-billboard.html`과
`proto/`를 삭제했다. 같은 렌더러의 사본 둘을 남겨 두면 반드시 갈라지고, 그때
어느 쪽이 진짜인지 아무도 모른다. 프로토타입은 고를 것이 남아 있는 동안에만 있다.

**기각돼도 지운다.** tool 02의 입체 프로토타입(`proto-solid.html` +
`geometry/axon.ts`, SVG 안에서 사등각 측면을 직접 그리는 것)은 돌아갔지만 **방식이
기각됐다** — 진짜 3D로 갈아탔다. 그래서 렌더러·벤치·투영 기하를 다 지웠다. 기각된
쪽을 "참고용"으로 남기면 그것이 진짜의 사본으로 오해받고, 지금 두 형태를 가진 도구
안에서는 셋째 형태처럼 보인다. **다만 그 프로토타입이 검증한 것 중 살아남은 것은
남긴다** — `shared/geometry/polygon.ts`의 `roundedRectRing`은 입체 프레임이 쓰고
있고, 그것과 `roundedRectPath`를 맞대는 검사는 `runner.test.ts`로 옮겼다(그 검사가
거울상 감김 버그를 잡은 오라클이다). 반대로 `clipHalfPlane`은 진짜 3D에서 필요가
없어져서 함수째 지웠다.

### 세 가지 핵심 원리

**① 화면에 보이는 SVG가 곧 내보내는 SVG다** (01·04, 그리고 02의 평면 형태.
03과 02의 입체 형태는 WebGL이라 해당 없다 — 거기서는 반대 규칙이 산다: **파일은
씬에서 꺼내지 않고 문서에서 다시 만든다.** 조명·카메라·사람이 둔 시점은 *보는 일*에
속하고, 돌아가는 렌더러에 손을 넣어 그것들을 벗기는 것이 파일과 그림이 조용히
갈라지는 방식이다. 그래서 뷰와 export가 같은 `buildSolid` 하나를 부른다).
Canvas가 아니라 SVG DOM을 직접 렌더링한다. export는 그 노드를 복제해 편집용
오버레이(`data-ui`)만 걷어낸다. 화면과 파일이 어긋날 경로가 없다. 그래서 배경
이미지를 `<image href="data:...">`로 렌더링하자 **export 코드를 한 줄도 고치지
않고** 그대로 파일에 실렸다.

**tool 05가 이 원리를 그림 밖으로 데려갔다.** 소리에는 "화면에 보이는 것"이 없으니
같은 규칙이 다른 형태로 산다: **파일은 스피커에서 녹음하지 않고 문서에서 다시
만든다.** `OfflineAudioContext`에 문서를 깔고, 라이브 트랜스포트가 부르는 것과 **같은
`playVoice`**를 부른다. 녹음이라면 그때 브라우저의 출력단이 한 일까지 실려 나가고,
무엇보다 두 번째 파이프라인이 되어 조용히 갈라질 수 있다 — 02·03의 `buildSolid`가
이미 정한 것이고, 매체가 소리일 때 더 크게 맞다. 실측으로 확인했다: 렌더된 파일의
길이가 `loopLength`와 소수점까지 같고, 온셋 격자가 312.5ms 스텝을 유지한다.
그리고 오프라인 컨텍스트는 사용자 제스처를 요구하지 않으므로 **한 번도 재생하지 않고
내보낼 수 있다** — tool 04가 "CAPTURE FIRST"라고 말해야 했던 자리가 여기엔 없다.

**tool 04가 이 원리를 한 번 더 확인했다.** 라이브 피드는 SVG 안에 없다 —
투명한 SVG 뒤의 진짜 `<video>`다(프레임마다 `toDataURL`을 부르는 대안은 손이
따라올 수 있는 이유 자체를 없앤다). 그래서 **셔터를 누르기 전에는 문서에 지면도
사진도 없고**, export가 그 사실을 말한다("CAPTURE FIRST"). 셔터가 프레임을
`<image>`로 문서에 넣는 순간 export는 다시 아무것도 몰라도 되는 코드가 된다.
글자는 `<textPath>`가 아니라 실제 윤곽선이다 — 원 위의 글자도 디자이너가 점을
잡을 수 있어야 한다.

**② 병합은 SVG 필터가 아니라 실제 기하학으로 계산한다.**
`feGaussianBlur` 트릭은 편집 가능한 패스가 아니고 외곽선 모드가 불가능하다.
대신 거리장(SDF)을 합쳐 마칭 스퀘어로 등고선을 뽑고 3차 베지어로 맞춘다.
**필렛 유니온**(`fOpUnionRound`)은 두 거리가 모두 반경 안일 때만 값이 바뀌고
그 밖에서는 정확히 `min(a,b)`가 된다 — 이 "그 밖에서는 정확히"가 원을 원으로
남기는 이유다. smooth-min은 이웃이 근처에 있기만 해도 표면 전체를 밀어낸다.

**③ 무거운 것은 문서 밖에, 원본은 그대로.**
- 실행 취소는 `structuredClone`으로 문서를 스냅샷하므로 **픽셀은 문서 밖
  레지스트리**에 두고 문서는 id만 갖는다 (`shared/media/images.ts`)
- 브러시 원본 궤적(`raw`)은 보관하고 스무딩은 매번 다시 적용한다 — 그려진 뒤에도
  부드러움을 조절할 수 있어야 한다
- 배치(이동·배율·회전)는 지오메트리에 굽지 않고 그룹의 SVG `transform`으로만
  적용한다 — 재계산 없이 즉각 반응하고 정밀도가 유지된다

### 성능 방식

**최적화는 항상 측정 후에.** 그리고 **무엇이 느린지 추측하지 말고 갈라서 재라.**
두 도구에서 답이 정반대로 나왔다.

- tool 01은 **정밀도**가 비용이었다. 드래그/재생 중 거친 그리드(3~4px), 손을 떼면
  고운 그리드(0.9px). 노드는 자기가 영향 주는 셀만 래스터화한다. 200노드 병합
  재생이 최악 13ms.
- tool 02는 **검색 범위**가 비용이었다. 문단을 프레임 하나에 담으면 46ms였는데,
  곡선 평탄화가 아니라 게이트 걸 자리를 찾느라 조각 78개가 앵커 70개 전부와
  비교되고 있었다. 박스 거리로 먼 앵커를 잘라내(진짜 하한이라 답이 같다) 5.4ms.
  **그러고 나니 저품질 패스가 고품질과 같은 값이 나와서 지웠다** — 없어진 비용을
  위한 장치는 남기지 않는다.
- tool 02의 **입체 형태는 비용이 카메라가 아니라 문서**다. 궤도 한 프레임이 4.8ms인
  것은 문서가 안 바뀌면 기하를 다시 만들지 않기 때문이고, 반대로 문서가 바뀌면
  압출을 다시 한다(KIOSK 2.2ms · 45자 문장 12ms). 그래서 드래그 대상이 깊이일 때가
  가장 비싸다 — 프로덕션 빌드에서 KIOSK는 p90 7ms, 45자 문장은 p90 29ms(최악 65).
  여기서 **두 번 재보고 두 번 뺐다**: 그림자맵은 215만 픽셀 중 680개만 바꾸면서
  30ms를 쓰고 있었고, 색깔별 지오메트리 병합은 문서 변경마다 13ms를 더 쓰면서
  프레임을 1ms도 못 줄였다(움직이는 것이 카메라뿐이니 드로우콜이 병목이 아니다).

- tool 03은 **비용이 프레임이 아니라 씬 구축**이다. 환경맵 + AO 패스로 한 프레임
  9ms인데, 움직이는 것이 아무것도 없으므로 **상시 루프를 돌리지 않는다** — 문서가
  바뀔 때와 리사이즈 때만 그린다. SVG 도구들과 같은 규칙이고, 같은 이유다.
- tool 04는 **비용이 프레임이 아니라 첫 프레임**이었다. 손 추론은 7~9ms(최악 24ms)로
  30fps 예산 안에 넉넉히 들어오는데 **첫 호출만 수 초**다(GPU 가속 없는 환경에서
  6.5초 실측). 그래프의 셰이더가 그때 만들어지고 그 일이 메인 스레드를 막으므로,
  피드가 뜨는 순간 도구가 멈춘 것처럼 보인다. 그래서 모델을 만든 직후 **빈 캔버스로
  한 번 돌려 놓는다** — 비용이 "starting the camera"라는, 스테이지가 이미 설명하고
  있는 상태 안에서 치러진다. 그 뒤 첫 라이브 프레임은 12ms.
  글자 쪽도 프레임 비용이 아니다. 윤곽선은 `GLYPH_EM`에서 **한 번** 뽑아 캐시하고,
  매 프레임 바뀌는 것은 행렬 여섯 개뿐이다 — `d` 속성은 손대지 않는다. 줄마다 크기가
  달라도(`taper`) 같다: 크기는 행렬의 배율이지 다시 뽑을 이유가 아니다.

- tool 05는 **비용이 프레임이 아니라 시계**다. 그래서 최적화가 아니라 **구조**가
  답이다: 거친 `setInterval`이 25ms마다 깨어나 120ms 앞까지 *예약만* 하고, 실제
  연주는 오디오 클럭이 한다. 인터벌이 수십 ms 늦어도 아무것도 안 밀린다 — 모든 음이
  이미 샘플 클럭 위의 정확한 `startTime`을 갖고 있다. **실측: 23스텝에서 간격 오차
  0.000000ms, 누적 0ms.** 스윙도 그 자리에서 확인했다(홀수 스텝 82.5ms, 목표와 일치).
  그리는 쪽도 프레임 비용이 아니다. 윤곽선은 EM 1000에서 **한 번** 뽑아 캐시하고,
  플레이헤드는 rAF가 `ctx.currentTime`을 읽어 노드의 `transform`을 직접 쓴다 —
  React를 통하지 않는다. **실측: 2.5초 재생 중 `transform` 301회, `d` 변경 0회,
  DOM 추가·삭제 0회.**

**Web Worker는 쓰지 않는다** — 실측해서 필요 없었다.

### 검증 방식

- 기하 모듈은 DOM 의존이 없어 단위 테스트 대상 (`npm test`, 현재 371개)
- **타입이 못 보는 것만 검사한다 — 번역표가 그 예다.** `type Copy = typeof en`이
  *빠진* 한국어 줄을 이미 빌드 오류로 만든다. 못 잡는 것은 **영어인 채로 남은 한국어
  줄**이다: 모양도 타입도 맞고, 한국어 패널 안에서 조용히 영어가 나온다.
  `shared/i18n/copy.test.ts`가 여섯 사전을 다 훑어 두 언어가 같은 문자열인 자리를
  찾는다. 템플릿은 비교하지 않고 **호출한다** — 절반이 숫자나 글자를 끼워 넣는
  함수이기 때문이다. 두 실패 모드를 각각 심어서 검사가 실제로 무는 것을 확인했다.
- **외부 자산은 넣기 전에 심사하고, 그 심사를 테스트로 남겨라.** 폰트 하나가 카운터를
  살로 읽히게 만들어서 글자가 메워져 나왔다. 눈으로 브라우저와 대조해서 찾았고,
  이제 `glyphs.test.ts`가 "카운터 있는 글자는 붙어 있는 살 하나"를 세 서체 × 두
  굵기로 검사한다. **무엇을 세는지가 중요하다** — 구멍 수는 디자인의 문제라 정상인
  서체까지 걸렸고, 조각 수는 그렇지 않다.
  **한글 서체에는 같은 심사의 한글 판이 붙는다**: ㅁㅂㅇ은 구멍을 가진 한 조각으로
  남고, ㅃ은 두 조각이며(닫힌 상자 둘은 붙을 수가 없다), 붙은 적 없는 자모는 떨어져
  있어야 한다(`이`=2, `한`≥3). **ㅆ·ㅍ은 단정하지 않는다** — Gothic A1은 ㅆ을 붙여
  그리고 UnJamo는 떼어 그리며, ㅍ은 반대다. 둘 다 정당한 드로잉이고, 라틴 쪽에서
  "구멍 수는 세지 않는다"고 배운 것과 같은 자리다
- **검사의 비용은 무엇을 재느냐가 아니라 어떻게 재느냐다 — 그리고 CI는 이 기계가
  아니다.** `build.test.ts`가 KIOSK를 16번 압출해 정점 좌표를 *하나하나* `expect`로
  단정하고 있었다. 여기서 2327ms였고 2코어 CI 러너에서는 5초 기본 타임아웃을 넘겨
  배포가 통째로 죽었다. **비용은 기하가 아니라 `expect`였다** — 하나를 만드는 값이
  숫자를 읽는 값의 마흔 배다. 평범한 루프로 훑어 **처음 어긋난 인덱스 하나만** 단정하니
  58ms가 됐고, 커버리지는 같다(NaN을 심어 인덱스를 짚는 것까지 확인). **타임아웃을
  키워서 덮지 말 것** — 그러면 진짜로 멈춘 검사가 같이 숨는다. 단정을 루프 안에 두는
  자리는 전부 같은 함정이다.
- **불변식이 있으면 조합 전체에 걸어라.** tool 02의 "모든 조각에 게이트가 최소
  하나"는 텍스트 5종 × 부품 단위 4종 × 런너 단위 4종 × 밀도 3종 = 240조합을 돈다.
  이 도구에서 이게 깨지면 부품이 바닥에 떨어진다.
  **한글이 같은 검사를 다시 480조합 돈다**(서체 2종 × 텍스트 5종 × 4 × 4 × 3).
  같은 불변식이지만 훨씬 센 시험이다 — 음절 하나가 조각 다섯이라 프레임 하나에
  부품이 다섯 들어가고, 게이트를 걸 벽이 그만큼 모자라다
- UI는 브라우저에서 실제로 조작해 확인한다. 합성 포인터 이벤트로 그리고 끌고,
  값·좌표·색을 실측한다
- **드로잉과 제스처가 같은 좌표를 쓰는지 확인할 것.** tool 05의 원판 드래그는 중심을
  핸들러 안에서 다시 계산하면서 박스 원점을 빼먹었다 — 그림은 정확했고 제스처만 90px
  옆의 점을 기준으로 각도를 재서 **휠이 그냥 반응하지 않았다.** 그림이 맞는다는 것이
  이 버그의 위장이다. 오프셋은 한 곳에서 파생시켜 양쪽이 쓰게 하고, 그 계산을 그것을
  쓰는 핸들러보다 **위에** 둘 것
- **측정 순서를 오염시키지 말 것.** 후속 동작을 한 뒤에 재면 안 되고, React가
  배치한 상태를 같은 tick에 읽어도 안 된다. 한 호출 안에서 처음부터 재현할 것
- **소리는 스크린샷이 안 찍힌다. 그러니 버퍼를 읽어라.** tool 05의 검증은 전부
  `OfflineAudioContext`로 한 음절을 렌더해 **숫자로** 본 것이다 — 피크·RMS·밴드
  에너지·자기상관 기음·엔벨로프 온셋. 그 방법이 잡은 것: 자모 68개 조합에서 무음 0·
  클리핑 0(피크 0.22~0.55), 모음 여덟 개의 기음이 표에 적힌 55·62·73·82·98·110·123·
  147Hz와 정확히 일치, ㅘ가 실제로 66→94Hz로 미끄러짐. **그리고 눈으로는 절대 못
  찾을 버그 둘을 잡았다** — 아래 유형 12의 오디오 판 참조.
- **비교하려면 같은 창에서 재라 — 길이의 백분율은 창이 아니다.** tool 05의 꼬리
  밝기를 "길이의 45%부터 끝까지"로 쟀더니 공란(720ms의 45%)과 ㅁ(220ms의 45%)이 서로
  **다른 시점**을 보고 있었고, 순서가 뒤집혀 나왔다. 같은 절대 구간(60~150ms)으로
  다시 재고 나서야 표의 진짜 버그가 드러났다 — 공란의 `close`가 ㅇ보다 크다는,
  얼굴만 봐도 틀린 줄이다. **첫 측정이 이상하면 값을 의심하기 전에 창을 의심할 것**
  (유형 11의 형제다: 측정 오류가 버그처럼 보이고, 반대로 버그를 가리기도 한다).
- **참조 자료를 자산이 아니라 계측기로 쓸 수 있다.** tool 05의 소리를 TTS 녹음에
  맞췄는데 샘플은 한 바이트도 안 들어갔다 — 재생하면 자모가 라벨이 되고 가획도 트림도
  할 일이 없어지기 때문이다. 대신 합성음과 **같은 계측**으로 재고 절대값이 아니라
  **비율**을 옮겼다. 그렇게 해서 나온 것이 추측으로는 안 나왔을 사실 넷이다(꼬리가
  아니라 *시점*이 초성을 가른다 · 공명음은 다 같이 유성이다 · 파열음끼리는 가깝고
  파찰음은 멀다 · 포먼트는 절대적이다). **레퍼런스를 베끼는 것과 재는 것은 다르다.**
- **소리의 매핑은 상수가 아니라 순서를 검사한다.** `voice.test.ts`는 어떤 숫자도
  단정하지 않는다. 단정하는 것은 **어떤 튜닝에서도 참이어야 하는 관계**다: 가획 다섯
  계열 전부에서 세기가 오르고 악기는 안 바뀐다, 종성의 꼬리가 ㅁ<ㄴ<ㅇ<없음이다,
  중성의 밝기가 여덟 개에 걸쳐 단조롭다, 초성 ㅇ만 어택이 없다, 음절말 중화가 일곱
  소리로 접힌다. 귀로 고른 상수는 한 사람의 상수라는 tool 04의 교훈이 소리에서 특히
  맞다. **그리고 trim은 그 순서를 뒤집을 수 없다** — 극단값 다섯 세트에서 검사한다
  (등호는 허용한다: clamp가 양 끝을 눌러 두 값이 같아지는 것은 컨트롤의 끝이고,
  뒤집히는 것은 버그다).
- **하드웨어에 붙은 부분은 하드웨어 없이 재현되게 갈라 놓는다.** tool 04의 제스처
  수학은 `hand/landmarks.ts`에 있고 카메라·모델·DOM을 전혀 모른다. 그래서 21점
  **합성 손**(굽힘 하나로 주먹↔펼친 손을 만드는 파라메트릭 손)으로 검사할 수 있다.
  검사하는 것은 특정 숫자가 아니라 **어떤 손에나 참이어야 하는 것**이다 — 손가락을
  접으면 openness가 절대 올라가지 않는다, 같은 손을 옮기고 돌리고 가까이 가져와도
  값이 같다, 주먹은 0에 붙고 펼친 손은 1에 붙는다. 한 사람 손에 맞춘 상수는 그
  사람의 상수일 뿐이다
- 개발 모드에서 `window.__store` / `window.__plasticStore` / `window.__magicStore` /
  `window.__beatStore`로 실제 스토어에 접근할 수 있다. tool 05는 `window.__beatAudio`로
  **트랜스포트와 그 예약 로그**(이상 시각 대 실제 `startTime` 짝)를 준다 — 드리프트를
  숫자로 볼 수 있는 유일한 길이고, 단위 테스트에는 오디오 클럭이 없고 귀는 0.5ms를
  못 잰다. tool 02의 입체 형태는 `window.__plasticSolid`로
  **렌더러·카메라·컨트롤·컴포저**를 준다 — 프레임 비용, 실제 카메라 행렬, AO 버퍼처럼
  단위 테스트가 닿을 수 없는 것을 재는 유일한 길이다. `await import()`는 Vite의 HMR 쿼리 때문에 **별개
  인스턴스**를 준다 — 주의
- `window.__magicHand()`는 카메라 없이 wasm 런타임과 모델을 세워서 돌려 준다.
  헤드리스 브라우저에 카메라가 없으므로, 이 도구에서 단위 테스트가 못 닿는 유일한
  부분(모델이 실제로 로드되고 답하는가)을 재는 길이 이것뿐이다

### 반복해서 나온 버그 유형

1. **React state를 제스처 로직에서 읽기.** 배치된 pointermove가 통째로 사라지거나
   드래그가 끝나지 않는다. 제스처는 항상 ref와 pointerdown 시점 값으로 계산.
   **tool 05에서 또 걸렸다 — 그 규칙을 자기 주석에 인용해 놓은 파일에서.** 플래터의
   `onSpinEnd`가 회전값을 React state에서 읽고 있었다. 실제 손으로는 *돌아간다*,
   React가 pointermove 사이에 다시 그려 주기 때문이다. 합성 이벤트로 제스처 전체를
   한 tick에 흘리면 클로저는 생성 당시의 `null`을 그대로 들고 있고 스핀이 아무데도
   안 떨어진다. **"실제로는 돌아간다"가 이 버그의 위장이다** — 그래서 합성 포인터로
   검사하는 것이 우연이 아니라 방법이다. 지금은 ref에 두 번째 사본을 들고 그것을
   읽는다: state는 그리는 것, ref는 제스처가 읽는 것.
2. **`setPointerCapture` 미보호.** 예외가 나면 뒤 코드가 통째로 안 돈다
3. **토큰 이름 오타.** CSS는 `var()`가 없는 변수면 **선언 전체를 무효화**한다.
   조용히 사라지므로 `currentColor` 같은 우회로를 쓰면 안전하다.
   **실제로 걸렸다** — `app.css`가 `var(--sunk)`(정의된 이름은 `--paper-sunk`)를
   써서 메인 페이지 미리보기 카드의 배경이 며칠간 투명했다. tool 01의 미리보기가
   같은 값을 스스로 하드코딩하고 있었던 것이 그 흔적이다. 눈으로는 "원래 그런
   디자인"으로 보여서 안 잡혔고, **`var()` 이름과 `tokens.css` 선언을 diff해서**
   잡았다. 그 대조를 정리 때마다 한 번 돌릴 것
4. **거울상 기하를 손으로 뒤집기.** 둥근 사각형을 반대로 감을 때 호와 직선이 한 칸
   밀렸다. 뒤로 돌면 모서리 호부터 시작하는데 변부터 시작한 탓이다. 그러면 호가
   모서리가 아니라 변 전체를 훑어서 링의 구멍이 닫히고 프레임이 통째로 칠해진다.
   **테스트가 서브패스 개수만 세고 있어서 못 잡았다** — 두 감김의 끝점 순서가 서로
   역순인지 비교해야 잡힌다
5. **모듈 최상단의 개발용 `window` 훅.** `if (import.meta.env.DEV)`만으로는 node
   테스트에서 그 모듈을 import하는 순간 죽는다. `typeof window !== 'undefined'`를
   같이 볼 것
6. **폰트 라이브러리에서 그리기 전에 재기.** opentype.js는 베리어블 폭 변이를
   글리프 변환 안에서 적용하고 그 변환은 `getPath`가 촉발한다. `advanceWidth`를
   먼저 읽으면 기본 인스턴스 값이 나와서 무거운 굵기에서 글자가 겹친다.
   **두 줄의 순서가 결과를 정한다** — 순서에 의존하는 곳은 주석으로 못박을 것
7. **라이브러리의 편의 함수를 진단에 쓰지 말 것.** opentype의 `toPathData`가 `NaN`을
   찍어서 한참 그걸 원인으로 쫓았는데, 커맨드 데이터 자체는 정상이었고 그 문자열
   헬퍼는 우리 코드가 쓰지도 않는 것이었다. **진단은 실제로 쓰는 경로에서 할 것**
8. **전역 상태를 만드는 세터를 두 군데서 부르기.** `variation.set`은 파싱된 폰트를
   움직이므로, 축을 세팅하는 일과 그 상태에서 재는 일은 한 함수 안에 같이 있어야
   한다. 나눠 두면 마지막 호출이 이기고 앞의 측정은 존재하지 않는 폰트를 설명한다
9. **한 도구의 CSS를 다른 도구가 몰래 빌려 쓰기.** `main.tsx`가 모든 `tool.css`를
   불러오므로 클래스는 전부 전역이다. `.stage-status`는 tool 02의 시트에만 선언돼
   있었는데 세 도구가 쓰고 있었고, `.export-btn`은 shared에 있는데 **tool 02가
   덮어쓰는 사본**을 갖고 있어서 네 도구 전부의 버튼 모양을 tool 02가 정하고 있었다.
   둘 다 "그냥 돌기" 때문에 안 보인다. **shared에 있는 선언과 도구 시트의 선언을
   교차로 훑는 것을 3번 항목의 `var()` 대조와 같이 돌릴 것**

   **그리고 그것을 옮기면서 두 번째 버그가 나왔다.** `.stage`에 `position`이 없어서
   `.stage-status`(absolute · `bottom:18px left:20px`)가 **뷰포트를 기준으로** 잡히고
   있었다 — 20px는 296px 패널 안쪽이므로 tool 02의 상태 문구는 스테이지가 아니라
   패널 위에 그려지고 있었다. tool 03만 자기 `.billboard-stage`에 `position: relative`를
   따로 갖고 있어서 멀쩡했다. `overflow: hidden`도 `transition`도 배치 기준을 만들지
   않는다. **absolute를 쓰는 쪽이 아니라 그 부모를 확인할 것.**
10. **활자의 자리는 재서 확인한다 — 캡 높이만 계산하면 디센더가 다음 단을 먹는다.**
   tool 04의 띠는 캡 높이만큼만 자리를 잡고 있었다. 글자는 띠 위에 발을 딛고 서므로
   `p`·`g`의 꼬리는 **안쪽 단으로 넘어간다.** 눈으로는 "좀 좁네" 정도로 보였는데
   렌더된 패스의 반경을 재 보니 둘째 줄의 꼬리와 셋째 줄의 캡이 **똑같이 197px**
   이었다 — 거터를 아무리 줘도 거짓말인 상태다. 이제 띠가 캡 높이의 0.26을 더 잡는다.
   **활자 관련 레이아웃은 계산으로 끝내지 말고 렌더된 것의 좌표를 뽑아 볼 것.**
11. **재기 전에 상태를 밀어 놓고, 같은 tick에 재기.** 3번·측정 항목에 이미 적혀
   있는데 이번에도 걸렸다. `setState`로 bloom을 1로 올리고 곧바로 DOM을 직렬화해서
   반지름을 읽었더니 239.25가 나왔다 — React가 아직 다시 그리지 않았으니 그건 이전
   bloom의 정답이었다. **값이 산술적으로 설명되면 버그가 아니라 측정 오류다.** 다시
   재기 전에 이 가능성을 먼저 지울 것
12. **다른 도구에서 상수를 빌릴 때는 단위를 확인할 것 — 그리고 패스가 도는지가 아니라
   무엇을 내놓는지 재라.** tool 02의 입체 형태가 AO 파라미터를 tool 03에서 그대로
   가져왔는데, `radius`와 `thickness`는 **월드 공간 거리**다. tool 03의 씬은 캡 높이
   단위(간판 하나가 1)이고 tool 02의 시트는 픽셀 단위(가로 1000)이므로, 그 숫자는
   "1.5픽셀 안의 차폐물을 찾아라"가 된다. 패스는 정직하게 돌면서 **215만 픽셀 전부
   같은 회색**을 내놓았다 — 비용은 다 치르고 결과는 0이다. 화면으로는 "AO가 은근한
   것"으로 보인다. **버퍼를 읽어서** 잡았고(`readPixels` + 히스토그램), 같은 방법으로
   그림자맵이 680픽셀만 바꾸고 있는 것도 잡았다. **효과를 켠 값과 끈 값을 픽셀로
   비교하는 것을 렌더 파이프라인을 손댈 때마다 한 번 돌릴 것.**

   **오디오에서는 이 유형이 두 번 나왔고, 둘 다 화면으로는 정상이었다.** tool 05의
   초성 매핑은 조음 위치(`tint`)로 파열음의 음정 하강만 움직이고 있었다 — 산술은
   맞고 노드는 다 돌고, 렌더 버퍼를 재 보니 **ㅂ 0.027 · ㄱ 0.029 · ㄷ 0.027**로
   파열음 셋이 7% 안에 붙어 있었다. 하나의 회색이다. 지금은 `tint`가 버스트를
   **톤과 노이즈로 쪼갠다**(순음=거의 톤, 치음=노이즈뿐): 0.095 · 0.340 · 0.453.
   두 번째는 몸통의 밝기 필터였다. 55Hz 삼각파는 배음이 1/n²로 떨어져 2kHz 위에
   아무것도 없으므로, 컷오프를 266Hz에서 5kHz로 열어도 통과시킬 것이 없었다 — 여덟
   모음이 전부 0.048~0.056으로 평평했다. 톱니파(1/n)로 바꾸니 0.43→0.84로 단조가
   됐다. **필터를 의심하기 전에 소스에 재료가 있는지 확인할 것.**

   **그때 같이 넣은 "컷오프를 기음의 배수로" 는 틀렸고, 세 번째 패스에서 그것이
   드러났다.** 재료 문제는 톱니파가 고쳤고 배수는 덤이었는데, 55Hz 킥에서 그 배수는
   157Hz다 — 톱니파를 자기 기음까지 깎으면 사인파이고, 녹음과 대 보니 고역 함량이
   실제의 10분의 1이었다. **포먼트는 절대 주파수다.** 같은 ㅜ가 높이 불러도 낮게
   불러도 같은 ㅜ인 이유가 그것이다. 지금은 절대값이되 기음 아래로는 안 내려간다
   (`max(pitch*2.4, 230 + open*1700)`). **유형 12의 교훈은 "상대값을 써라"가 아니라
   "단위가 무엇인지 알아라"이고, 물리가 절대적이라고 말하는 곳에서는 절대값이 맞다.**

13. **오디오 스케줄러에서 React state 읽기.** 유형 1의 사촌인데 증상이 더 나쁘다.
   제스처는 한 번 튀고 끝나지만 스케줄러는 매 스텝마다 낡은 패턴을 예약하므로,
   텍스트를 고쳐도 몇 마디 뒤에 반영되거나 아예 반영되지 않는다. 그리고 **소리는
   스크린샷이 안 찍히므로 눈으로 못 잡는다.** 스케줄러는 `useStore.getState()`로만
   읽고(tool 05는 `makeTransport(() => useStore.getState().doc)`로 스토어의 게터
   자체를 넘긴다), 플레이헤드는 `ctx.currentTime`으로만 읽는다. 둘 사이에 React가
   없어야 한다.

14. **개발용 `window` 훅과 HMR — 스토어 인스턴스가 둘이 된다.** "`await import()`는
   별개 인스턴스를 준다"는 것은 이미 적혀 있었는데, **같은 함정이 DEV 스토어 훅에도
   있다.** Vite가 `store.ts`를 HMR로 다시 부르면 새 모듈이 `window.__beatStore`를
   덮어쓰고, 화면의 컴포넌트는 여전히 옛 인스턴스를 쓰고 있을 수 있다. 콘솔에서
   `setDoc`을 불렀는데 스윙과 템포가 안 듣는 것으로 나타났다 — **버그가 두 개
   있는 것처럼 보였고 둘 다 없었다.** 하드 리로드 후 값이 정확히 맞았다.
   **모듈을 고친 뒤 개발 훅으로 재려면 먼저 하드 리로드할 것.** 규칙 11("값이
   산술적으로 설명되면 버그가 아니라 측정 오류다")의 형제이고, 이쪽은 값이 *설명되지
   않을* 때에도 측정 오류일 수 있다는 쪽이다.

15. **실행취소 이력이 *측정값*을 들고 다니기.** `width`/`height`는 창의 사실이지 문서의
   결정이 아니다. 그런데 렌더러가 거기서 읽으니 다들 `doc`에 넣어 두고, `structuredClone`
   스냅샷이 그것까지 같이 얼린다. 한 창 크기에서 찍은 스냅샷을 다른 크기에서 되돌리면
   `viewBox`가 자기 상자와 안 맞고 `preserveAspectRatio`가 **악기 전체를 줄여서** 맞춘다.
   tool 05의 Reset이 정확히 그랬다 — 578px 스테이지에 기본값 1200×800이 들어가 전부
   48%로 나왔다. **`ResizeObserver`는 못 잡는다. 아무것도 리사이즈되지 않았기 때문이다.**
   그래서 `undo`·`redo`·`reset`이 나가는 길에 살아 있는 크기를 다시 얹는다(`sized`).
   **`doc`에 있는 값 중 무엇이 문서고 무엇이 측정인지 갈라 볼 것** — 그리고 스냅샷을
   되돌린 뒤 화면이 "왜 작아졌지"로 보이면 먼저 이것을 의심할 것.
   **tool 01·02·04에 같은 구멍이 있다.** 구조가 같고 Reset이 같은 기본값을 되돌린다.
   이번 범위가 아니라 안 고쳤고, 고치는 것은 도구마다 세 줄이다.

16. **한 부재의 치수를 다른 부재에 쓰기 — 그리고 선으로 생각하고 도형으로 그리기.**
   tool 02의 게이트는 붙잡는 부재 안으로 **묻혀서** 시작한다(평면 도면에 이음매가 안
   보이게). 그 깊이가 **벽 두께 상수**였다. 벽에 붙는 게이트는 맞다 — 앵커선이 벽의
   안쪽 면이고 뒤에 벽 전체가 있다. 그런데 셀 바에 붙는 게이트의 앵커선은 **바의
   중심선**이라 뒤에 반 두께밖에 없고, 나머지가 반대쪽으로 뚫고 나가 셀 안에 아무것도
   붙잡지 않는 돌기로 선다. 실측: 기본 시트에서 **10개 중 8개, 최대 6px**.
   → 깊이를 앵커가 들고 다니게 했다(`Anchor.back`). **치수는 그것이 속한 부재의
   성질이지 전역 상수가 아니다.**
   **그리고 그 다음 절반이 더 미묘하다.** 게이트는 선이 아니라 **도형**이다 — 넓은 쪽
   끝이 `gateWidth`만큼 벌어져 있으므로, 비스듬히 나가면 그 끝의 모서리가 부재의 면을
   지나쳐 허공에 선다(실측 3~4px). 발을 옮기거나 더 깊이 묻는 것은 문제를 옮길 뿐이고,
   **뿌리를 부재의 면에 대고 자르는 것**만이 모든 각도에서 맞다(`clipHalfPlane`).
   덤: 그 함수는 기각된 가짜 3D와 함께 지웠던 것인데, 진짜 렌더러가 반대편에서 같은
   모양의 문제를 찾아냈다 — 그 자체가 그 연산이 프로토타입 비계가 아니라는 증거다.
   **검사는 불변식으로 걸었다**: 이음매에서 뿌리까지, 게이트는 재료 안에 있다
   (`runner.test.ts`, 9종 시트 · 190여 게이트). 두 실패 모드를 각각 되돌려서 검사가
   실제로 무는 것을 확인했다(4건 / 182건).

---

## 명령

```bash
npm run dev     # http://localhost:5173
npm test        # 기하·소리·카피 단위 테스트 (371개)
npm run lint
npm run build

npm run sync:vision   # tool 04의 wasm 런타임을 node_modules에서 public/으로 다시 복사
```

폰트 라이선스 원문은 `public/fonts/`에 있고, tool 04가 재배포하는 MediaPipe 런타임의
Apache 2.0 원문은 `public/vision/`에 있다. **서체 하나만 OFL이 아니다** —
UnJamo Dotum은 GPL-2이고(`GPL-2-UnJamoDotum.txt`), 그 뜻과 대안 없음은
`src/shared/media/type/fonts/NOTICE.md`에 적혀 있다. 두 라이선스 다 재배포할 때 원문을 같이
넣도록 요구하고, 빌드 산출물에 들어가는 건 `public/`뿐이다. 무엇이 왜 저장소에
들어와 있는지는 `src/tools/magic-circle-typography/models/NOTICE.md`에 적어 뒀다.

Node는 Homebrew로 설치돼 있다. PATH에 없으면 `export PATH="/opt/homebrew/bin:$PATH"`.

---

## 배포

**작업 폴더가 곧 저장소다** — `/Users/kim/Documents/GitHub/TinkerType/Tinkertype`,
`github.com/designedbyyeon/Tinkertype`(공개). 사이트는
**https://designedbyyeon.github.io/Tinkertype/** 에서 돈다.
`260820_main`은 손대지 않은 아카이브이고, 거기서 작업하면 두 벌이 갈라진다.

**배포는 손으로 하는 일이 아니다.** main에 푸시되면
`.github/workflows/deploy.yml`이 `npm ci` → `npm test` → `npm run build` →
Pages 업로드를 한다. **테스트가 빌드 앞에 있는 것이 요점이다** — 깨진 기하나 소리
표가 사이트에 도착하지 않는다.

`vite.config.ts`의 `base: './'`가 이 배포를 이미 가능하게 해 뒀다. 자산 URL이
상대경로라 도메인 루트에서든 `/Tinkertype/` 서브경로에서든 같은 빌드가 돈다 —
Pages 때문에 고친 줄이 하나도 없다.

**푸시는 사용자가 한다.** 터미널 git에 GitHub 자격증명이 없다(`gh` 없음 · SSH 키
없음). 커밋까지 만들어 두고 **GitHub Desktop에서 푸시**하게 넘기는 것이 정상
절차다. 결과는 인증 없이 확인할 수 있다 — `git log --oneline origin/main..main`,
그리고 공개 API의 `actions/runs`. Actions 로그 본문은 로그인이 필요하지만
**annotations는 로그인 없이 브라우저로 읽힌다**.

**레퍼런스 폴더는 작업 폴더에 있고 저장소에는 없다.** `0819_ref` · `0824_*` ·
`0825_*` · `0827_*` 전부 `.gitignore`에 걸려 있다 — 남이 만든 이미지를 공개
저장소로 재배포할 것이 아니다. 예외는 `0819_ref/README.md` 하나, 우리가 쓴
메모라서 따라간다. (`0827_DJ is my Type ` 은 **폴더 이름 끝에 공백이 있다.**
git이 패턴 끝의 공백을 버리므로 `.gitignore`에서 역슬래시로 지켜 놨다.)
