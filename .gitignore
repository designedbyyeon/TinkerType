# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 레퍼런스 자료 — 작업 폴더에는 두고, 공개 저장소에는 올리지 않는다.
# 대부분 남이 만든 이미지다(제품 사진 · 구매한 3D 팩의 렌더 · 수집한 참고 이미지).
# 우리가 쓴 메모는 예외로 따라간다 — 그 메모가 "이 이미지가 무엇을 가르쳤나"이고,
# HANDOVER.md가 그것을 가리킨다.
0819_ref/*
!0819_ref/*.md
0824_building ref/
0824_building shape ref/
0825_model kits ref/
0825_render ref/
0827_DJ is my Type\ /
