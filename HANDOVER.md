# 인수 메모 — 2026-08-20 (최신 갱신 2026-08-29: 퍼블리싱 — 사이트가 인터넷에 올라갔다)

**여기가 유일한 작업 폴더다.** `/Users/kim/Documents/GitHub/TinkerType/Tinkertype`.
git 저장소이고, main에 푸시하면 자동으로 배포된다.

사이트는 **https://designedbyyeon.github.io/Tinkertype/** 에서 돌고 있다.

이 폴더의 내력: `260815_Test` → `260820_main` → 여기(2026-08-29). 앞의 둘은 git이
아니었고 마지막 이동이 저장소로 들어온 것이다. **`260820_main`은 지우지 않고 손대지
않은 아카이브로 남아 있다** — 거기서 작업하면 안 된다. 두 벌이 갈라지는 것이 이
저장소가 프로토타입을 지우는 이유와 같은 종류의 사고다.

**먼저 읽을 것은 이 파일이 아니다.**

| | |
|---|---|
| `CLAUDE.md` | 정본. 목적 · UI 규칙 9개(9번이 언어 규칙) · 작동 원리 · 반복된 버그 유형 16가지 · 배포 |
| `README.md` | 기능별 설계 근거. 도구 01 · 02(평면 · 입체 · 한글) · 05 · 공통 구조. **2026-08-28에 한 번 유실됐고 복원본이다** — 아래 12번 |
| `0819_ref/README.md` | 도구 02의 레퍼런스 이미지 ↔ 무엇을 가르쳤나 |
| `0827_DJ is my Type /` | 도구 05의 레퍼런스 — `01.png`이 기계의 구조(원판 두 개 사이의 ㅜ), 나머지 셋이 시각 언어 |
| 이 파일 | **지금 어디까지 왔고 다음이 무엇인지**만 |

**레퍼런스 폴더는 폴더에는 있고 저장소에는 없다.** 여섯 개 전부(`0819_ref` ·
`0824_*` · `0825_*` · `0827_*`) `.gitignore`에 걸려 있다 — 대부분 남이 만든
이미지라(제품 사진 · 구매한 3D 팩의 렌더 · 수집한 참고 이미지) 공개 저장소로
재배포할 것이 아니다. **예외는 `0819_ref/README.md` 하나**, 우리가 쓴 메모라서
따라간다. 그러니 다른 기계에서 새로 clone하면 이미지가 없다 — 원본은
`260820_main`에도 그대로 있다.

```bash
npm run dev     # http://localhost:5173
npm test        # 371개
npm run lint
npm run build
```

`node_modules`가 이미 설치돼 있으므로 `npm install` 없이 바로 돈다.
Node는 Homebrew — PATH에 없으면 `export PATH="/opt/homebrew/bin:$PATH"`.

### 푸시는 사용자가 한다

**터미널 git에는 GitHub 자격증명이 없다**(`gh` 없음 · SSH 키 없음 · keychain의
자격증명은 거부된다). 커밋까지는 여기서 만들고, **푸시는 사용자가 GitHub Desktop에서**
한다. 그것이 이 저장소의 정상 절차이지 고장이 아니다.

푸시됐는지와 배포 결과는 인증 없이 확인할 수 있다:

```bash
git log --oneline origin/main..main        # 비어 있으면 다 올라간 것
curl -s "https://api.github.com/repos/designedbyyeon/Tinkertype/actions/runs?per_page=3"
```

Actions **로그 본문**은 로그인이 필요하지만 **annotations는 브라우저로 로그인 없이
읽을 수 있다** — 실패 원인은 거기서 찾는다(실제로 그렇게 찾았다, 아래 20번).

---

## 지금 상태

도구 다섯 다 동작하고, 셸(해시 라우터 · 도구 등록부 · Home)도 완성돼 있다.

- **01 `diagram-typography`** — 완성. 선을 그으면 도형 체인에 글자가 앉는다.
- **02 `plastic-type`** — 동작. 글자를 프라모델 런너로 만든다. **라틴과 한글 둘 다**
  — 저장소에서 유일하게 그렇다(라틴 3종 + 한글 2종: 네모꼴 Gothic A1 · 세벌식
  탈네모꼴 UnJamo Dotum). **형태가 둘이다** — 평면(SVG,
  편집 가능) / 입체(three.js 라이브 렌더 · 자유 궤도 · OBJ). `Page`의 `Form`이
  고르고, **기본값은 입체다**(아래 4번, 한글은 4c).
- **03 `billboard-typography`** — **등록됨.** 글줄이 상가 건물의 간판이 된다.
  **한글 전용**(조선일보 견고딕). WebGL 라이브 렌더이고, 산출물이 SVG가 아닌 첫
  도구다 — **OBJ·STL 입체 모델**로 나간다(아래 5번).
- **04 `magic-circle-typography`** — **등록됨.** 카메라에 주먹을 뻗고 손을 펼치면
  글줄마다 하나의 고리가 되어 손바닥 위에 써진다. **라틴 전제**(레퍼런스가 라틴
  활자라서 샘플이 어느 언어에서든 라틴이다. 목록의 한글 서체도 그려지고 실측으로
  확인했다 — 패널이 그 사실을 적는다). 사이트의
  첫 입력장치 도구이고, 산출물은 다시 SVG다(윤곽선 + 박아 넣은 프레임).
  **이름은 비유다** — 기본값은 활자뿐이고 선이 한 가닥도 없다(아래 9번의 2차 패스).
  설계 근거는 `README.md`의 04절.
- **05 `type-the-beat`** — **등록됨.** 원판 세 개로 음절을 맞추고 격자에 탭해
  넣으면 한 마디의 드럼이 된다. **텍스트 필드가 없다** — 세로모임꼴만. **한글 전용**(라틴에는 초성·중성·종성이 없으니
  성립조차 안 한다). 사이트의 **첫 소리 도구**이고 **첫 비-그래픽 산출물**이다 —
  **WAV 44.1k 스테레오 루프**. 샘플 없이 전부 Web Audio 합성이므로 자산이 0바이트다.
  설계 근거는 `README.md`의 05절, 남은 것은 아래 10번.

**사이트 전체가 영문 / 한국어 두 언어로 돈다** (2026-08-29). 오른쪽 위 `EN / KO`
스위치 하나, `localStorage`가 기억한다. 병기가 아니라 전환인 이유와 번역되지 않는
절반(단위·`ON`/`OFF`·워드마크·도구 이름)에 대해서는 `CLAUDE.md` 2절 규칙 9.

검증: 371개 단위 테스트 · lint · tsc · 프로덕션 빌드 · 브라우저 실측 모두 통과.
번들 **인덱스 548KB / gzip 171KB** — 두 언어를 얹고 늘어난 것이 **약 5KB**다
(인덱스에 실리는 것은 셸의 말과 도구 01·02의 사전뿐이고, 지연 로드되는 세 도구의
사전은 자기 청크로 갈라진다). **tool 05는 인덱스에 한 바이트도 안 얹었다.**
확인 방법은 `dist/assets/index-*.js`에서 `voiceFor`·`playVoice`·`patternOf`·`deckOf`·
`ChosunBg`·`encodeWav`·`AudioContext`를 grep해 전부 0회인지 보는 것이다(실측 0/0/0/0/0/0/0).
three는 별도
**212KB core + 385KB 포스트프로세싱 청크**, MediaPipe JS는 별도
**154KB(gzip 45KB) 청크**로 갈라져 각각 그 도구를 쓸 때만 받는다. 한글 폰트,
손 모델(7.8MB), wasm 런타임(11.8MB)도 별도다. **인덱스 청크에 three는 한 바이트도
없다** — 확인 방법은 `dist/assets/index-*.js`에서 `three.core-*`가
`__vite__mapDeps`(동적 import의 프리로드 표) 안에만 나오는지 보는 것이다.

**tool 05에서 브라우저 자동화로 못 본 것: 소리 그 자체.** 헤드리스 브라우저에서
스피커를 들을 수는 없으므로 **전부 렌더 버퍼를 숫자로 읽어서** 확인했다 — 피크·RMS·
밴드 에너지·자기상관 기음·엔벨로프 온셋. 그 방법이 눈으로는 절대 못 볼 버그 둘을
잡았다(아래 10번). 하지만 **"좋게 들리는가"는 사람이 판정해야 한다** — 상수 표는
음성학에서 왔고 순서 관계는 검사로 고정돼 있으니, 톤이 마음에 안 들면 `audio/voice.ts`의
표 숫자만 고치면 되고 검사는 순서만 보므로 안 깨진다.

**단 하나 브라우저 자동화로 못 본 것: 실제 카메라 앞의 손.** 헤드리스 브라우저에
카메라가 없어서 `getUserMedia`가 거절로 떨어진다(그 경로와 메시지는 확인했다).
제스처 수학은 합성 손으로 단위 테스트했고, wasm + 모델이 실제로 로드되고 답하는
것은 `window.__magicHand()`로 실측했다 — **손을 흔들어 보는 것은 사람이 해야 한다.**

### 도구 02의 최우선 레퍼런스

`0819_ref/01-kiosque-bridged-column.jpg` — Le Kiosque 포스터. 사용자가
**"최우선 레퍼런스"** 로 지정했다. 세로 1열 · 브리지로 이은 격자 · 글자보다 얇은
벽 · 사출탭과 명판 없음 · 2색. 현재 기본값이 여기에 맞춰져 있다.

**코드 주석은 레퍼런스를 번호가 아니라 이름으로 부른다** — "the bridged column",
"the spare-parts frame" 같은 식이다. 번호는 폴더를 한 번 정리하면 뜻을 잃고,
tool 01의 레퍼런스 번호와도 겹쳤다. 매핑은 `0819_ref/README.md`에 있다.

게이트 프로파일은 별도로 받은 도면을 따랐다 — 긴 테이퍼, 한 단 떨어진 평행 목.

### 절대 깨뜨리면 안 되는 불변식 둘

1. **모든 조각에 게이트가 최소 하나** (`runner.test.ts`, 240조합).
   깨지면 부품이 바닥에 떨어진다.
2. **카운터 있는 글자는 붙어 있는 살 하나** (`glyphs.test.ts`).
   **폰트를 추가하기 전 통과해야 하는 심사다.** Gabarito · Outfit · Sora ·
   Lexend · Montserrat · Unbounded · Hanken Grotesk가 모두 여기서 떨어졌다.

---

## 2026-08-20 정리 패스에서 한 것

기능 추가가 아니라 **두 도구를 같은 규칙 아래 놓는** 작업이었다.

**버그 하나를 잡았다.** `app.css`가 `var(--sunk)`를 썼는데 정의된 이름은
`--paper-sunk`였다. CSS는 없는 변수를 만나면 선언 전체를 버리므로 **메인 페이지
미리보기 카드의 배경이 며칠간 투명**했다. tool 01의 미리보기가 같은 값을 스스로
하드코딩하고 있었던 것이 그 흔적이다. `CLAUDE.md`가 "반복된 버그 유형 3번"으로
이미 적어 둔 유형이고, 이제 그 항목에 잡은 방법까지 적혀 있다 — **`var()` 이름과
`tokens.css` 선언을 diff한다.** 정리할 때마다 한 번 돌릴 것.

그 밖에:

- **UI 문자열 전부 영문** (규칙 9). 레이블은 원래 영문이었고 오류·알림만
  한국어였다. 코드 주석과 문서는 한국어 그대로.
- **tool 02 패널 순서** — Paint를 4번으로 올리고 `Page` 그룹을 새로 만들어
  `Ground`·`Zoom`을 넣었다. 이제 두 도구가 같은 순서를 따른다(규칙 6의 표).
- **tool 01에 mark 8행** — 규칙 3은 tool 02가 만든 것인데 tool 01에는 하나도
  없었다. `Corner` `Start` `Amount` `Cycles` `Per gap` `Dot size` `Smooth`
  `Follow path`. 안 붙인 행과 이유는 `marks.tsx` 머리에 적혀 있다.
- **작품 팔레트를 `shared/styles/canvas.ts`로** — 지면 색이 세 파일에 따로
  적혀 있었고 그중 하나가 화면 토큰과 한 단위 어긋났다. `CANVAS_INK`가 `--ink`와
  다른 것은 **의도**이니 통일하지 말 것(화면 잉크는 따뜻하고, 인쇄 잉크는 중성).
- **`isTyping`을 `shared/ui/typing.ts`로** — tool 02가 다시 쓰면서
  `isContentEditable`을 빠뜨렸던 것이 같이 고쳐졌다.
- **삭제** — 템플릿 잔여 파일(`public/icons.svg`는 이 프로젝트와 무관한 소셜
  아이콘 스프라이트였다), 죽은 export 다섯(`pathTotalLength` ·
  `IDENTITY_TRANSFORM` · `ImportedImage` · `setDefaults` · `loadDoc`),
  미사용 토큰 `--desk`, `.DS_Store`.
- **탭 제목이 라우트를 따른다** — 전에는 어느 페이지에서나 "Diagram Typography"
  였다. `index.html`의 `lang`도 `en`으로. `package.json` 이름도 `design-tools`로
  (사이트가 도구 하나의 이름을 쓰고 있었다).
- **`exportSvg`의 `filename`을 필수로** — 공유 모듈이 tool 01의 파일명을
  기본값으로 갖고 있었다.
- **파비콘** — 템플릿의 보라색 마크(참조도 안 되고 팔레트에도 없는 색)를 지우고,
  다섯 개를 그려 사용자가 골랐다. 채택은 **치수 보조선**: 액센트 헤어라인이 실심
  틱으로 들어가는 모양으로, 메인 페이지 레일이 "지금 이것"을 가리킬 때 쓰는 바로
  그 표시다. 어느 도구의 출력물 형태도 아니고(그러면 다음 도구가 첫 도구의 얼굴을
  쓴다), 덩어리 둘뿐이라 16px에서 안 뭉개진다. `public/favicon.svg` +
  `index.html`의 상대 경로 링크(`./favicon.svg` — 하위 경로 배포에서도 돈다).
- **tool 01의 여는 값** — 원 87px · 이음 26px · 글자수에 맞추기. 빈 캔버스가
  메인 페이지 카드와 같은 짜임으로 시작한다. 프리셋 셋에는 `countMode: 'spacing'`
  을 못박아 뒀다(기본값이 바뀌어도 끌려가지 않게).

---

## 2026-08-29 세 번째 패스 — 사이트 전체를 두 언어로

**물어본 것은 두 안 중 무엇이 나은가였다: (1) 한글·영문 병기, (2) 언어 전환.**
답은 (2)이고, 이유는 취향이 아니라 이 저장소가 이미 적어 둔 규칙이다.

병기는 **규칙 9가 지키려던 것을 정확히 깨뜨린다.** 이 패널의 톤은 10px 대문자
자간 0.09em 레이블이 만드는 것이고, `지면 GROUND`는 그 레이블을 두 배로 늘리면서
두 문자 체계를 한 줄에 넣는다. 296px 레일에 그럴 자리도 없다. 그리고 병기는
**어느 쪽도 완성된 화면이 아니다** — 한국어 사용자는 매 줄 영어를 지나쳐 읽고,
영어 사용자는 매 줄 한글을 지나쳐 읽는다.

전환은 반대로 **두 개의 완성된 화면**을 준다. 덤이 하나 있는데 그게 사실은 본편이다:
언어가 정해지면 **샘플 문구와 기본 서체도 그 언어의 것일 수 있다.** 도구 02를
한국어로 열면 `플라스틱 활자`가 Gothic A1으로, **자모 분해된 채로** 나온다 — 그것이
이 도구가 한글에 대해 하는 주장 자체이고, 첫 화면이 그것을 말한다.

### 무엇을 했나

- `shared/i18n/` — `lang.ts`(언어 스토어 · `localStorage` · `<html data-lang>`),
  `site.ts`(셸과 공용 컨트롤과 임포터 오류), `LangSwitch.tsx`
- `tools/<id>/copy.ts` 다섯 개. **한국어가 영어를 타입으로 받는다**
  (`type Copy = typeof en`), 그래서 빠진 줄은 빌드 오류다
- `app/tools.ts`의 `Tool`이 처음으로 바뀌었다 — `blurb`·`spec` → `copy`
- `[data-lang='ko']` 블록 넷(공용 · 셸 · 도구 01 · 04 · 05)
- `shared/i18n/copy.test.ts` — 타입이 못 보는 것 하나만 검사한다

### 이번에 새로 알게 된 것

- **번역되지 않는 절반이 계기판을 지킨다.** 단위·`ON`/`OFF`·파일 포맷·`SPACE`·
  워드마크·도구 이름·서체 이름·숫자. 그건 영어가 아니라 측정의 언어다. 한글
  레이블이 대문자와 자간을 잃는 대신 숫자 쪽이 두 언어에서 **완전히 동일하다**
- **언어를 `doc`에 넣으면 버그 유형 15가 된다** — 스냅샷마다 얼어붙어 Ctrl-Z가
  패널을 다른 언어로 되돌린다. 스토어 하나에 따로 산다
- **샘플 교체는 `isSample`이 먼저 묻는다.** 손댄 문서는 안 건드린다
- **못 가는 곳은 못 간다고 적는다.** tool 02의 인덱스 카드는 두 언어에서 라틴이다 —
  한글로 짜면 인덱스가 3.9MB 서체를 받아야 하고, `faces.ts`가 통째로 막으려고
  쓰여 있는 사고가 그것이다. tool 04는 라틴을 전제로 만든 도구이므로 샘플이
  라틴이고, **패널이 그것을 한국어로 말한다**
- **tool 04에서 한글이 실제로 그려진다는 것을 실측으로 확인했다** — 목록의 Gothic A1을
  고르고 두 줄을 한글로 넣으면 고리에 앉는다. 그래서 패널의 문구가 "라틴 전용"이
  아니라 "라틴을 전제로 만들었고, 한글 서체를 고르면 된다"로 쓰여 있다.
  **등록부의 `Input`은 아직 `Latin`이다** — 그것을 `라틴 · 한글`로 바꾸는 것은
  번역이 아니라 도구의 주장을 넓히는 일이라 사용자 결정으로 남긴다

### 브라우저 자동화에서 한 번 속을 뻔한 것

프리셋을 누르면 도형이 0.08px로 붕괴하는 것처럼 보였다. **버그가 아니라 측정
환경이었다** — 브라우저 패널이 숨겨져 `innerWidth`가 0이 되고, `ResizeObserver`가
`doc.width/height`에 1을 써 넣은 상태에서 프리셋이 1×1 뷰에 맞춰진 것이다.
패널을 다시 띄우니 60.07px로 정확했다. **CLAUDE.md 버그 유형 11의 형제이고,
"값이 산술적으로 설명되면 버그가 아니라 측정 오류"가 여기서도 맞았다.**

---

## 2026-08-29 두 번째 패스 — 게이트 돌기, 서체 구분, 새 샘플

사용자 피드백 셋.

1. **한글/영문 서체를 구분해 달라** — `Face` 한 줄이 다섯 개를 접어 넣고 있었다.
   이제 `Latin` / `Hangul` 두 줄이고, 값은 하나다(선택된 쪽만 하이라이트를 든다).
   서체가 바꾸는 것이 취향만이 아니라 **자모로 자를 수 있느냐**이므로, 두 벌을
   한 줄에 섞는 것은 그 사실을 감추는 배치였다. `is-faces` 행만 왼쪽 정렬이다.
2. **게이트가 런너 밖으로 삐져나온다** — 진짜 버그였고 원인이 둘이었다. 아래.
3. **샘플 텍스트를 `PLASTIC TYPE`으로** — 열두 글자라 한 줄 세로 배치(`perRow: 1`)로는
   실이 되어서 **4×3 시트**(`perRow: 4`)로 같이 바꿨다. 첫 화면이 도구가 만드는
   것 자체이고, 자기 이름을 부품으로 읽는다.

### 게이트 돌기 — 원인 둘, 둘 다 실측으로 잡았다

**원인 1: 묻히는 깊이가 전역 상수였다.** 게이트는 붙잡는 부재 안으로 묻혀서 시작하는데
(평면 도면의 이음매를 없애려고) 그 깊이가 `style.bar`(벽 두께)로 고정이었다. 벽에서는
맞다 — 앵커선이 벽 안쪽 면이라 뒤에 벽 전체가 있다. **셀 바와 가지의 앵커선은 바의
중심선**이라 뒤에 반 두께밖에 없고, 나머지가 반대쪽으로 뚫고 나온다.
실측: 기본 시트에서 **게이트 10개 중 8개, 최대 6px**. → `Anchor.back`(그 부재 뒤에
실제로 있는 재료)을 앵커가 들고 다닌다.

**원인 2: 게이트는 선이 아니라 도형이다.** 넓은 끝이 `gateWidth`만큼 벌어져 있으므로
비스듬히 나가면 **그 끝의 모서리**가 부재의 면을 지나쳐 허공에 선다(실측 3~4px, 가지에
붙은 게이트에서). 발을 반 게이트만큼 부재 끝에서 떼는 것(`closest`의 `margin`)과
바의 끝을 반 두께 늘리는 것(`makeBar`의 캡)으로도 안 없어졌다 — 남는 것은 **뿌리를
부재의 면에 대고 자르는 것**이고, 그것만이 모든 각도에서 맞다.
→ `clipHalfPlane`을 되살렸다. **기각된 가짜 3D와 함께 지웠던 함수인데, 진짜 렌더러가
반대편에서 같은 모양의 문제를 찾아냈다.**

**같이 나온 것:** 마진을 넣자 "이미 런너에 닿아 있는 조각"이 닿지 않는 것으로 읽혀
붙어 있는 자리에 게이트가 생겼다. **닿았는지는 마진 없이 물어야 한다** — 마진은 게이트를
*걸 수 있는 곳*을 말하지 조각이 어디 있는지를 말하지 않는다. `Connection`이 둘을 같이
들고 다닌다(`distance` = 게이트가 걸릴 거리, `contact` = 실제 거리). 검색을 두 번 하지
않으려고 한 번에 계산한다 — 검색이 이 파일에서 비싼 쪽이다.

**검사:** 불변식 하나를 새로 걸었다 — *이음매에서 뿌리까지, 게이트는 재료 안에 있다*
(`runner.test.ts`, 서체 5종 · 시트 9종 · 게이트 190여 개). 중심선을 걸어가며 재고,
넓은 끝의 두 모서리도 본다. **두 실패 모드를 각각 되돌려서 검사가 무는 것을 확인했다**
(자르기를 빼면 4건, 앵커별 깊이를 빼면 182건). `CLAUDE.md` **버그 유형 16번**.

## 2026-08-29 패스 — 도구 02에 한글, 그리고 기본값이 입체로

요청 셋을 그대로 했다: 기본값 3D · 한글(구조를 고려해 기능과 작동 원리까지) ·
한글 서체 두 종(네모꼴 산세리프 / 세벌식 탈네모꼴). 전부 **4c번**에 있다.

**열려 있는 결정 하나**: 탈네모꼴로 넣은 UnJamo Dotum이 **GPL-2**다(폰트 예외 없음).
재배포 가능한 세벌식 탈네모꼴이 그것뿐이었고, 산출물이 아웃라인을 싣는 도구에서는
실질적인 위험이라 사용자가 알고 고를 자리로 남겼다. 4c번과
`src/shared/media/type/fonts/NOTICE.md`.

## 2026-08-27 패스 — 도구 02에 입체 형태를 넣었다

사용자 요청 그대로다: 메인 사이트에 평면만 올라와 있던 것을 **한 도구로 합치고**,
방식은 전에 만든 SVG 가짜 3D가 아니라 **풀 3D**로, 룩은 대체로 유지하면서
**마우스로 자유롭게 앵글을 바꾸고 OBJ로 내보낼 수 있게.**

- 전부 **4번 항목**에 있다(무엇이 생겼고, 무엇을 지웠고, 무엇을 재봤는지).
- 문서 세 개 다 갱신했다 — `CLAUDE.md`(도구별 산출물 계약 · 규칙 4·6 · 성능 ·
  **버그 유형 12번 신설**), `README.md`(02절에 「두 번째 형태 — 진짜 3D」),
  이 파일(4번).
- **손대지 않은 것**: 평면 형태의 아트워크·SVG export·인덱스 카드 그림은 한 픽셀도
  안 바뀐다(`Preview`는 파이프라인만 공유 함수로 바꿨고 결과는 동일하다).
  `app/tools.ts`의 `Tool` 인터페이스도 여전히 한 줄도 안 고쳤다 — **도구 하나가
  형태 둘과 산출물 둘을 갖게 되어도** 그 모양이 맞았다.

---

## 열려 있는 것

### 1. 서체 최종 선택 — 사용자 결정 대기

셋 다 유지하기로 했다(정리 패스에서 확인). 기본값은 Big Shoulders.

| | 축 | 실측 H | O 편차 @900 |
|---|---|---|---|
| **Big Shoulders** (기본) | wght 100–900 | 0.800em | — |
| **Kumbh Sans** | wght 100–900 | 0.732em | 0.024 |
| **Poppins Black** | 없음 (정적) | 0.708em | **0.011** |

하나로 정하면 나머지 둘을 네 군데서 빼면 된다 — `faces.ts` · `fonts/` ·
`public/fonts/OFL-*.txt` · 테스트의 face 목록. 번들이 ~270KB 줄어든다.

**폭 축을 가진 서체가 지금은 없다.** 배선은 남겨 뒀고(굵기 축이 쓰는 것과 같은
"있을 수도 없을 수도 있는 축" 장치), 없는 축의 행은 알아서 사라진다.

### 2. 도구 01의 프리셋 좌표 — 사용자 자료 대기

가장 오래 막혀 있는 항목. 사용자가 직접 만든 레퍼런스 이미지를 주면 그대로
좌표로 옮기기로 했다. 현재 프리셋은 임시다.

### 3. 한글 복귀 — **끝났다** (2026-08-29)

**살려 두라고 적어 둔 자산이 전부 그대로 쓰였다.** `hangul.ts`의 분해와 분할,
11,172음절 왕복 검사, `Panel.tsx`의 자모 단위 주석 — 지운 것이 하나도 없었으므로
서체 둘을 붙이는 것으로 한글이 돌아왔다. 이 항목이 그 결정의 값이다.

- **메인 페이지 spec의 `Hangul · Latin` / `Jamo split`** — 오랫동안 "기능과 어긋나
  보이지만 의도된 것"이었고, **이제 그냥 사실이다.**
- **다중 스크립트 런 분할만 안 돌아왔다.** 필요가 없어졌기 때문이다 — 한글 서체 둘 다
  라틴을 완전히 갖고 있어서 한 서체가 줄 전체를 그린다(4c번).
- `layout.ts`에 남아 있던 그 자리 주석은 그래서 갱신했다.

무엇을 어떻게 넣었는지는 **4c번**에 있다.

### 4. 입체 형태 — **끝났다. 한 도구 안에 두 형태로 들어갔다**

`Page`의 `Form` 한 행이 평면/입체를 고른다. 위쪽의 모든 컨트롤은 두 형태에서 같은
뜻이고, 플랜을 만드는 함수도 하나다(`render/plan.ts`의 `sheetOf` — 평면 스테이지 ·
입체 스테이지 · 인덱스 카드 · 모델 파일이 모두 이것을 부른다). 설계 근거 전부는
`README.md`의 「두 번째 형태 — 진짜 3D」에 있고, 여기에는 **결정과 실측만** 적는다.

**가짜 3D는 기각하고 지웠다.** 사용자 결정이다 — "svg기반 fake 3D가 아닌 풀 3D로".
지운 것: `proto-solid.html` · `tools/plastic-type/proto/` · `geometry/axon.ts` ·
`geometry/axon.test.ts` · `shared/geometry/polygon.ts`의 `clipHalfPlane`.
**살린 것**: `roundedRectRing`(입체 프레임 링이 쓴다)과 그것을 `roundedRectPath`와
맞대는 검사 셋 — `runner.test.ts`로 옮겼다. 그 검사가 거울상 감김 버그를 잡은
오라클이므로 axon 테스트와 함께 지우면 안 됐다.
**이 저장소는 git이 아니다.** 지운 파일은 이번 세션 스크래치패드의
`removed-fake3d/`에 사본이 있는데, 그건 세션이 끝나면 사라진다고 봐야 한다.

새로 생긴 파일:

| | |
|---|---|
| `geometry/solid.ts` | 평면 플랜 → **덩어리 목록**. three를 모른다. 단위 테스트 있음 |
| `render/shapes.ts` | 덩어리 → `THREE.Shape`. **three를 만나는 유일한 기하 파일** |
| `render/build.ts` | 덩어리 → 압출된 `Group`. **뷰와 export가 같이 부른다** |
| `render/SolidView.tsx` | 렌더러 · 궤도 카메라 · 환경광 · AO |
| `render/SolidStage.tsx` · `render/FlatStage.tsx` | 두 형태의 스테이지 |
| `render/Stage.tsx` | 둘 중 하나를 고르고, **입체 쪽만 지연 로드한다** |
| `render/exportModel.ts` | OBJ. 클릭 시점에 import된다 |
| `render/plan.ts` | 파이프라인 한 곳 |

**세 깊이와 하나의 뒷면.** 부품 34 · 프레임 22 · 게이트 11px(기본값). 게이트는
런너로 클램프된다. 진짜 3D여서 가짜 3D의 결함 여섯 개가 전부 없어졌다 — 특히
게이트의 파묻힌 끝을 잘라낼 필요가 없다(더 두꺼운 재료 안에 있으므로 그냥 안 보인다).

**카메라는 문서에 없다.** 렌더러·카메라·컨트롤은 마운트 때 한 번 만들고, 문서가
바뀌면 그 안의 오브젝트만 다시 만든다. 다시 맞출 때 각도는 그대로 두고 대상점·
클리핑·줌만 잡는다. 사람이 고른 줌은 맞춤값에 대한 **배율**로 보관된다.
**알고 있는 한계 둘**: (1) 다시 맞출 때 대상점이 시트 중심으로 돌아가므로, 한
프레임을 보려고 이동(pan)해 둔 상태에서 글자를 치면 가운데로 돌아온다. 시트 모양이
같이 바뀌므로 지금은 이쪽이 맞다고 판단했다. (2) 평면↔입체를 왕복하면 스테이지가
언마운트되므로 시점이 여는 값으로 돌아간다.

**실측 (프로덕션 빌드, M2 Max).**

| | KIOSK (프레임 5 · 덩어리 24) | 45자 문장 (프레임 24 · 덩어리 184) |
|---|---|---|
| `sheetOf` | 0.7ms | 2.8ms |
| `buildSolid` | 2.2ms | 12ms |
| **궤도 한 프레임** | **4.8ms** | **5.0ms** |
| 깊이 드래그 p90 / 최악 | 7.1 / 15.3ms | 29 / 65ms |
| OBJ 쓰기 | 21ms · 1.8MB · 오브젝트 29개 | — |

**두 번 재보고 두 번 뺐다.**

- **그림자맵**: 215만 픽셀 중 **680개**만 바꾸면서 문서 변경마다 30ms를 쓰고 있었다.
  런너는 정면에서 빛을 받는 평판이고 부품 뒤는 프레임의 구멍이라 그림자가 떨어질
  데가 거의 없다. 통째로 뺐다(`castShadow`까지).
- **색깔별 지오메트리 병합**: 드로우콜 184→2인데, 병합에 문서 변경마다 13ms가 들고
  프레임은 4.7ms 그대로였다. 움직이는 것이 카메라뿐이니 드로우콜이 병목이 아니다.
  되돌렸고, 덩어리를 따로 두는 쪽이 파일에도 맞다(부품마다 오브젝트).

**AO 함정 하나 — 이것이 이번 작업에서 가장 값진 발견이다.** tool 03에서 AO
파라미터를 그대로 가져왔는데 `radius`·`thickness`는 **월드 공간 거리**다. tool 03은
캡 높이 단위(간판 1), 여기는 픽셀 단위(가로 1000). 그래서 "1.5픽셀 안의 차폐물을
찾아라"가 되고, 패스는 정직하게 돌면서 **AO 버퍼를 215만 픽셀 전부 같은 회색으로**
내놓았다 — 비용 전액, 결과 0. 화면으로는 "AO가 은근한 것"으로 보인다. `readPixels`로
버퍼를 읽어서 잡았고 시트 깊이에 맞춰 주니 **27,000픽셀**이 어두워졌다.
`CLAUDE.md`의 **버그 유형 12번**이 이것이다.

**OBJ는 부품마다 이름이 붙는다** — `A-frame` · `A-tab` · `A-gate-1` · `A-part-1` ·
`bridge-1`. 롱사이드 200mm(기본값에서 두께 5mm). 색은 안 실린다(OBJ의 한계이고
패널이 그렇게 말한다).

**남은 것 / 안 한 것**

1. **STL은 안 넣었다.** tool 03은 OBJ·STL 둘을 내보내는데 여기는 OBJ만이다.
   요청이 OBJ였고, 버튼 하나가 늘어난다는 뜻 이상은 아니다 — `STLExporter`를
   `exportModel.ts`에 한 갈래 더 넣으면 된다. 프린트로 갈 사람이 생기면 넣을 것.
2. **깊이 드래그가 긴 문장에서 무겁다**(p90 29ms). 재보면 압출 12ms + 나머지는
   React와 GPU 업로드다. 필요해지면 **깊이만 바뀔 때는 다시 압출하지 않고
   `scale.z`로 처리**하는 길이 있다(tool 01의 "배치는 transform" 원리와 같다).
   단 모따기가 같이 늘어나므로 손을 떼는 순간 값이 튄다 — 그 타협을 받아들일지
   먼저 봐야 한다.
3. **분리 애니메이션은 계산이 달라졌다** — 아래 4b는 가짜 3D 기준으로 쓴 것이다.
   진짜 3D에서는 부품을 옮기는 것이 그냥 오브젝트 `position`이므로 더 쉽다.

### 4b. 분리 애니메이션 — 가능하다, 아직 안 만들었다

**아래는 가짜 3D(사등각 SVG) 기준으로 쓴 것이고 그 렌더러는 없어졌다.** 평면
형태에 애니메이션을 붙이는 이야기로는 그대로 유효하고(게이트를 목에서 끊는 것,
파단면을 미리 그려 두는 것은 형태와 무관하다), 입체 형태에서는 더 쉽다 — 진짜 3D에서
부품을 띄우는 것은 오브젝트 `position` 하나다.

**결론: 된다. 그리고 싼 길이 실제로 있다.** 판단 근거만 적는다(구현 안 함).

**핵심 사실 하나.** 오소그래픽 압출에서 측면은 윤곽을 **상수 벡터**만큼 옮긴 것이다.
그러므로 부품을 **평행이동**하면 그 측면도 정확히 같은 만큼 옮겨진다 —
즉 부품 하나의 이동은 **그룹 `transform` 한 줄**이고, 기하를 다시 계산할 필요가 없다.
애니메이션의 대부분이 여기서 공짜가 된다.

필요한 것 셋:

1. **게이트가 끊어진다.** 실제로는 목에서 부러지므로 게이트를 목 평면에서 둘로
   갈라 런너 쪽 그루터기와 부품 쪽 그루터기를 만든다. `clipHalfPlane`이 이미
   있으니 그 자리에 한 번 더 쓰면 끝이다.
2. **파단면을 처음부터 그려 둔다.** t=0에서 두 그루터기는 겹쳐 있으므로 파단면을
   항상 내보내도 보이지 않는다. 이게 중요하다 — 안 그러면 부품이 떨어질 때 새로
   드러나는 면 때문에 겹침 판정을 매 프레임 다시 돌려야 하고, 그러면 순수
   `transform`이 아니게 된다.
3. **부품 쪽 그루터기가 부품과 함께 움직인다.** `PiecePlan.gates`가 이미 게이트를
   조각에 매달아 두었으므로 매핑은 공짜다.

이 셋이면 애니메이션 전체가 그룹당 `transform` 하나다.

**단, 화면 평면 안의 이동·회전까지다.** 진짜 3D 텀블(부품이 공중에서 뒤집히는 것)은
매 프레임 솔리드를 다시 계산해야 한다. 2.8ms니까 재생은 되지만, 그때는 하나의
애니메이션 SVG가 아니라 **프레임을 구워내는** 쪽이 된다.

**내보내기**

| | 나오는 것 | 열리는 곳 |
|---|---|---|
| SVG 안의 SMIL `<animateTransform>` | 한 파일 · 진짜 벡터 · 열면 재생 | 브라우저 ✓ · **일러스트레이터는 애니메이션을 무시하고 아트워크는 그대로 살린다** ✓ · 피그마 무시 |
| SVG 안의 CSS `@keyframes` | 같음. 이징 쓰기가 더 쉽다 | 브라우저 ✓ · 편집기 무시 |
| 프레임 시퀀스(SVG N장) | 어디서나 편집 가능, AE·프리미어로 바로 | 전부 ✓ |
| Lottie | 모션 쪽 표준 | 익스포터를 새로 써야 한다 |
| 영상 · GIF | 래스터 | 전부 ✓, 대신 벡터가 아니다 |

**권장: SMIL 애니메이션 SVG를 기본으로, 프레임 시퀀스를 보조로.** 전자는 한 파일로
끝나고 편집기에서 열어도 정지 아트워크가 온전하다("결과는 항상 편집 가능한 SVG"를
깨지 않는다). 후자는 AE로 가져갈 사람을 위한 것이고, 타이밍이 순수 함수라 싸다.

**그리고 이건 두 번째 신호다.** tool 01의 `reveal.ts`가 이미 순수 타이밍 함수 ·
플레이헤드 · 이징 · 루프를 갖고 있고, 그 HANDOVER에도 "나중에 프레임을 구워내기
쉽다"고 적혀 있다. 같은 필요가 두 도구에서 나왔으므로 프로젝트 규칙대로 **타이밍
모델은 `shared/`로 꺼낼 자리**다. 두 번 만들지 말 것.

**입체 쪽에서 아직 안 한 것**

- **프레임 외곽선 자체의 우회.** 레퍼런스는 프레임 바깥선이 부품을 향해 꺾여
  들어가는데, 지금은 둥근 사각형 + 내부 바로 읽었다. **레퍼런스와의 가장 큰
  눈에 보이는 차이**다. 입체와 무관하게 남아 있다.
- **깊이 슬라이스는 역할 경계에서만 자른다.** 같은 역할 안에서 오목한 글자가
  자기 벽을 자기 벽 뒤에 두는 경우는 `far` 정렬로만 처리한다 — 정렬은 리본 단위라
  한 리본이 다른 리본을 절반만 가려야 하는 상황에서는 전부 가리거나 전부 드러난다.
  실측에서 눈에 띄지는 않았지만 남아 있는 한계다.
- **도구에 넣을지, 넣으면 컨트롤을 어떻게 낼지** — 카메라 둘 · 두께 · 단계 ·
  광원으로 다섯 행이다. 규칙 3대로면 전부 mark가 필요하고, 규칙 6대로면 Paint
  앞뒤 어디에 놓을지가 정해져야 한다.

### 4c. 한글 — 들어갔다. 서체 둘과 문자 체계를 따르는 분할

사용자 요청 셋이었다: **기본값을 3D로**, **한글을 넣되 한글의 구조를 고려해 기능과
작동 원리를 다듬을 것**, **서체는 깔끔한 네모꼴 산세리프와 세벌식 탈네모꼴 두 가지.**

설계 근거는 `README.md`의 「폰트 — 라틴 셋, 한글 둘」과 「한글이 이 도구를 가장 세게
시험한다」에 있다. 여기에는 **결정과 실측만** 적는다.

**새로 만든 기하가 없다.** 이 도구는 원래 "붙어 있는 덩어리 하나가 부품 하나"였고,
한글은 그 규칙이 훨씬 자주 참이 되는 문자다. `한`은 조각 다섯(ㅎ의 획 셋 + ㅏ + ㄴ)으로
나오고 각각 게이트를 받는다. i의 점에 게이트를 주던 그 규칙 그대로다. 바뀐 것은
**압력**이다 — 글자 하나 크기의 프레임에 부품이 다섯.

**바뀐 것 넷:**

1. **등록부에 한글 두 종.** `FaceSpec`에 `script: 'latin' | 'hangul'`이 생겼고,
   **한글 항목의 `url`은 문자열이 아니라 함수다** — 2.2MB를 `faces.ts`에 `?url`로
   적으면 그 참조가 인덱스 모듈 그래프에 들어간다(조선일보 견고딕 3.9MB 사고와 같은
   자리). `store.ts`의 로더가 파일을 받기 전에 **파일이 어디 있는지부터** 받는다.
   확인: `dist/assets/index-*.js`에 `GothicA1Black`이 없다(실측 확인함).
2. **분할 단위가 문자 체계를 따른다.** 한글이면 `Jamo`가 생기고 `Letter`가
   `Syllable`로 바뀐다. 라틴에는 글자 아래 단위가 없으므로 자모 행이 없다 — 있으면
   글자 단위와 같은 답을 내는 버튼이 둘이 된다. 서체를 라틴으로 되돌릴 때 `partUnit`이
   `jamo`면 `syllable`로 내린다(안 그러면 행에 아무것도 선택 안 된 채 기하는 다른 일을
   한다).
3. **Size 기준 글자가 서체마다 다르다.** 라틴 `H`, 한글 `한`. 배선은 이미 있었다
   (`FaceSpec.reference`).
4. **스크립트 런 분할은 만들지 않았다.** 한글이 오면 필요하다고 적혀 있었는데,
   **두 한글 서체 모두 라틴을 완전히 갖고 있다**(실측: 없는 글자 0). 한 서체가 줄
   전체를 그리면 나눌 런이 없다.

**서체 선택의 근거와 값.**

| | 파일 | 라이선스 | 크기 | 음절 |
|---|---|---|---|---|
| 네모꼴 | `GothicA1Black.ttf` | OFL | 2.2MB | 11,172 |
| 탈네모꼴 | `UnJamoDotum.ttf` | **GPL-2** | 917KB | 11,172 |

- **베리어블 한글은 못 넣었다.** 후보가 Asta Sans 하나였는데 **opentype.js가 그 축을
  못 움직인다** — `variation.set` 뒤 400과 800의 아웃라인이 같게 나왔다(라틴
  베리어블에서는 정상이다). 그래서 두 한글 서체 모두 `Weight` 행이 사라진다.
- **탈네모꼴은 재배포 가능한 것이 이것뿐이었다.** 찾은 범위: Google Fonts 한글 38종
  전부 네모꼴(Orbit·Dongle·Gugi·Moirai One까지 눈으로 대조), 공유마당 OFL 목록도
  마찬가지, 눈누 계열은 대부분 폰트 파일 재배포 금지. UnJamo는 Un 글꼴(1998 HLaTeX
  type1 → 2003 TrueType)의 하나이고 **GPL-2에 폰트 예외가 없다.**

**GPL-2는 열려 있는 결정이다.** 이 도구는 아웃라인을 파일로 내보내므로(SVG의 패스,
OBJ의 정점), 예외 없는 GPL 폰트에서 그 산출물이 파생물인지가 다투어진다 — FSF가 폰트
예외를 만든 이유가 그 애매함이다. **상업 작업에 쓸 사람에게는 실질적인 위험이라
사용자가 알고 고르도록 남겨 뒀다.** 빼기로 하면 세 줄이다(`faces.ts` 항목 · 폰트 파일 ·
`public/fonts/`의 라이선스 둘). 배선은 서체와 무관하게 남는다. 자세한 것은
`src/shared/media/type/fonts/NOTICE.md`.

**검사.** 폰트 심사의 한글 판이 `glyphs.test.ts`에 들어갔다 — ㅁㅂㅇ은 구멍을 가진 한
조각, ㅃ은 두 조각, `이`=2 / `한`≥3, 음절 안의 카운터, 11,172자 전수. **ㅆ·ㅍ은
단정하지 않는다**(서체마다 다르고 둘 다 정당하다). 그리고 게이트 불변식이 한글에서
다시 480조합 돈다(서체 2 × 텍스트 5 × 부품 4 × 런너 4 × 밀도 3). 전부 통과.
**첫 판은 ㅃ을 한 조각으로 단정했다가 서체 둘이 옳다고 알려 줘서 고쳤다** — 문자 체계의
사실과 드로잉의 선택을 가르는 것이 이 심사의 전부다.

**실측(개발 서버, M2 Max).** 서체 도착 ~1.3초(2.2MB). `한글 부품`(프레임 4 · 덩어리
35): 플랜 0.9ms · 압출 5.8ms. 17음절 문장(덩어리 142): 1.9ms · 9.8ms. 라틴과 같은
자릿수다.

**기본값이 입체로 바뀌었다.** 근거는 `store.ts`의 주석에 적혀 있다 — 이 도구가 만드는
것이 성형된 물건이고 평면은 그것의 도면이므로, 먼저 보이는 화면이 도면이면 도구를
거꾸로 가르친다. 대가는 도구를 열 때 three 청크 하나이고, `Form`이 한 번의 클릭으로
도면을 되돌린다.

### 5. Tool 03 Billboard Typography — 등록됨

```bash
npm run dev   # → http://localhost:5173/#/billboard-typography
```

**프로토타입은 지웠다.** `proto-billboard.html`과 `proto/`가 없어졌다 — 같은
렌더러의 사본 둘을 남겨 두면 반드시 갈라지고, 그때 어느 쪽이 진짜인지 아무도
모른다. 렌더러는 `render/View.tsx` + `render/build.ts` 하나뿐이고, 도구의 Stage와
메인 페이지의 미리보기 카드가 같은 것을 쓴다.

**three.js는 지연 로드한다.** `index.ts`가 `Preview`·`Panel`·`Stage`를 각각
`lazy` + `Suspense`로 감싸고 겉은 평범한 컴포넌트로 내보낸다. 인덱스 청크 527KB,
three 597KB 별도, 한글 폰트 998KB 별도. **`Tool` 인터페이스는 한 줄도 안 고쳤다.**

**"결과물은 항상 편집 가능한 SVG"가 도구별 계약으로 내려왔다.** `CLAUDE.md`를
고쳤고, 메인 페이지 소개 문구와 `index.html`의 description에서도 그 약속을 뺐다 —
셋 중 둘만 SVG다. 각 도구의 `spec`이 자기 산출물을 적는다.

**남은 비용 하나: 한글 폰트 서브셋.** 조선일보 견고딕 전체 자족이 **3.9MB**다.
동적 import 뒤라 인덱스에는 안 실리지만 도구를 열면 그만큼 받는다. 빌드에 서브셋
단계가 아직 없다. 라이선스 원문도 아직 없다 — `fonts/NOTICE.md` 참조.

### 2차 정리 — 레퍼런스(`0824_building shape ref`)에 맞춤

**① 빈 간판을 없앴다.** 간판 개수 = 단어 개수다. 밀도를 채우려고 글자 없는 이웃
간판을 넣었었는데, 건물은 그럴듯해지고 도구는 틀려졌다 — **아무것도 안 쓰인 간판은
장식이고, 이건 타이포그래피 도구다.** 대신 밀도를 **길이**에서 얻는다: 한국 상가의
파사드 띠는 상호가 두 자든 여섯 자든 그 층의 폭을 다 쓴다. 그래서 짧은 단어는 짧은
간판 + 가짜 이웃이 아니라 **긴 띠 하나**가 된다(`STRETCH` 4.5).

**② 간판이 판이 아니라 띠다.** 레퍼런스가 만장일치다 — 층마다 가로 띠가 폭을 다
쓰고, 그 아래 창문 한 줄, 모서리에 세로 기둥. 그래서 `TILE_HEIGHT`를 0.86 → **0.55**
로 내렸다. **벽은 간판 사이가 아니라 행 사이에서 보인다.** 이걸 거꾸로 잡았던 게
예전 버전이 색타일 탑이 된 이유고, 그러면 커버리지 하한이 구조적으로 보장된다 —
띠가 행의 절반을 조금 넘게 차지하니 나머지는 무조건 벽이다.

**③ 1층과 옥상을 비웠다.** 옥탑 헛간 · 다리 달린 물탱크 · 테라스 난간 · 차양을 전부
지웠다. 지어낸 것처럼 보였고, 특히 옥상은 없는 구조물이 건물의 관冠 자리를 놓고
간판과 경쟁하다 진다. 지금은 **매스 둘**(프리즘 + 1층 플린스), 옥상은 파라펫 하나,
1층은 유리 띠 + 인방 + 문턱. **조용한 밑단이 간판을 주인공으로 만든다.**

**④ 세로 간판은 늘리지 않는다.** 행 전체에 스트레치를 걸었더니 세로 기둥이 옆으로
불어나 "음절을 이유 없이 쌓은 넓은 판"이 됐다. 기둥은 좁아서 기둥이다.

**⑤ 창문은 띠 아래 줄에 들어간다.** 예전엔 "간판 없는 자리"에 흩뿌렸는데 그러면
패킹이 남긴 우연한 틈에 생겨서 층과 아무 관계가 없었다. 지금은 각 띠 바로 아래,
벽이 보이는 유일한 자리에 한 줄로 간다.

**⑥ 배경색이 이제 정확히 같다.** 지면을 씬에 칠하면 톤매퍼를 통과해서 다른 두 도구와
미묘하게 어긋났다 — 눈으로 맞출 수 없는 종류의 어긋남이다. **캔버스를 투명하게 두고
지면은 CSS로 칠한다.** 그러면 사이트의 종이색 그대로다.

**⑦ 산출물이 OBJ·STL이다.** 그림만 내보내면 이미지 생성기지만 모델을 내보내면
오브젝트 생성기다. 라이브 씬에서 빼내지 않고 `buildBuilding`으로 **새로 짓는다** —
조명·카메라·포스트·베이스 플레이트는 보는 일에 속하지 물건에 속하지 않고, 돌아가는
렌더러에 손을 넣어 그것들을 걷어내는 방식이 둘을 조용히 갈라놓는 길이다.
**글자는 밀어낸 솔리드다** — 두께 0면 STL에서 속이 없는 껍데기가 된다. 200mm 기준으로
내보낸다(레퍼런스가 찍은 그 책상 모형 크기).

### 3차 — 건물 디자인 (레퍼런스 `0824_building shape ref`)

세 안(판상 · 기둥과 창 · 층띠)을 축측도로 그려서 고르게 했고, **1번(판상) + 3번(층띠)**
을 섞기로 했다. 옵션 이미지는 스크래치패드에만 있고 저장소에는 안 넣었다.

**층띠에서 가져온 것** — 층마다 간판 띠가 슬래브 선에 매달리고, 그 아래 창 코스가 있고,
**슬래브가 실제로 튀어나와** 층이 물리적으로 끊긴다.

**판상에서 가져온 것 — 창.** 이게 제일 크다. **창은 벽에 붙이는 틀이 아니라 벽을
뚫은 구멍**이어야 한다. CSG가 없으니 이렇게 했다:

> 코어를 **스킨 두께만큼 안으로 물린다**(`SKIN` 0.3). 스킨은 개구부를 뺀 *남는 부분*만
> — 층마다 전폭 패널 + 창 코스의 기둥들 — 로 쌓는다. 스킨 바깥면이 정확히 레이아웃이
> 재는 벽면에 놓이므로 **하류 코드는 스킨의 존재를 몰라도 된다.** 구멍으로 보이는 건
> 코어의 자기 면이고, 리빌과 창턱이 진짜라서 AO가 거기에 걸린다.

**1층도 같은 방식이다.** 처음엔 통짜 플린스를 유리 앞에 세워서 유리가 건물 안에
들어가 있었다 — 1층 전체가 회색 덩어리로 나온 원인. 지금은 유리가 코어 면에 있고
인방·문턱·양 옆 기둥이 그 앞으로 나온다. 슬랫 차양은 그 위에.

**간판 색은 하나다.** 여섯 개를 돌렸더니 색표처럼 읽혔고, 더 나쁜 건 **눈이 글을
읽는 대신 색으로 보드를 분류**한 것이다. 사이트의 `--accent`를 그대로 쓴다 — 화면
토큰 쪽 규칙("켜진 것 하나에만, 장식으로 쓰지 않는다")이 작품 쪽에서도 같은 일을 한다.

**간판은 층 선에 매달린다.** 행 안에서 가운데 정렬하면 벽 한복판에 떠서 소속이 없고,
창 코스의 높이까지 잡아먹는다. 위 여백만 8%로 두고 나머지는 전부 창 코스다.
높이 지터도 없앴다 — 높이가 제각각인 띠는 파샤가 아니라 스티커로 읽힌다.

**옥상과 1층은 여전히 비어 있다.** 옥탑은 계단실 하나, 파라펫은 립 하나. 지어낸
구조물이 건물의 관 자리를 놓고 간판과 경쟁하면 진다.

**4차 — 모델링 셋과 비례 다이얼 둘**

**① 파라펫을 네 면 다 만들었다.** 카메라가 정면으로 보는 두 면에만 있었다. 그림이면
넘어갈 일이지만 모델이면 아니다 — **카메라는 내려다보고 있어서 옥상 바닥이 다 보이고**,
끝난 모서리는 잘라낸 단면으로 읽힌다. OBJ·STL로 나가는 것도 이유다. "뒷면은 모델링
안 함"은 거기서 생략이 아니라 구멍이다.

**② 옥탑이 떠 있었다.** 계단실이 파라펫 꼭대기 높이에 놓여 있어서 바닥에서 파라펫
높이만큼 떠 있었다. 바닥에 앉혔다.

**③ 최상층 스킨이 파라펫까지 안 올라갔다.** 처마 밑에 코어가 그대로 드러난 홈이
한 줄 생겼는데, 실제로 모델링 실수였다.

**④ 1층 둘레가 건물 밖으로 튀어나가 있었다.** 인방·문턱이 좌우로 `jut`만큼 더
나가서 선반처럼 보였다. **앞으로만 나가고 좌·뒤는 매스와 나란히** 있게 했다. 코어도
겹쳐 있었다 — 타워 코어와 플린스 코어가 같은 자리를 차지해서 z-파이팅 소지가 있었다.
타워는 이제 `PLINTH` 위에서 시작한다.

**⑤ Width · Height 다이얼.** 문장이 요구한 것에 곱해지는 비례다. **배치가 알아서
따라간다** — 넓히면 한 층에 단어가 더 들어가서 같은 글줄이 더 낮아지고, 높이면 같은
단어가 더 많은 층에 퍼진다. "행당 단어 수" 같은 컨트롤을 두지 않는 이유가 이것이다.

**불변식 둘을 척도에서 자유롭게 고쳤다.** 기본값이 바뀌자(fill 0.95→0.45, depth
1→0.25) 절대 수치로 쓴 두 테스트가 깨졌다 — 테스트가 설계가 아니라 그때의 기본값을
검사하고 있었다는 뜻이다. 점유율은 **`style.fill`에 대해** 재고, 깊이는 "가장 깊은
것이 가장 얕은 것의 몇 배인가"로 잰다. 종류 이름을 세던 예전 버전은 종류표를 갈아
끼우는 순간 깨졌고, 고정 거리를 세던 버전은 다이얼 기본값이 내려가는 순간 깨졌다.
**두 번 깨진 자리는 세 번째로 안 깨지게 써야 한다.**

### 5차 — 다이얼 넷

**① 간판 폭은 글자 길이에서 온다.** 행의 목표 폭까지 늘리던 것(`STRETCH`)을 통째로
없앴다. 한 음절짜리 간판과 다섯 음절짜리 간판이 같은 크기로 나오면 그건 타이포그래피가
아니라 격자다. 대신 **건물이 가장 긴 단어에 맞춰 커진다** — 앞벽이 제일 긴 보드보다
좁으면 그 보드가 눌려서 글자가 작아지는데, 규칙 위반은 아니어도 실수처럼 보인다.

**② 정돈됨(Order).** 0이면 모든 보드가 같은 폭·같은 높이·같은 자체 크기로 한 층에
하나씩, 가운데 정렬된 기둥이 된다. 올리면 각자 자기 단어 폭을 갖고, 깊이 종류가 열리고,
세로 기둥이 나오고, 한 층에 둘 이상이 선다. **스위치가 아니라 다이얼인 이유**는 재미있는
설정이 중간에 있기 때문이다 — 대부분 정돈된 건물은 디자인된 것으로 읽히고, 완전히
흐트러진 건물은 잡음으로 읽힌다.

**③ Girth — 건물 깊이.** 앞벽 폭에 대한 측면 벽의 비율. `Width`는 앞벽을, `Girth`는
깊이를 따로 정한다. 간판 돌출은 `Relief`로 이름을 바꿔서 둘이 안 헷갈리게 했다.

**④ Angle — 방위각.** **시점이 아니라 레이아웃의 입력이다.** 0°(정면)에서는 측면 벽의
화면 폭이 0이라 그 벽의 간격 요구가 무한대로 커지고, 패커가 알아서 거기 아무것도 안
놓는다 — 특수 분기가 아니라 같은 산술의 결과다. `shiftOf`의 탄젠트에 하한을 뒀는데,
∞ − ∞ = NaN이 패킹을 오염시키기 때문이다. 앙각은 고정.

**잡은 버그 하나가 재미있다 — mulberry32는 등차수열 시드를 싫어한다.**
`seed*A + index*B`로 시드를 만들면 **첫 번째 난수는 잘 흩어지는데 세 번째가 안 흩어진다.**
간판 종류를 고르는 게 정확히 세 번째 draw여서, 아홉 단어 글줄에 종류가 둘밖에 안 나왔다.
다이얼이 고장 난 것처럼 보였지만 고장 난 건 없었고, 생성기에 경사로를 먹이고 있었을
뿐이다. 시드를 먼저 아발란치(`hashSeed`)하면 세 번째 draw도 첫 번째만큼 좋다.

### 6차 — 두 벽에 고르게, 그리고 진짜 정면

**① 간판을 두 벽에 나눠 준다.** 앞벽 구간이 `s` 순서상 먼저라서 그리디가 앞벽을
채우고 측면에는 남는 것만 줬는데, 균등 분배 패스가 행당 한 장으로 줄이고 나면 남는
게 없어서 **측면 아래 절반이 통째로 비었다.** 이제 행마다 슬롯을 두 벽에 나눠 주고,
한 장뿐인 행은 층마다 벽을 번갈아 간다.

**단어 순서는 그대로다** — 앞벽이 작은 `s`를 가지므로 앞 단어를 먼저 받는다. 바뀐 것은
**각 벽이 몇 장까지 받는가**뿐이고, 그래서 읽기 불변식은 손댈 필요가 없었다.

**Order가 낮으면 측면은 빠진다.** 정돈된 건물은 주 파사드의 기둥 하나여야 한다.

**② 측면 벽은 단어를 눌러 넣지 않는다.** 건물은 **앞벽이 가장 긴 단어를 담도록** 크기가
정해지므로 앞벽에서의 압축은 거의 일어나지 않는 최후 수단이다. 측면은 설계상 좁아서
그 보장이 없는데, 거기에 긴 단어를 밀어 넣으면 아무 이유 없이 그 단어의 자체 크기만
작아진다. 다음 층 앞벽으로 넘긴다 — 어느 쪽이든 단어를 잃지는 않는다.

**③ Angle이 앙각까지 같이 움직인다.** 앙각을 12°에 고정해 두니 "각도 0"이 **정면인데
살짝 위에서 본 것**이 되어, 파사드를 정면으로 읽을 수도 없고 옥상을 제대로 볼 수도
없는 어중간한 뷰였다. 둘을 묶으면 이 컨트롤의 뜻이 **"얼마나 사선인가"** 하나로
정리된다 — 0에서 진짜 정면 입면, 기본값에서 건물이 설계된 3/4 뷰.

**④ 정면에서는 측면에 슬롯을 배정하지 않는다.** 4° 아래에서는 측면 벽의 화면 폭이
사실상 0이라, 거기 배정한 슬롯이 그대로 **빈 층**이 된다. 실제로 정면 뷰에서 한 층씩
건너뛰며 비어 있었다.

### 7차 — 간판 종류, 베벨, 글자 처짐

**① 한글이 정말로 처져 있었다.** 조선견고딕은 음절을 **베이스라인 아래로 0.09캡** 까지
내려 그린다(`한`의 bbox가 y1 −831 ~ y2 +91). 그런데 배치는 캡 박스를 기준으로 가운데를
잡고 있었으니, 가로 간판마다 글자가 캡의 10분의 1만큼 아래로 내려앉았다. 작지만 한 번
보이면 안 보이던 것으로 못 돌아간다.

→ `MeasuredWord`가 **잉크의 위/아래 끝**을 들고 다니고, 그것을 기준으로 가운데를 잡는다.
서체가 무엇이든, 단어가 마침표로 끝나든 상관없이 한 번에 맞는다. 테스트도 캡 박스가
아니라 잉크로 재게 고쳤다 — **엠 박스로 재면 넘친 간판이 통과하고 제대로 앉은 간판이
떨어진다.**

**② 베벨은 된다.** 모든 박스를 모따기한 압출로 바꿨다. 완벽하게 날카로운 모서리는
어떤 공산품에도 없고, 손 안 댄 렌더가 렌더처럼 보이는 이유의 대부분이 그것이다 —
진짜 모서리는 그 선을 따라 빛을 문다.

**위험한 곳은 딱 하나, 클램프다.** 베벨이 가장 짧은 변의 절반을 넘으면 프로파일이
뒤집혀서 면이 안쪽을 보고, 화면에서는 검은 얼룩으로 STL에서는 비-매니폴드로 나온다.
**모든 박스를 자기 최단변의 4분의 1로 클램프**해서, 종잇장 같은 슬래브는 실낱만큼,
매스는 제대로 된 모따기를 받는다. 실측: 아티팩트 0, STL 17,080 삼각형(854,084B —
헤더 84 + 50×삼각형에 정확히 맞아떨어지므로 형식도 온전하다).

**③ 간판 종류 넷.** 색이 하나뿐이니 변화는 **극성 · 테두리 · 글자**에서 온다. 실제
간판이 변화를 얻는 방식도 그렇다 — 상가 한 줄은 색이 다른 게 아니라 극성이 다르다.

| | |
|---|---|
| `panel` | 색 판에 어두운 글자. 기본 파샤 (0.48) |
| `inverse` | 어두운 판에 색이 뒤집혀 나온다. 발광 박스 (0.16) |
| `plate` | 밝은 판 + 튀어나온 색 테두리 + 작은 글자. 사무실·의원 명판 (0.20) |
| `channel` | **판이 없다.** 글자가 벽에 직접 붙어 깊게 솟는다 (0.16) |

`channel`이 제일 세다 — 파사드를 장식하는 게 아니라 바꾼다. 여기에 **로고 블록**(판 한쪽
끝의 정사각형)이 붙기도 한다. 어느 업종도 거기에 같은 걸 안 붙이니까, 사각형 하나로
그 말이 된다.

**가중치는 일부러 기본 파샤 쪽으로 기울였다.** 처음 배분(inverse 0.24)에서는 절반이
반전되어 **주황 건물이 회색이 됐다.** 반전과 명판은 간판 *안의* 강조이지 기본형이 아니다.

**폰트는 아직 한 벌뿐이다.** 정적 컷이라 축이 없으니 지금 얻을 수 있는 타이포그래피
변화는 **크기 · 자간 · 극성** 셋이고, 종류마다 다르게 잡아 뒀다(명판은 0.74캡,
채널은 자간 0.06). **진짜 서체를 하나 더 넣으려면 또 몇 MB**다 — 이미 3.9MB를 받고
있으므로, 서브셋 단계 없이 두 번째 서체를 넣는 건 미결로 남긴다.

### 8차 — 빌딩이 아니라 상가로

레퍼런스가 바뀌었다: 종합상가 빌딩이 아니라 **종로·을지로의 3~5층 구축 상가**다.
문제는 디테일이 아니라 **문법**이었다.

**① 층수와 비례.** `ASPECT` 1.05 → **0.78.** 상가는 높은 것보다 넓다. 그리고 행 수를
"단어당 비율"로 추측하던 것을 **수용량에서 유도**하도록 바꿨다 — 이 설정에서 한 층이
간판을 몇 장 받는가(`perRow`)를 알고 있으니, 몇 층이 필요한지는 물어볼 필요가 없다.
추측한 비율은 패커의 실제 수용량과 어긋나서, 네 층에 넉넉히 들어갈 글줄이 여섯 층으로
나왔다.

**② 간판이 위는 성기고 아래는 빽빽하다.** 균등 분배는 "간판을 붙인 사무실 빌딩"으로
읽힌다. 레퍼런스는 전부 위층이 창 + 간판 한둘이고 나머지는 1층에 몰린다. 남은 단어를
매 층마다 다시 나누므로, 못 받은 층은 아래로 넘긴다.

**③ 모서리 세로 기둥.** 상가의 서명 같은 요소다. **측면 벽이 세로판을 우선한다** —
측면은 행에서 마지막이라 거기 놓아도 문장 순서가 안 흔들리고, 그렇게 좁은 면에는
음절 기둥 말고 들어갈 것도 없다. 앞벽에서도 행의 마지막 세로판은 모서리 쪽으로 민다.

**④ 1층이 다른 재료다.** 밝기만 다른 게 아니라 **색이 다르다**(따뜻한 타일 톤). 한 색의
명도 세 단계는 그림자 진 한 장의 벽으로 읽힌다. 차양도 앞으로 나온 슬래브 + **밑으로
떨어지는 밸런스**로 바꿨다 — 그 립이 없으면 선반이고, 있으면 차양이다.

**⑤ 시도를 넷으로 늘렸다.** 아래로 갈수록 빽빽한 배분은 슬롯을 단어 수만큼만 만들어
**빈틈이 없어서**, 한 층이 자기 몫을 못 받으면 패스 전체가 무너지고 그리디 배치로
떨어졌다(위쪽만 빽빽하고 측면은 텅 빈 그것). 슬롯 한둘의 여유를 주고 다시 시도한 뒤에야
포기한다. 그리디 패스는 **캡이 아예 없다** — 그건 "들어가느냐"만 판정해야 하고, 거기에
취향을 넣으면 취향이 문장을 잃는다.

**⑥ 압축은 온전한 앞벽에서만.** 위층에서 내려온 세로판이 앞벽을 반으로 가른 조각에서
단어를 눌러 넣으면 자체 크기만 작아진다. 건물이 크기를 보장하는 건 **온전한 앞벽**
하나뿐이다. 그리고 그 보장은 **가장 넓게 나올 수 있는 조합**(깊은 돌출 간판 + 로고
블록)으로 재야 한다 — 기본형으로 쟀더니 히어로 간판이 약속받은 벽보다 3분의 1 넓게
나왔다.

### 9차 — 간판 종류를 셋으로

**주황 판 + 검정 글자 · 흰 판(테두리 없음) + 검정 글자 · 판 없이 검정 글자.** 이 셋이
기본이고, 글자는 전부 검정이다. 바뀌는 것은 **글자가 무엇 위에 있는가**뿐이다.

**어두운 판에 색을 뒤집어 넣던 것(`inverse`)은 뺐다.** 쓸 만한 빈도로 넣으면 주황
건물이 회색이 되고, 문장을 나르는 간판들과 경쟁하지 합류하지 않는다.

**채널 글자를 주황에서 검정으로 바꿨다.** 밝은 벽 위의 주황 글자는 받쳐 줄 것이 없어서
읽히지 않는다.

**알아 둘 것 하나:** 흰 판은 테두리가 없으므로 밝은 벽 위에서 **판 없는 것과 거의
구분되지 않는다.** 지금은 베벨 모서리와 접촉 음영이 그 차이를 만드는 전부다. 벽 색을
조금만 내리면 확실히 갈린다.

추가 여섯 안은 스크래치패드의 `sign-proposals.html`에 있다 — 저장소에는 안 넣었다.
그중 **B(밑줄)와 E(반전)** 이 채택되어 들어갔다. 지금 다섯 종류다:

| | |
|---|---|
| `panel` | 주황 판 + 검정 글자 |
| `plate` | 흰 판(테두리 없음) + 검정 글자 |
| `channel` | 판 없이 검정 글자 |
| `underline` | 흰 판 + 발치의 주황 띠 |
| `knockout` | 주황 판 + 흰 글자 (유일하게 글자가 밝다) |

밑줄판은 발치의 띠가 자기 몫을 먼저 가져가고 **글자는 남은 면에서 가운데를 잡는다** —
판 전체로 가운데를 잡으면 글자가 자기 띠에 반쯤 묻힌다. 띠 높이는 `Sign.foot`로
나가므로 렌더러와 테스트가 같은 수를 본다.

### 세로 간판이 층 슬래브에 잘려 있었다

**두 층에 걸치는 판은 정의상 층 선을 가로지른다.** 그런데 슬래브는 구조가 나오는
만큼 나오고(0.5), 간판은 Relief가 낮으면 그보다 얕게 붙었다. 그러면 **슬래브가 간판
앞을 지나가며 글자를 잘라 낸다.** 실제로 잘려 있었다.

→ `SLAB_OUT`을 `plan.ts`에 두어 슬래브와 간판이 같은 수를 보게 하고, **span > 1인
종류는 그보다 나오도록 바닥을 깐다.** 바닥을 다이얼이 아니라 돌출값에 건 이유는,
다이얼은 "얼마나 튀어나와 보이는가"의 문제이고 이건 "묻히느냐"의 문제라서다.

### 판 없는 간판 뒤는 비운다

**창은 구조가 아니었다.** 스킨의 창 코스는 층마다 무조건 뚫려 있었고, 그래서 판 없는
간판(글자만)은 어두운 개구부 줄 위에 걸쳐 앉아 **단어이기를 그만뒀다.** 판이 있는
간판은 불투명하니 알아서 가렸고, 그래서 이 문제는 `channel`에서만 보였다.

→ 창 코스를 **기둥의 나열이 아니라 개구부의 여집합**으로 짓는다. 그러면 "비워야 하는
자리"가 그냥 *뚫지 않은 개구부*가 되고 스킨이 알아서 덮는다. 배수관도 그 자리를 지나가면
통째로 뺀다 — 글자 뒤를 비우는 이유가 파이프 하나로 없어지면 안 된다.

**측면 벽의 좌표 변환을 한 군데로 모았다.** 측면은 `s`가 커질수록 `z`가 작아지는데,
개구부를 `z`로 만들고 `s`로 검사하면 두 규약이 갈라진다. 간판의 좌표는 `s`이므로
개구부도 `s`로 만들고 마지막에 되돌린다.








한국·동아시아 상가의 오밀조밀한 간판에서 출발한다. 글줄이 간판이 되어 건물을 이루고,
알고 보면 하나의 글줄로 읽힌다. **실제 3D(three.js)로 라이브 렌더**하기로 결정했다.

**스파이크가 물은 것과 답:**

1. **그림자가 깊이를 읽히게 하는가 — 예.** 3D를 고른 가장 큰 실익이 이것이다.
   깊이가 다른 간판이 벽과 서로에게 그림자를 던지고, 깊이 순서가 눈에 명확하다.
   SVG로는 흉내조차 못 냈던 것이고, 09·10의 인상 전체가 여기서 온다.
2. **글리프 파이프라인 — 된다.** `scene/glyphShapes.ts`. opentype 커맨드를
   `THREE.Shape`에 **평탄화 없이** 그대로 넘긴다(`quadraticCurveTo`/`bezierCurveTo`).
   테스트가 "폰트의 곡선 수 = 결과의 곡선 수"로 고정한다 — 하나도 안 깎인다.
3. **오소그래픽 고정** — 01·03·08이 그렇고, 무엇보다 건물이 자라도 구도가 안
   흐트러진다. 퍼스펙티브는 높이가 늘면 수직선이 벌어져 다시 맞춰야 한다.
4. **비용 — 108fps / 9.2ms** (환경광 + AO 패스 포함).

**2차 — 완성도.** 1차는 "돌아간다"를 증명하고 **맹맹한 3D 렌더처럼 보였다.** 홍콩
당루 디오라마 사진을 기준으로 다시 잡았고, 문제는 렌더러가 아니라 넷이었다.

1. **카메라가 너무 사선이었다.** 38°/30°는 두 벽을 똑같이 보여줘서 어느 쪽도 주
   파사드가 못 된다. → **21°/12°.** 읽기 규칙은 0°–90° 사이 어느 방위각에서도
   성립하므로, 각도는 제약이 아니라 조형 선택이다.
2. **조명이 양이 아니라 종류가 틀렸다.** 강한 키 + 딱딱한 그림자가 "렌더처럼
   보이는" 것의 대부분이다. 레퍼런스에는 키가 아예 없고, 깊이는 **맞닿은 자리의
   어두워짐**에서 읽힌다. → 환경광은 `RoomEnvironment` PMREM,
   깊이는 **GTAO 패스**, 디렉셔널은 힌트로 강등(0.8), 톤매핑 ACES.
   재질도 `MeshLambert` → `MeshStandard`(램버트는 환경맵을 못 받는다).
3. **작은 부품이 부족했다.** 실외기·배수관·브래킷·창틀·파라펫·물탱크·난간·차양,
   그리고 **층마다 슬래브 띠.** 개별로는 아무것도 아니지만 완성도의 대부분이다.
4. **박스 하나는 건물이 아니다.** → 파사드 평면을 공유하는 높이 다른 두 덩어리 +
   앞으로 튀어나온 저층 상가 + 옥탑.

**창은 개구부지 판이 아니다.** 처음엔 트림색 박스를 그리고 그 안에 유리를 넣었는데,
박스의 앞면이 유리를 완전히 가려서 창마다 밝은 타일로 나왔다 — 파사드가 지어진 게
아니라 인쇄된 것처럼 읽힌 원인이 이것이다. 프레임은 **구멍을 둘러싼 바 네 개**여야
한다.

**밝은 재질은 환경광 아래서 갈 곳이 없다.** 근백색 구조체 + 환경광 1.2 + 노출 1.1이면
중간 회색 콘크리트까지 종이 흰색으로 날아가고 아무것도 형태를 싣지 못한다.
구조체를 중간 회녹색으로 내리고 환경광 0.82 / 노출 0.95.

**대비는 명도가 아니라 채도에 있다.** 탁한 회녹색 건물이 채도 만점의 간판을 이고
있는 것 — 명도를 맞추고 채도를 갈라놓는 것이 색칠한 건물을 색표처럼 안 보이게 한다.

**구도는 모델을 재서 맞춘다.** 상수로 프레이밍하면 창 모양 하나에서만 맞는다.
모델의 바운딩 박스 여덟 점을 카메라 공간으로 투영해 줌과 오프셋을 정한다 — 시선
방향은 고정이므로 구도 자체는 흐트러질 수 없다.

**프레임 시간을 유휴 시간으로 재지 말 것.** 버스트가 끝난 뒤 리사이즈 한 프레임을
"1 fps · 934ms"로 찍었다. 느린 적 없는 것을 최적화하러 보내는 종류의 계측이다.
버스트 구간만 잰다.

**잡은 문제 둘:**

- **한글 폰트 998KB가 프로덕션 빌드에 실렸다.** 공유 `faces.ts`에 등록했더니
  그것이 인덱스 모듈 그래프에 있어서 `?url` import가 에셋을 끌고 들어갔다. 아무
  도구도 안 쓰는 폰트다. → **폰트와 face 스펙을 도구 안으로 옮겼다**
  (`scene/face.ts` · `fonts/`). 직전에 적어 둔 교훈이 일주일 만에 그대로 왔다 —
  **실제 두 번째 사용자가 생길 때 공유로 옮긴다.** 승격하면 스펙이 올라가고 OFL
  원문이 `public/fonts/`로 간다.
- **`Face`가 두 가지를 묶고 있었다** — 파싱된 폰트와 공유 등록부의 항목.
  `applyAxes`만 후자를 필요로 하는데(축을 찾아본다) `drawGlyph`·`runWidth`·
  `missingFrom`이 다 그것을 요구하고 있었다. `Parsed`를 갈라내서, 자기 폰트를
  가져오는 도구가 시그니처를 맞추려고 등록부 id를 발명하지 않아도 되게 했다.

**프로덕션 빌드는 스파이크 전과 완전히 동일하다** — JS 525.26KB, 라틴 폰트 셋,
`three`·한글 폰트·이 도구의 심볼 모두 0.

### 1단계 — 레이아웃 엔진 통과

`geometry/` 다섯 모듈. **three.js를 한 줄도 import하지 않는다** — 렌더러가 마음에
안 들면 렌더러만 갈아끼운다는 보험이고, 덕분에 불변식 전체가 브라우저 없이 돈다.

| | |
|---|---|
| `wall.ts` | 병합된 벽선 · 화면 투영 · **깊이 시프트** |
| `plan.ts` | 글줄 길이 → 행 수 → 발판·높이 · 매스 · 고정 카메라 |
| `pack.ts` | 행 단위 배치 · 글자 없는 간판 · 간격 · 채움률 |
| `signs.ts` | 종류·깊이·색 배정 · 보드 위 글자 배치 |
| `layout.ts` | 조합 · 읽는 순서 · 가림 계산 |

**23개 불변식 테스트가 실제로 넷을 잡았다.** 그중 둘이 설계를 바꿨다.

**① 벽에서 떠 있는 간판은 화면에서 옆으로 밀린다.** 이게 이 도구의 진짜 함정이었다.
`X(s) = s·cos a − (D+out)·sin a` 이므로 정면 벽에서 `out`만큼 나온 보드는
`out·tan a` 왼쪽에 있는 것처럼 보인다. **측면 벽은 훨씬 나쁘다** — 그 벽의 화면
폭은 `sin a`로 눌리는데 돌출은 `cos a`로 투영되니 시프트가 `out·cot a`, 얕은
방위각에서는 측면 벽 전체 폭에 맞먹는다. 그래서 **`(행, s)` 순서는 화면 순서가
아니고**, 깊이가 다른 이웃 둘이 자리를 바꿔 문장이 틀리게 읽힌다.

- 해결: **인접 쌍마다** 필요한 최소 간격(`gap > shift(prev) − shift(next)`)을 두고,
  **측면 벽의 돌출을 0.5로 제한한다**(`SIDE_MAX_OUT`). 부수 효과로 한 행 안에서는
  화면 겹침이 아예 없어져 삼킴도 같이 막힌다.
- 처음엔 모든 간격에 "최악의 시프트 폭"을 물렸다. 안전하지만 벽 길이의 1/5를
  공기에 쓰고 채움률이 바닥났다. 쌍마다 정확히 계산하는 것이 맞다.
- **테스트를 모델이 아니라 투영에 대고 써야 잡힌다.** 처음 쓴 테스트는 타일을 `s`로
  정렬해 `s`와 비교하는 동어반복이었다. 실제 월드 박스를 투영해 왼쪽 끝으로 정렬하게
  바꾸자 즉시 걸렸다.

**② 균등 분배와 단조 성장은 같이 못 한다 — 두 패스로 나눠야 한다.** 위쪽 행만
그리디로 채우면 아래층이 텅 비고, 남은 단어 수로 행당 할당량을 계산하면 **단어를
하나 더했을 때 8층이 6층으로 줄었다.** 할당량이 전체 단어 수에 의존하니 접두사의
배치가 바뀌기 때문이다. → **1패스(그리디)만 행 수 가능 여부를 정하고, 2패스(균등)는
행 수가 확정된 뒤에만 돈다.** 단어의 종류가 (단어, 인덱스)만으로 결정되므로 접두사
배치는 뒤에 무엇이 오든 동일하고, 그래서 "단어를 더하면 건물은 줄지 않는다"가
경험이 아니라 성질이 된다.

**③ 채움률 50–70%은 내가 틀린 목표였고, 보고 고쳤다.** 60%면 벽이 사라진다 —
합이 맞으려면 보드 하나가 벽 폭의 1/4이 되어야 하고, 건물이 색타일 탑이 된다.
직전에 접은 시도가 실패한 그 지점을 반대편에서 다시 만난 것이다. 요구는 퍼센트가
아니라 "실제 벽이 존재하고, 그 위로 다양한 형태와 깊이의 간판"이었고, **40% 근처가
그게 읽히는 자리다.** 테스트도 33–55%로 고쳤다.

**④ 밀도는 크기가 아니라 개수다 — 글자 없는 간판을 넣었다.** 문장의 단어 수는
정해져 있고 파사드는 그보다 많은 간판을 필요로 한다. 글자 없는 이웃 간판이 없으면
짧은 글줄은 거대한 타일 몇 개가 되거나 벽이 비고, 처음 버전이 전자였다.
`style.words`가 그 배분이고 — 올리면 빈 벽에 광고판, 내리면 남의 간판 숲에 문장이
숨는다. **읽기 난이도의 실제 다이얼이 이것이다.**

**⑤ 행 수는 패킹이 아니라 글줄 길이가 정한다.** "가장 작은 행 수"에 맡겼더니
아홉 단어에 5층이 나왔다. 보드가 큰 게 아니라 건물이 작았던 것. `rowsFor(단어 수)`로
직접 못박았다 — 요구사항 문장 그대로이고, 단조성이 자명해진다는 이점이 따라온다.

**창문은 간판이 비운 자리에 들어간다.** 채움률 상한이 남겨 둔 바로 그 자리이므로
두 결정이 싸우지 않고 협력한다.

**의도적으로 미룬 단순화 하나:** 1층 상가가 앞으로 튀어나오지만 **거기엔 간판이
없다.** 레퍼런스에서는 그 면이 더 가까운 평면에 자기 간판을 달고 있다. 읽기 규칙은
그것도 견딘다(한 행 안에서 화면 x는 여전히 단조다) — 행마다 벽 오프셋이 생기는
복잡도를 나머지가 맞기 전에 낼 이유가 없어서 안 했다.

**다음 — 3단계 컨트롤과 내보내기.** 패널은 이미 집 규격을 쓰고 있고, 남은 것은
고해상도 PNG(긴 변 4000px 이상, 오프스크린 타깃)와 4단계 주변 사물이다.
승격할 때 `Output: 'High-res PNG'` · `CLAUDE.md:15`의 도구별 계약 · three.js
코드 분할이 한 건으로 묶인다.

계획 전문은 `~/.claude/plans/vast-popping-map.md`.

### 6. mark 후보 두 행

규칙 3의 "전부에 붙이면 장식이 된다"를 지키려고 8행에서 멈췄다. 다음 후보는
**`Dim`**(사진 위 지면 덮기)과 **`Baseline Shift`**(글자가 선 위/아래로)다.
필요해지면 붙인다.

### 7. 알려진 타협 하나

트래킹 0에 문장 전체를 프레임 하나로 담으면, 위아래가 모두 막힌 조각에 상한을
넘는 긴 게이트가 하나 생긴다. **게이트 없는 조각보다는 긴 게이트가 낫다**는
쪽을 택했고 테스트로 고정해 뒀다. 트래킹·밀도·런너 단위 중 아무거나 움직이면
풀린다. 그래서 기본 트래킹이 0이 아니다.

### 8. 손 안 댄 사항

인덱스 청크가 526KB(gzip 161KB)로 Vite의 500KB 경고를 넘는다. opentype.js가
대부분이고, **메인 페이지가 도구 02와 04의 미리보기를 실제 파이프라인으로 그리기
때문에** 인덱스에서도 필요하다.

**무거운 의존성은 이미 갈라져 있다** — three.js(388KB), MediaPipe JS(154KB),
한글 폰트, 손 모델, wasm 런타임이 전부 별도이고 그 도구를 쓸 때만 온다.
남은 500KB는 "인덱스가 실제 파이프라인으로 카드를 그린다"는 결정의 값이고,
그 결정은 카드가 도구와 어긋나지 않게 하는 값이므로 지불할 만하다. opentype.js
자체를 지연시키려면 인덱스 카드를 그림으로 바꿔야 하고, 그건 다른 거래다.

### 9. Tool 04 Magic Circle Typography — 등록됨

**2026-08-25.** 레퍼런스는 사용자가 대화에 붙여 준 이미지 한 장 —
카메라를 향해 손을 펼친 인물, 그 손 주위를 도는 원형 활자
(*SUMMER OF SOMETHING SPECIAL · Deadline: August 5th 2022*). 아이디어는 그 한
줄이었다: **주먹을 뻗고 손을 펼치면 내가 입력한 문장이 동그랗게 등장한다.**

**그 이미지는 저장소에 없다** — 01·02의 `0819_ref/`나 03의 `0824_*`처럼 폴더로
받은 것이 아니라 대화에 붙은 것이었다. 다시 필요하면 받아서 `0825_magic ref/`로
넣고 여기에 이름을 적을 것. 레퍼런스에서 실제로 가져온 것은 셋이다 — 하나의
연속된 고리로 읽히는 원형 활자, 사진 위의 흰 잉크, 그리고 **손이 화면을 향해
뻗어 있다는 구도** 자체.

설계 근거 전문은 `README.md`의 04절에 있다. 여기에는 **결정과 남은 것**만 적는다.

#### 확정한 것

- **카메라는 장치이고 문서가 아니다.** 손은 `Partial<MagicDoc>` 패치 하나를 만들고,
  화면은 `{ ...doc, ...patch }`를 그리고, 셔터는 **그 같은 패치**를 문서에 굽는다.
  라이브 값과 문서 값이 나란히 존재하지 않으므로, 사진에 안 보이는 자리에 마법진이
  박힌 파일이 나올 경로가 없다. 그래서 카메라가 없어도 도구는 멀쩡히 돈다.
- **`fill: 'repeat'`가 기본값.** 자간을 벌려 원을 닫는 방식(`ring`)은 열네 글자
  문장에 글자마다 80픽셀을 넣는다 — 원은 되고 문장은 안 된다. 인장이 실제로 하는
  대로 문장을 반복해 새긴다. 구분자 `·`는 세 서체에 다 있는지 재서 골랐다.
- **글자는 실제 윤곽선.** `<textPath>`가 아니다. 윤곽선은 `GLYPH_EM`에서 한 번
  뽑아 캐시하고 매 프레임 바뀌는 것은 행렬 여섯 개뿐이다.
- **`Radius`는 손이 잡으면 `Reach`로 갈아 끼워진다**(감추는 게 아니다). 단위가
  픽셀에서 자기 손바닥으로 바뀌는 것뿐이니까. `Bloom`은 그냥 사라진다.
- **20MB는 세 단으로.** 인덱스 0 → 도구 코드 9KB → 카메라 켤 때 런타임+모델.
  `Preview`는 지연시키지 **않았다**(라틴 3종만 쓰므로).
- **첫 추론을 미리 돌려 놓는다.** 실측 6.5초. 자세한 것은 `models/NOTICE.md`.

#### 2차 패스 — 마법진을 덜고 활자를 세웠다

**사용자 지적 두 개.** "표현이 마법진일 뿐이지 너무 마법진스럽다, 최대한 레퍼런스처럼",
그리고 "이건 타이포그래피가 메인이다".

맞는 지적이었다. 1차는 펜타그램·눈금 60개·안쪽 원 2개·이중 테두리를 **기본값으로**
켜 놓고 있었고, 레퍼런스 사진에는 **선이 한 가닥도 없다.** 손 하나와 그 주위를 도는
활자뿐이다.

**바꾼 것:**

1. **모든 furniture를 껐다.** `rim`·`bandRules` 스위치가 새로 생겼고(전에는 테두리와
   띠 괘선이 무조건 그려졌다), `ticks`·`rings`·`starPoints`·`spokes` 기본값이 0이다.
   `Rule`(선 굵기) 행은 **뭐라도 선을 그리기 전까지 안 보인다**(규칙 4).
   실측: 기본 문서의 `#artwork`에 `<circle>`이 **0개**, `<path>` 67개가 전부 글자다.
2. **활자를 키웠다.** `size` 28 → **46**(반지름의 약 1/6). 세 띠가 반지름의 바깥
   절반을 채우고 가운데는 손에 남는다 — 레퍼런스의 비례다. `wght` 700 → 800.
3. **`taper`(패널의 `Step`) 추가.** 줄마다 크기가 안쪽으로 갈수록 한 단씩 작아진다.
   숫자 하나로 위계가 생긴다 — 제목, 그 아래 상세. 기본 0.82.
4. **`angles` — 줄마다 자기 각도.** 이게 레퍼런스를 재현하는 열쇠였다. 처음엔 "줄마다
   n도씩 돌린다"는 균일한 `stagger` 하나로 해 봤는데 **안 된다**: 90도면 나선처럼
   돌아가고 180도면 셋 중 둘이 같은 자리로 돌아온다. 레퍼런스는 제목과 상세가 **둘 다
   위**에 있고 날짜만 아래다 — 어떤 균일한 단계로도 안 나오는 배치다. 그래서 배열
   하나(`[0, 0, 180]`)로 갈아탔고, 패널이 줄마다 한 행을 그린다. **고리를 짠다는 건
   위 줄이 남긴 빈 곳을 어느 호가 덮을지 정하는 일이고, 균일한 값은 그걸 못 한다.**
5. **`fill` 기본값 `repeat` → `natural`.** 레퍼런스는 각 문구가 필요한 만큼의 호다.
   `repeat`(인장)과 `ring`(자간 벌리기)은 남아 있다.
6. **`band` 기본값 `alternate` → `out`.** 세 띠가 같은 방향이라 하나의 연속된 고리로
   읽힌다 — 위는 정방향, 아래는 거꾸로, 레퍼런스 그대로.
7. **기본 문구의 줄 길이를 재서 잘랐다.** 세 호가 서로의 빈 곳을 덮어 원을 닫도록.
   둘째 줄이 길면 위 두 줄을 지나쳐 감기고 읽는 순서를 따라갈 수 없게 된다.
8. **인덱스 카드는 도구의 기본 문서를 카드 배율로 그린다.** 반지름·크기·거터를 전부
   같은 0.773로 줄였다 — 크기를 반지름과 같이 줄이지 않으면 줄이 카드를 감는다.

#### 그 과정에서 잡은 버그 — 디센더가 다음 줄을 먹고 있었다

띠가 **캡 높이만큼만** 자리를 잡고 있었다. 글자는 띠 위에 발을 딛고 서므로 `p`·`g`의
꼬리는 안쪽 단으로 넘어간다. 눈으로는 "좀 좁네" 정도였는데 렌더된 패스의 반경을 재
보니 **둘째 줄의 꼬리와 셋째 줄의 캡이 똑같이 197px**이었다. 거터를 아무리 줘도
거짓말인 상태다.

이제 띠가 캡 높이의 `0.26`을 더 잡는다(`DESCENT`). `out`은 베이스라인 아래로,
`in`은 뒤집힌 상자이므로 바깥으로 — **어느 쪽이든 같은 양을 쓰므로 한 띠의 방향을
바꿔도 아래 단이 밀리지 않는다.** 실측 재확인: 254/235(19px), 187/179(8px).
대문자만 있는 줄은 그 자리를 비워 두는데, 그건 거터가 조금 넓은 것으로 읽힌다.

`CLAUDE.md`에 **버그 유형 10번**으로 올렸다: 활자 레이아웃은 계산으로 끝내지 말고
렌더된 것의 좌표를 뽑아 볼 것.

#### 남은 것 / 안 한 것

1. **실제 카메라 검증.** 위에 적은 그대로 — 손을 흔들어 보는 것은 사람이 해야 한다.
   특히 **`hand/landmarks.ts`의 손가락별 접힘/펼침 기준값**은 해부학에서 잡은
   추정치다. 합성 손에서 주먹 <0.1, 펼친 손 >0.9로 나오고 굽힘에 단조롭지만,
   **실제 손에서 범위 위아래가 다 닿는지는 봐야 안다.** 안 닿으면 그 상수 여덟 개만
   고치면 되고, 테스트는 불변식만 보므로 안 깨진다.
2. **앙각/원근 없음.** 마법진은 화면과 나란한 평면 위에 있다. 손바닥 기울기로
   타원으로 눕히는 것은 일부러 안 했다 — 그러면 산출물이 원 위의 활자가 아니게
   되고 편집 가능한 SVG라는 계약이 흔들린다. 하고 싶으면 그 계약부터 다시 봐야 한다.
3. **wasm 압축은 호스트의 몫이다.** `vision_wasm_internal.wasm`은 11.8MB 그대로
   나가고 `vite preview`는 압축하지 않는다. 실제 호스팅에서 gzip/brotli를 켜면
   4MB대가 된다. 켜야 한다.
4. **`window.__magicHand`는 DEV 전용 훅이다.** 프로덕션 빌드에서 없어지는 것을
   확인했다. 카메라 없이 모델을 재는 유일한 길이므로 남겨 뒀다.
5. **한글은 안 된다.** 라틴 3종만이고, 원 위에 앉히는 기하는 서체를 가리지 않으니
   한글 서체를 물리면 그 자체로는 돌 것이다. 다만 정방형 자형이 원둘레에서 어떻게
   읽히는지는 별개 문제고, 조선일보 견고딕은 tool 03의 폴더에 있다.
6. **줄마다 크기는 아직 못 준다.** `Size` + `Step`(taper)이므로 크기는 안쪽으로
   **단조롭게** 줄어든다. 레퍼런스는 그렇지 않다 — 제목 크게, 상세 작게, 날짜 다시
   중간이다. 각도는 배열로 갈아탔으니 크기도 같이 갈아탈 수 있지만, 그러면 `Step`이
   무의미해지고 패널이 줄마다 두 행이 된다. **흔한 경우(위계가 한 방향으로 내려간다)를
   숫자 하나로 처리하는 쪽**을 골랐다. 필요해지면 `sizes: number[]`가 `angles`와 같은
   모양으로 들어간다.
7. **긴 줄은 자기 고리를 감는다.** `natural`에서 문구가 원둘레보다 길면 겹친다.
   `arcRun`이 `laps`로 알려 주는데 **스테이지가 아직 그 말을 하지 않는다** — 자리
   부족(`dropped`)만 말한다. 상태 줄에 한 줄 더 붙이면 된다.
8. **줄을 가운데에서 지우면 각도가 밀린다.** `angles`는 줄 번호로 인덱싱하므로,
   두 번째 줄을 지우면 세 번째 줄이 두 번째의 각도를 물려받는다. 텍스트 필드가 배열을
   같이 관리하게 하면 막을 수 있는데 그건 텍스트 필드가 두 번째 상태를 소유하는 일이고,
   실행 취소와 엮이면 더 나쁘다. **지금은 밀리는 쪽이 낫다고 판단했다** — 각도는 눈에
   보이는 값이고 다시 끌면 되니까.

#### 이 도구를 만들면서 공유로 꺼낸 것 셋

전부 "두 번째 사용자가 생겼다"는 신호였고, **둘은 이미 몰래 빌려 쓰고 있던
것이었다.**

- **`outlineData`** (`Seg[]` → 패스 문자열) → `media/type/measure.ts`.
  `Seg`가 거기 있으니 그것을 쓰는 방법도 거기 있어야 한다. tool 02는 자기 래퍼를
  유지한다 — 컨투어에는 "닫혀야 한다"는 규칙이 하나 더 있다.
- **`registerBitmap`** → `media/images.ts`. 카메라 프레임은 파일 임포터를 통과하지
  않지만 같은 레지스트리에 있어야 한다. **`releaseBitmap`은 만들지 않았다** —
  실행 취소 스택이 옛 프레임 id를 들고 있으니 안전하게 지울 수 있는 것이 없다.
  대신 캡처 크기에 상한(1600px)을 걸고 그 이유를 코드에 적었다.
- **`.stage-status`** → `app/app.css`. **세 도구가 쓰는데 선언이 tool 02의 시트에만
  있었다.** `main.tsx`가 모든 시트를 불러오기 때문에 우연히 돌고 있었을 뿐이다.

#### 발견했지만 **일부러 안 고친 것** — `.export-btn`

`shared/styles/controls.css`에 `.export-btn`이 있는데
`tools/plastic-type/tool.css`에 **덮어쓰는 사본**이 또 있다. 값이 다르다
(패딩 11 vs 10, 테두리 있음/없음, radius·margin-top·font-weight 없음). `main.tsx`가
tool 02의 시트를 나중에 불러오므로 **네 도구 전부의 Export 버튼 모양을 tool 02가
정하고 있다.**

지금 사이트는 그래서 오히려 일관돼 보인다. 사본을 지우면 **도구 01·03·04의 버튼
모양이 한꺼번에 바뀐다** — 이번 작업 범위가 아니고, 눈으로 골라야 하는 변경이라
손대지 않았다. 어느 쪽을 남길지 정하면 한 줄 작업이다.

이건 `CLAUDE.md`에 **버그 유형 9번**으로 올려 뒀다: 한 도구의 CSS를 다른 도구가
몰래 빌려 쓰는 것. 정리할 때 `var()` 대조와 같이 한 번 훑을 것.

#### 그 김에 고친 버그 하나 — tool 02의 상태 문구가 패널 위에 있었다

`.stage-status`를 옮기면서 나왔다. `.stage`에 `position`이 없어서 그 absolute가
**뷰포트**를 기준으로 잡히고 있었고, `left: 20px`는 296px 패널 안쪽이다. tool 03만
자기 `.billboard-stage`에 `position: relative`를 따로 갖고 있어서 멀쩡했다.
`.stage`에 `position: relative`를 넣었고(tool 04의 스테이지 UI도 그것이 필요하다),
tool 02에서 폰트에 없는 글자를 넣어 상태 문구를 띄워 실측했다 — 이제 스테이지
왼쪽 아래 18/20이다. tool 01은 `.stage-status`를 안 쓰고 스테이지 안에 absolute인
것이 없으므로 영향 없음(눈으로도 확인).

`overflow: hidden`도 `transition`도 배치 기준을 만들지 않는다. **absolute를 쓰는
쪽이 아니라 그 부모를 확인할 것.**


### 10. Tool 05 Type the Beat — 등록됨

레퍼런스는 `0827_DJ is my Type /01.png` 한 장. 원판 두 개 사이에 `ㅜ`가 낀 DJ 머신이고,
요청받은 소리는 둠 / 움 / 붐 / 둥 / 웅 / 붕 여섯이었다. 사용자 확인을 받은 결정 넷:
**시퀀서 + 데크**(한 음절 = 한 스텝, 한 줄 = 한 트랙, 데크는 커서 아래 음절의 편집기),
**모임꼴대로 재배치**("같은 구조 내에서 재미있는 조합들이 드러나야 해"), **일단 WAV만**,
이름은 **Type the Beat**.

#### 아이디어가 서는 근거 — 비유가 아니다

음절의 세 자리가 소리의 세 구간과 맞는다. 초성=어택(조음 위치가 타악기를 정한다),
중성=몸통(음정·밝기), 종성=릴리스(ㅁ은 멈추고 ㅇ은 울리고 ㄱ은 불파음이라 끊긴다).
그리고 **가획이 에너지다** — ㄱ→ㅋ은 획 하나와 공기 한 번이라 뚬은 둠보다 센 킥이고,
그래서 **강세가 이미 철자 안에 있으므로 트랙별 레벨 컨트롤이 없다.**

세 표 어느 것도 발명이 아니다: 초성은 **오음**으로 묶고, 중성은 단모음 여덟에서 글자가
만들어진 그대로 합성하고(ㅑ=ㅏ+획→두 번 침, ㅘ=ㅗ+ㅏ→글라이드), 종성은 **음절말 중화**
일곱이다(ㅅㅆㅈㅊㅌㅎ→[ㄷ], ㅋㄲ→[ㄱ], ㅍ→[ㅂ]). 겹받침은 자음군 단순화로 지워지는
글자를 **꼬리 안의 고스트 탭**으로 들려준다 — 값[갑]의 ㅅ이 그것이다.

**그리고 세 개의 휠이 곧 한글 입력기다.** 조합형 문자라서 회전 세 개로 11,172자를 다
칠 수 있다. 이게 이 도구가 한글로만 성립하는 이유이자 논지다. 자세한 것은
`README.md`의 05절.

#### Phase 0 — 프로토타입은 쓰고 지웠다

`proto-djtype.html` + `proto/`에 자모 패드 벤치를 먼저 만들었다(초성 19 · 중성 21 ·
종성 28을 눌러 듣고, `VoiceSpec` 숫자를 그 자리에서 보고, trim 다섯을 돌린다). 소리는
귀로 골라야 하는 것이므로 가장 큰 미지를 앞으로 당긴 것이다. 기하와 오디오는 정상
모듈에 두고 `proto/`에는 UI만 뒀다.

**채택되면서 지웠다** — 저장소 규칙 그대로. 그리고 이번에는 지울 근거가 하나 더
있었다: **도구 자체가 그 벤치보다 나은 벤치다.** 세 킷을 `all`로 두면 자모 51개를 다
눌러 들을 수 있고, 음절 안에서 들린다.

#### 공유로 꺼낸 것 셋 — 하나도 예상이 아니었다

- **한글 분해** → `shared/text/hangul.ts`. tool 02의 `geometry/`에 있었고 **기하가
  아니었다.** 스프루 쪽 절반(`splitText`·`PartUnit`·`RunnerUnit`)은 tool 02에 남겼다 —
  프레임은 한글의 사실이 아니다. 세 표(`CHO`/`JUNG`/`JONG`)를 export했다: 05는 림에
  올릴 전체 목록과 가획 계열을 알아야 한다. **11,172자 왕복 검사도 같이 옮겼다** —
  증명은 증명하는 함수 옆에 있어야 하고, 그러면서 표 자체의 검사 넷을 더 붙였다.
- **조선일보 견고딕** → `shared/media/type/hangul/`. 03과 05가 같은 3.9MB를 쓴다.
  **`faces.ts`에는 넣지 않았다** — 그 파일이 인덱스의 모듈 그래프에 있어서 `?url` 한
  줄이 3.9MB를 `dist`로 끌고 온다. 그게 애초에 이 폰트를 도구 폴더에 넣게 만든 사고이고
  옛 `face.ts`의 주석이 기록해 뒀다. **검사는 간단하다: `app/`의 어떤 파일도 이 모듈을
  import하지 않는다.** 기준 글자가 둘이 됐다 — `REFERENCE`(`한`)와 `JAMO_REFERENCE`(`ㅁ`).
  자모 하나에 `한`을 쓰면 Size가 말한 것보다 3분의 1 작아진다.
  `signFace.fixture.ts`는 **tool 03에 남겼다** — 아직 호출자가 그 도구의 검사뿐이다.
- **스테이지 위의 장치 칩** → `app/app.css`의 `.stage-deck`/`.stage-chip`. 04는 카메라를
  켜고 05는 트랜스포트를 켜는데 같은 물건이다. 04에 남겨 두고 빌려 쓰는 것이 정확히
  `.stage-status` 사고(버그 유형 9)이므로 **04의 마크업도 같이 고치고 눈으로 재확인했다.**
  `.magic-shutter`는 04에 남겼고(사용자 하나) — 옮기면서 그 버튼이 공유 칩의 상자를
  조용히 물려받고 있던 것이 드러나 이제 자기 상자를 자기가 적는다.

#### 측정이 잡은 버그 여섯

**소리는 스크린샷이 안 찍힌다.** 그래서 전부 `OfflineAudioContext`로 렌더해 숫자로 봤고,
그 방법이 아니었으면 셋은 못 찾았다.

1. **조음 위치가 귀에 도달하지 않았다.** `tint`가 파열음의 음정 하강만 움직여서
   ㅂ 0.027 · ㄱ 0.029 · ㄷ 0.027 — 셋이 7% 안에 붙어 있었다. 산술은 맞고 노드는 다
   돌고 결과는 **하나의 회색**이다(tool 02의 AO와 같은 실패). 지금은 `tint`가 버스트를
   **톤과 노이즈로 쪼갠다**: 0.095 · 0.340 · 0.453 · 0.526.
2. **밝기 필터에 통과시킬 재료가 없었다.** 55Hz 삼각파는 배음이 1/n²라 2kHz 위에
   아무것도 없다 — 여덟 모음이 0.048~0.056으로 평평했다. 톱니파 + **기음의 배수**
   컷오프로 0.43→0.84 단조.
3. **`centred`가 잉크 높이로만 맞추고 있었다.** ㅡ는 잉크 높이가 거의 없으니 26px를
   맞추려고 폭이 260px로 부풀어 원판을 가로지르는 회색 사선이 됐다. 자모는 **em 안에
   설계돼 있으므로** em으로 재고 잉크로 중심을 잡는다(`placed`).
4. **림이 21개에서 겹쳤다.** 옵션이 아니라 고장으로 읽힌다. 림 글자 크기는
   `min(요구값, 둘레/개수 × 0.82)`로 **파생**된다 — 컨트롤이 아니다. 불변식으로 고정했다.
5. **모임꼴 배치가 받침을 보고 있었다.** 받침이 없으면 종성 원판을 당겨 올렸는데, 모든
   간격값에서 중성과 겹쳤다. **겹치는 것보다 나쁜 이유로 틀렸다** — 세 번째 휠은 항상
   그려지므로, 받침이 생길 때 배치가 움직이면 나머지 둘이 손 아래에서 밀린다. 지금은
   모임꼴만 본다. **1,596조합 불변식 검사가 잡았다.**
6. **`onSpinEnd`가 React state를 읽고 있었다** — 그 규칙을 자기 주석에 인용해 놓은
   파일에서. 실제 손으로는 *돌아간다*(React가 pointermove 사이에 다시 그린다). 합성
   이벤트로 한 tick에 흘리면 클로저가 생성 당시의 `null`을 들고 있고 스핀이 아무데도
   안 떨어진다. **"실제로는 돌아간다"가 이 버그의 위장이다.** → `CLAUDE.md` 버그 유형 1.

그리고 **버그가 아니었던 것 둘.** 스윙과 템포가 안 듣는 것처럼 나왔는데, HMR이
`store.ts`를 다시 불러서 `window.__beatStore`와 화면의 컴포넌트가 **다른 인스턴스**를
쓰고 있었다. 하드 리로드 후 정확히 맞았다(홀수 스텝 82.5ms = 목표값, 187.5ms = bpm 160).
→ `CLAUDE.md` 버그 유형 14. 온셋 간격이 1~2ms로 나온 것도 제 검출기가 한 트랜지언트
안의 영점 교차를 세고 있던 것이었다(규칙 11).

#### 실측

| | |
|---|---|
| 스케줄 드리프트 | 23스텝 간격 오차 **0.000000ms**, 누적 **0ms** |
| 스윙 | 짝수 0ms · 홀수 82.5ms (목표 82.5) |
| 플레이헤드 | 2.5초 재생 중 `transform` 301회 · **`d` 변경 0회** · DOM 변경 0회 |
| WAV | 5.350초 = `loopLength`와 일치 · 피크 0.55 · 클리핑 0 · 온셋 격자 312.5ms 유지 |
| 자모 68조합 | 무음 0 · 클리핑 0 · 피크 0.22~0.55 |
| 모음 기음 | 55·62·73·82·98·110·123·147Hz — 표와 정확히 일치 |
| ㅘ 글라이드 | 66 → 94Hz (실제로 미끄러진다) |
| 인덱스 청크 | 543KB / gzip 167KB — tool 05의 식별자 **0회** |

#### 남은 것 / 안 한 것

1. **"좋게 들리는가"는 사람이 판정해야 한다.** 상수 표는 음성학에서 왔고 순서 관계는
   검사로 고정돼 있다. 톤이 마음에 안 들면 `audio/voice.ts`의 표 숫자만 고치면 되고,
   검사는 순서만 보므로 안 깨진다. **한 사람 귀에 맞춘 상수는 그 사람의 상수다.**
2. **악보 SVG는 이번 범위가 아니다.** 사용자가 "일단은 WAV만"이라고 정했다. 스테이지가
   SVG DOM이므로 나중에 `exportSvg({ filename })` 한 줄이고, `spec`은 지금 WAV만 말한다 —
   지킬 수 없는 약속을 적지 않는다.
3. **MIDI 없음.** 자모→노트 번호는 두 번째 계약이고 우리 신스와 어긋난다.
4. **트랙별 레벨 없음.** 강세가 글자 안에 있다(둠/뚬/툼)는 것이 이 도구의 답이다.
   필요해지면 tool 04의 `angles: number[]`와 같은 모양으로 줄당 한 행이 들어간다.
5. **세 중앙 글리프는 음절을 *넓게 벌려* 쓴다.** 모임꼴 자리에 놓이므로 읽히지만, 활자
   조판처럼 맞물리지는 않는다 — 원판이 서로 닿아야 하니 `Gap` 0에서도 글자 간격이
   글자 크기의 세 배쯤이다. 패널의 `Note`는 그 사실대로 적혀 있다("Gap은 얼마나 떨어져
   서는가"). 진짜로 맞물리게 하려면 원판을 겹쳐야 하고 그러면 기계가 아니게 된다.
6. **동시 발음 수 상한 없음.** 8트랙 × 겹받침 고스트면 최악 16보이스다. 지금 피크가
   0.55이므로 여유가 있고, 넘치면 상한을 걸고 **거는 순간 상태 줄이 말한다**(조용한
   상한 금지).
7. **`webkitAudioContext` 접두어를 안 붙였다.** 옛 사파리를 범위에서 뺀 결정이고,
   필요하면 한 줄이다.
8. **`.magic-stage`가 마크업에만 있고 선언이 없다.** tool 04의 것이고 이번에 대조표가
   드러냈다. 무해하지만(스타일 훅) 정리할 때 지울 후보다. `.disclosure-body`도 같다.


### 11. Tool 05 — 2차 패스: 다이얼식으로 바꾸고 다시 디자인했다

1차에서 사용자가 셋을 지적했다: **텍스트를 직접 입력하지 않는다**(비트메이킹처럼 디스크를
돌려 만든다), **디자인이 구리다**(레퍼런스 셋을 폴더에 넣어 뒀다), **일단 세로모임꼴만**
(가로·섞임은 더 고민해야 한다). 그리고 확인 질문에 **사운드 다이얼 → 스텝 탭**(808 방식)을
골랐다.

#### 텍스트 필드를 없앴다 — 그것이 이 도구를 악기로 만든 변경이다

1차의 시퀀스는 `textarea`였다. 한 줄이 한 트랙, 한 음절이 한 스텝, `·`가 쉼표, `-`가 타이.
이야기는 깔끔했고 — 의성어가 이미 리듬 표기법이다 — **악기가 아니었다.** 비트를 타이핑해서
만들게 되고, 원판은 커서 밑의 음절을 고치는 편집기로 내려앉는다.

지금은 문서가 **레인 배열**이다. `{ syllable, steps: boolean[] }`. 휠로 소리를 맞추고
(`dialed`), 격자에서 탭해 넣는다. 레인의 이름이 곧 그 음절인 것은 음절이 소리의 전부를
말하기 때문이고 — 초성=어택, 중성=몸통, 종성=릴리스 — 따로 채널 이름이 없다.

**레인은 놓을 때 생긴다.** 다이얼할 때 생기면 초성 열아홉 개를 훑고 지나간 자리에 빈 행이
열아홉 개 남는다. 그래서 아직 레인이 아닌 다이얼값은 격자 맨 아래 **고스트 행**이고, 그
행을 탭하면 레인이 된다. 마지막 히트를 지우면 레인이 사라진다 — 소리는 휠에 남아 있다.

**그래서 `Text` 그룹이 없어졌고 `Type`이 됐다.** 활자는 사라진 게 아니라 기계로 들어갔다:
읽기값, 레인 이름, 원판에 파낸 자모. `CLAUDE.md`의 "Text가 가장 큰 그룹" 규칙은 tool 04의
것으로 남고, tool 05은 **화면의 모든 글자가 일하는 자모**라는 더 강한 형태로 그 자리를
지킨다.

지운 것: `geometry/pattern.ts`(+검사), `geometry/score.ts`(+검사), `render/ScoreStrip.tsx`.
새로 쓴 것: `geometry/sequence.ts`, `geometry/grid.ts`, `render/Sequencer.tsx`.

#### 세로모임꼴만 — 재배열 코드를 지웠다

사용자가 처음 "가로모임꼴만"이라고 했다가 **"말을 잘못했어, 세로모임꼴만이야"**로 정정했다.
물어본 것이 맞았다 — 레퍼런스의 둥은 ㅜ이므로 두 답이 정반대 작업이었다.

중성 휠은 이제 `ㅗ ㅛ ㅜ ㅠ ㅡ` 다섯만 든다. `shapeOf`·`Shape`·`placesFor`의 가로/섞임
분기를 **지웠다** — 안 나가는 배치를 위한 죽은 코드는 존재하지 않는 도구의 로드맵과 같은
종류의 못 지킬 약속이다. **`audio/voice.ts`의 소리 표는 21개 중성을 그대로 다 갖고 있다**
(그건 음성학이고, `voice.test.ts`에 "데크가 못 만드는 모음도 여전히 안다"는 검사를 붙였다).

**비용은 하나: ㅣ가 안 되므로 칫이 못 들어온다.** 하이햇은 `츳`이다 — ㅊ 치음(노이즈뿐),
ㅡ 중립(쌓인다), ㅅ→[ㄷ] 불파음(끊긴다). 같은 악기 같은 길이다.

#### 디자인: 화면이 문서가 아니라 기계다

1차는 전부 1px 헤어라인이었다. 계기판 톤은 맞았고 **악기의 무게가 없었다** — 종이 위의
빈 원 세 개. 레퍼런스 셋(알람 다이얼 · Music OS의 릴과 원판 · 점 격자)이 정확히 반대다.

바꾼 것 다섯: **속이 찬 검은 원판에 표시를 파낸다** · **도형/바탕 반전을 딱 한 번**(림은
잉크 위 종이=선택지, 허브는 그 안 종이 위 잉크=고른 것) · **둘레 60개 가는 눈금 + 킷마다
긴 것 + 12시 액센트 점** · **한 단 낮은 둥근 사각 패널**(반경 22) · **가장 큰 활자는
읽기값**(손에 든 음절, 레퍼런스가 큰 숫자를 두는 자리).

`CLAUDE.md` §2에 **"악기는 문서와 다른 무게를 갖는다"**로 올렸고, 동시에 **사이트 전역
언어로 올리지 않았다**고 못박았다 — tool 01의 캔버스에 속이 찬 검은 원판을 두면 그것이
작품이 된다. 화면에 기계를 그리는 도구만 이 등록부를 쓴다.

#### 2차에서 잡은 버그 다섯

1. **드래그 중심이 그림과 달랐다.** `onSpinStart`가 중심을 다시 계산하면서 `deck.box.y`를
   빼먹어 **90px 옆의 점**을 기준으로 각도를 재고 있었다. 그림은 정확했고 휠만 반응하지
   않았다 — **그림이 맞는다는 것이 위장이다.** 이제 오프셋(`deckDX`/`deckDY`)을 한 곳에서
   파생시켜 그림과 제스처가 같이 쓰고, 그 계산을 핸들러보다 **위에** 뒀다. → 버그 유형 목록의
   검증 항목으로 올렸다.
2. **실행취소 이력이 스테이지 크기를 들고 다녔다.** Reset이 기본값 1200×800을 578px 스테이지에
   되돌려 놓고 `preserveAspectRatio`가 **악기 전체를 48%로** 줄였다. `ResizeObserver`는
   못 잡는다 — 아무것도 리사이즈되지 않았다. `undo`·`redo`·`reset`이 나가는 길에 살아 있는
   크기를 다시 얹는다(`sized`). → **`CLAUDE.md` 버그 유형 15.**
   **tool 01·02·04에 같은 구멍이 있다.** 구조가 같고 Reset이 같은 기본값을 되돌린다.
   이번 범위가 아니라 안 고쳤고, 도구마다 세 줄이다.
3. **드래그면이 림 글자를 덮고 있었다.** 맨 위에 그렸으니 히트 테스트를 항상 이기고,
   `stopPropagation`은 애초에 도달하지 않은 이벤트를 살릴 수 없다. 원판 위·글자 아래로 옮겼다.
4. **큰 활자를 중심으로 앉히면 왼쪽 변이 흔들린다.** 모음을 돌릴 때마다 읽기값이 덜컹거리는
   것으로 보인다. 그려진 폭에서 계산해 패널 여백에 맞춘다(`drawnSize`).
5. **읽기값이 메타 줄을 덮었다.** 132px 활자는 코너를 비워 주지 않는다. 겹치지 않게 쌓았다.

그리고 **버그가 아니었던 것 하나 더**: `reset()` 직후에 MutationObserver를 걸어 재니 React의
재렌더가 창 안에 들어와 `path.d` 56회로 나왔다. 문서가 가라앉은 뒤 다시 재니 `d` 변경 0회다.
규칙 11을 또 어긴 것이고, 이 저장소에서 세 번째다.

#### 2차 실측

| | |
|---|---|
| 스케줄 드리프트 | 26스텝(1/16, 96bpm, 156.25ms) 간격 오차 **0.000000ms**, 누적 **0ms** |
| 플레이헤드 | 3초 재생 · 패스 255개 중 `g.transform` 361회 · **`d` 변경 0회** · DOM 변경 0회 |
| WAV | 10.350초 = `loopLength`와 일치 · 피크 0.55 · 클리핑 0 |
| 스핀 | 붐 → 움 → 음 → 으 (초성·중성·종성 각각), **바는 그대로** |
| 탭 | 고스트 행 탭 → 레인 생성 · 되탭 → 레인 삭제 · 기존 레인 → 토글 · 실행취소 복원 |
| 인덱스 청크 | 543KB / gzip 167KB — tool 05의 식별자 **0회** |

#### 2차에서 남은 것

1. **가로·섞임 모임꼴.** 사용자가 "더 고민해봐야겠어"로 미뤘다. 소리는 이미 있고, 배치
   코드는 지웠다 — 돌아올 때 `deck.ts`에 배치 함수 하나를 다시 넣는 일이다.
2. **라이브 녹음(REC) 없음.** 다이얼 → 탭만이다. 물어봤을 때 사용자가 탭 방식을 골랐다.
3. **좁은 창에서 16스텝이 촘촘하다.** 스텝 폭에 하한(10px)이 있어 깨지지는 않지만
   막대가 8px가 된다. 창이 넓으면 제대로 벌어진다. 대지 경계가 없다는 규칙의 결과다.
4. **"좋게 들리는가"는 여전히 사람이 판정해야 한다.** 1차와 같다 — `audio/voice.ts`의 표
   숫자만 고치면 되고 검사는 순서만 본다.

### 12. **README.md를 한 번 유실했다 — 부분 복원본이다**

**먼저 알아야 할 것.** 2026-08-28, 문서 갱신 스크립트가 `README.md`를 덮어썼다.
파이썬으로

```python
old = s[s.index("## 화면은 문서가 아니라 기계다") : s.index("## 현재 범위")]
s = s.replace(old, new)
```

를 돌렸는데 **`"## 현재 범위"`가 01·02·04장에 먼저 나온다.** `str.index`는 첫 번째를
찾으므로 슬라이스가 역방향이 되어 `old`가 **빈 문자열**이 되고, `replace("", new)`가
모든 문자 사이에 `new`를 끼워 넣어 84KB 파일이 26MB가 됐다. 원본은 사라졌다 —
저장소가 git이 아니고, APFS 로컬 스냅샷도 없고, 편집기 백업도 없었다.

**복원한 것:** 형제 폴더 `~/Documents/GitHub/260815_Test/README.md`(2026-08-20
시점 — 당시 작업 폴더의 형제였다)에서 01장 ·
02장(평면) · 공통 구조장을 되살리고, 공통 구조장은 코드를 보고 현재로 갱신했다.
05장은 이번 세션에서 직접 쓴 것이므로 다시 썼다.

**되살리지 못한 것 — 그리고 지어 넣지 않은 것:**

| 무엇 | 어디에 살아 있나 | 상태 |
|---|---|---|
| tool 02의 **입체 형태** 절 (약 150줄) | 이 파일 **4번** | **2026-08-29 복원됨** |
| tool 04 **전체 장** (약 230줄) | 이 파일 **9번** | 아직 없다 |

README의 그 자리에 「유실된 자리」라는 절을 두고 위 표를 박아 뒀었다. **옮겨 쓰는 것과
새로 짓는 것의 경계가 흐려지는 작업이라 표시만 남겼다** — 되살리고 싶으면
`HANDOVER.md`의 4번·9번을 README의 문체로 옮기는 일이고, 내용은 다 있다.

**tool 02의 절은 2026-08-29에 사용자 요청으로 복원했다.** 옮겨 쓰는 것과 새로 짓는
것의 경계 문제가 이 절에는 없었다 — **그 글을 쓴 세션이 아직 열려 있어서 원문이
문맥에 그대로 있었다.** 지어 넣은 문장은 없고, 그 뒤에 바뀐 사실 하나(기본값이 입체가
된 것)만 갱신했다. tool 04는 그 조건이 아니므로 여전히 표시만 남아 있고, README의
그 절은 이제 「03 · 04 — 유실된 자리」다.

**교훈 셋.**

1. **`str.index` 두 개로 슬라이스를 만들지 말 것.** 두 번째 앵커가 첫 번째보다 앞에
   있으면 조용히 빈 문자열이 되고, `replace("", x)`는 파일을 폭발시킨다. 잘라낼
   구간이 필요하면 **앵커의 출현 횟수를 먼저 세고 1인지 확인**하고, `s.index(b, i)`
   처럼 시작 위치를 주고 찾을 것. 이번 복원 스크립트는 그렇게 썼다(`once()` 헬퍼가
   횟수를 검사한다).
2. **이 저장소는 git이 아니다.** 되돌릴 것이 아무것도 없다. 큰 문서를 스크립트로
   고치기 전에 `cp README.md /tmp/`를 한 줄 하는 것이 이 전체 사고의 비용보다 싸다.
3. **`CLAUDE.md`와 `HANDOVER.md`가 살아남은 것이 이번의 유일한 운이 아니다** —
   프로젝트의 근거를 세 파일에 겹쳐서 적어 두는 관례가 실제로 작동했다. tool 03의
   장이 README에 없고 HANDOVER에만 있던 것도 결과적으로 그것을 지켰다.


### 13. Tool 05 — 3차 패스: 이름을 바꾸고 기계를 다시 만들었다

사용자가 이미지 한 장(목업)과 두 마디를 줬다: **"디자인을 이 이미지처럼, 좀 더
직관적으로 세로모임꼴의 글자 조합이 보이도록"**, 그리고 **"툴 이름을 Type the Beat로"**.

#### 이름

`this-dj-is-my-type` → **`type-the-beat`**. 폴더 · id · export 이름 · 워드마크
(`TYPE THE` / `BEAT`) · 내보내는 파일명 · DEV 훅(`__beatStore`/`__beatAudio`) · CSS
클래스 접두어까지 전부. 라우트가 바뀌므로 옛 URL은 죽는다 — 아직 배포처가 없어서
문제가 아니다.

#### 목업에서 읽은 것 — 취향이 아니라 구조였다

1. **원판이 밝고 글자가 잉크다.** 2차의 파낸(knock-out) 검은 원판과 반대. 무게는
   생겼었고 활자가 묻혔었다.
2. **글자가 기울지 않는다.** 원판의 자리를 타고 돌지만 똑바로 선다.
3. **읽는 자리가 마주본다** — 위 휠은 **6시**, 아래 휠은 **12시**. 목업의 글자
   y좌표를 재서 알았다: 위 원판의 아래쪽 글자(478) · 띠(625) · 아래 원판의 위쪽
   글자(783)가 **147과 158로 거의 등간격**이었다. 그게 "직관적으로 조합이 보이도록"의
   답이다 — **기계가 자기가 연주할 음절을 쓴다.**
4. **중성이 세 번째 원판이 아니라 가로 눈금 띠다.** 두 원판 사이의 틈에 정확히 들어
   가고, 그 틈이 세로모임꼴이 모음을 두는 자리다.
5. 아래의 주황 원 하나 — **오디션 패드**로 읽었다(손에 든 소리를 듣는 것. 기계 위에서
   스스로 소리를 내는 유일한 것이라 유일하게 채워진 액센트다).

**목업은 세로 비율이라 패널이 위아래로 쌓여 있다.** 브라우저 창은 가로이므로 데크
왼쪽 / 바 오른쪽을 유지했다 — 목업 자체의 패널 폭 비율(490 : 715)이 가로로 놓으면
그대로 맞는다.

#### 그래서 없어진 것: 132px 읽기값

2차에는 바 패널 왼쪽 위에 손에 든 음절을 132px로 두고 있었다. 읽는 자리를 마주보게
하자 **기계가 그 말을 스스로 하므로** 같은 말을 두 번 하는 것이 됐다. `display` 필드와
`Type` 그룹의 `Display` 행이 같이 없어지고, `letter`(기계의 글자)와 `lane`(바의 이름)
둘만 남았다.

#### 3차에서 잡은 것

1. **간격의 하한이 산술이라는 것을 뒤늦게 알았다.** 슬라이더가 두 원판 사이에 들어가야
   하므로 `spacing`은 `r - rimRadius + rulerH/2 + 여유` 아래로 못 간다. 처음엔 여유를
   `0.06r`로 잡았고, 그러자 **각 휠의 액센트 점 두 개가 슬라이더 아래에 깔려** 어느
   쪽에서 읽는지 말해 주는 유일한 표시가 안 보였다. 실측(점 y 115.9/201.3 대 띠
   119.6~197.6)으로 확인하고 `0.3r`로 올렸다.
2. **스텝 폭의 하한 10px이 넘침의 원인이었다.** 가진 폭이 지킬 수 없는 하한은 하한이
   아니다 — 좁은 창에서 격자가 패널 오른쪽 변을 뚫고 나갔다. 4px로 내렸다.
3. **카드의 y값 두 개가 기하와 어긋났다.** 오디션 패드가 아래 원판 밑으로 나오는데
   `BAR_TOP`이 하드코딩이라 바의 첫 레인을 뚫고 그렸다. 이제 `DECK.box.height`에서
   파생시킨다 — 기하 모듈과 합의해야 하는 숫자를 손으로 적으면 반드시 갈라진다.
4. **슬라이더 순서가 이제 뜻을 갖는다.** 목록은 고리와 달리 방향이 있어서
   `ㅜ ㅠ ㅗ ㅛ ㅡ` = 음정 순서로 뒀다. **그 순서를 `voice.ts`가 내는 실제 음정에 대고
   검사한다** — 목록에 대고 검사하면 순환 논증이다.

#### 3차 실측

| | |
|---|---|
| 스케줄 드리프트 | 1/16 · 96bpm(156.25ms)에서 간격 오차 **0.000000ms** |
| 플레이헤드 | 3초 재생 · `g.transform` 313회 · **`d` 변경 0회** · DOM 변경 0회 |
| 슬라이더 | 한 피치 드래그 → 붐 → 봄 (ㅜ→ㅗ, 음정 축을 따라) |
| 액센트 점 | y 115.9 / 201.3, 띠 119.6~197.6 — **둘 다 띠 밖** |
| 탭 | 토글 · 실행취소 복원 · 고스트 행 생성/삭제 |
| 검사 | 326개 |

#### 3차에서 남은 것

1. **가로·섞임 모임꼴.** 여전히 미뤄져 있다. 소리는 이미 있고 배치 코드는 지웠다.
2. **좁은 창에서 데크가 폭을 많이 먹는다.** 데크 패널이 `2.65r + 52`이므로 578px
   스테이지에서 바에 220px만 남는다(스텝 5px). 창이 넓으면 제대로 벌어진다. 세로로
   쌓는 반응형 분기를 넣을 수도 있는데, 8레인 높이를 위아래로 쌓으면 세로가 모자란다.
3. **오디션 패드에 레이블이 없다.** 목업에 없었고, 눌러 보면 알 수 있는 종류라
   그대로 뒀다.


### 14. Tool 05 — 4차 패스: 평평한 것을 부품으로 만들었다

사용자 피드백 셋: **홀자에 입체감을**(레퍼런스: 널링된 엄지 휠), **닿자가 실제
턴테이블/레코드처럼 회전하도록**(레퍼런스: 자기 스크린샷 위에 바큇살과 기운 글자를
그려 보냄), **격자도 너무 평평하니 전체 톤을 보고 디테일업**.

#### 회전을 말하는 것은 림이 아니라 바큇살이었다

3차에서 글자를 전부 똑바로 세웠다. "기울이면 ㅡ와 ㅜ를 못 읽는다"가 이유였고 읽기는
실제로 좋아졌는데, **바퀴가 바퀴로 안 읽혔다** — 도는 것이 아무것도 없고 글자 셋이
자리만 옮긴다. 사용자가 보낸 두 장(정지/회전)을 비교하니 답이 거기 있었다: **글자가
같이 기울고, 바큇살이 같이 돈다.**

코드로는 **위치와 회전이 다른 각도를 쓴다.** 위치는 `read + spin + i*step`(읽는
자리에서), 회전은 `spin + i*step`(선택에서). 그래서 선택된 글자만 똑바로 서고 나머지는
얼마나 멀리 돌아가 있는지만큼 기운다 — 레코드 라벨의 정의다. `rimAt`이 `angle`을 다시
돌려주고, `spinOf`가 면 전체의 회전을 준다.

**그리고 바큇살 넷이 진짜 신호다.** 림 눈금이 몇 도 움직이는 것은 안 보이고, 방사선이
면 전체를 쓸고 지나가는 것은 보인다. 고리(가는 눈금 60 + 사분점 넷)는 **안 돈다** —
도는 면과 고정된 눈금자의 대비가 계기의 읽기 전부다.

#### 홀자: 자에서 배럴로

평평한 눈금 띠는 컨트롤의 **도면**으로 읽혔다. 지금은 옆으로 누운 원통이다 — 널링,
위아래로 어두워지는 곡면, 가운데 위쪽의 고정된 광택 띠, 양 끝이 곡면으로 사라지는
그라디언트. 그리고 **글자가 가운데를 벗어날수록 가로로 눌린다**(원통 뒤쪽은 비스듬히
보인다). 흐려지는 것보다 그쪽이 "감겨 있다"를 더 말한다.

**능선은 아주 흐려야 한다.** 처음에 불투명도 0.34로 그었더니 글자 둘이 얹힌 바코드가
됐다 — 밝은 면 위에서 능선은 늘 이긴다. 0.17.

#### 음영은 색이 아니라 덮개다 — `render/Surfaces.tsx`

모든 입체감이 `ink`와 지면색을 낮은 불투명도로 덮은 그라디언트다. **회색을 박아 넣으면
디자이너가 `Disc`를 검게 하는 순간 전부 무너진다.** 덮개는 아래가 무슨 색이든 같은
방향으로 작동한다. 그림자는 하나(`feDropShadow`)를 정의해 돌려 쓰고 짧다 — 긴 그림자는
부품을 패널 위에 띄운다.

`IDS` 상수는 `surfaceIds.ts`로 따로 뺐다. 컴포넌트와 상수를 같은 파일에서 export하면
fast refresh가 죽는다(`shared/`의 `editSessionContext.ts`와 같은 이유).

#### 격자: 파인 트랙과 비트 음영

스텝은 트랙 안에 선 키다 — 트랙은 자기 윗변 아래로 그늘이 지고, 히트는 자기 그림자를
가진 둥근 막대다. 그리고 **비트마다 한 칸씩 음영을 번갈아 준다**: 열여섯 개의 같은 칸은
셀 수 없고, 두 톤이 번갈면 3박이 어디서 시작하는지 한눈에 보인다. **비트 괘선은
지웠다** — 마디선과 음영이 같은 것을 말하고 있었고 한 질문에 두 개의 답이었다.

#### 또 걸린 것: 드래그면이 다시 글자 위로 올라갔다

3차에서 고쳤던 것을 4차에서 다시 만들었다. 배럴에 드래그 `rect`를 맨 마지막에
그렸더니 글자 탭이 전혀 안 닿는다 — `stopPropagation`은 애초에 도달하지 않은 이벤트를
살릴 수 없다. 지금은 **드래그면이 그림자 위·그리는 모든 것 아래**이고, 그 위의 장식은
전부 `pointer-events: none`이며 글자만 `auto`다. 원판의 스핀들도 같은 이유로 귀먹였다
(가운데를 잡아도 원판이 돌아야 한다).

**요소를 새로 얹을 때마다 히트 테스트 순서를 다시 볼 것.** 이 도구에서만 세 번째다.

#### 4차 실측

| | |
|---|---|
| 제스처 다섯 | 원판 드래그 붐→움 · 배럴 드래그 움→옴 · 림 글자 탭 옴→돔 · 스텝 탭 · 패드 |
| 프레임(필터 13개) | 중앙값 **8.3ms** · p90 8.6 · 최악 9.4 |
| 플레이헤드 | 3초 재생 · `g.transform` 359회 · **`d` 변경 0회** · DOM 변경 0회 |
| 드리프트 | **0.000000ms** |
| 검사 | 326개 · 인덱스 청크 543KB(gzip 167) |

### 15. Tool 05 — 5차 패스: 레퍼런스대로 되돌리고 자모를 그렸다

4차에서 입체감을 넣었더니 사용자가 이미지를 다시 보내며 **"디자인은 내가 보낸 이미지
그대로"** 라고 했다. 지적 넷: **입체감 과함**, **디스크와 자소의 비례가 큼**, **칸이
글자 수와 안 맞음**, **자모를 폰트가 아니라 기하로**.

#### 4차의 입체감은 과했다 — 전부 뺐다

그림자 · 방사형 조명 · 말린 테두리 · 널링된 배럴 · 광택 띠. 붙일 때는 하나씩 근거가
있었고 합치니 **렌더링된 버튼**이었다. 레퍼런스는 인쇄된 계기이고, 조명이 글자를
이기고 있었다.

남긴 것: 원판의 헤어라인 하나, 스핀들(구멍), 슬라이드 양 끝의 페이드, 스텝 트랙의
얕은 그늘. `Surfaces.tsx`가 그라디언트 여덟에서 **둘**로 줄었고 `feDropShadow`는 사라졌다.

**교훈:** "너무 플랫하다"의 반대는 "입체적으로"가 아니라 **"부품처럼"** 이다. 두 번을
왕복해서야 그 차이를 알았다.

#### 비례는 재서 가져왔다

레퍼런스 이미지에서 픽셀로 뽑았다 — 자모 **0.38r**(4차에는 0.57r), 스핀들 0.2r,
림 0.63r, 슬라이드 폭 = 원판 지름 × 1.36, 슬라이드 글자 = 원판 글자 × 1.2.

0.57r에서는 **원 안에 갇힌 글자 셋**이었다. 원판이 물건이고 글자가 그 위의 것이어야
한다.

#### 칸은 킷을 따른다 — 그리고 살을 두 번 긋고 있었다

`SPOKES = 4` 고정이었다. 자모가 셋인데 칸이 넷 — 그림이 자기가 그리는 것과 어긋나
있었다. 지금은 `rim.length`이고 각 살이 글자 사이를 이등분한다. 실측으로 확인:
킷 3/3 → 살 3/3, 6/8 → 6/8, 19/28 → 19/28.

**그리고 그 김에 버그 하나가 드러났다.** 살 하나를 그릴 때 `a`와 `a+180°` 양방향을
그리고 있었는데 루프가 한 바퀴를 다 돌았으므로 **모든 살이 두 번 그어지고 있었다.**
불투명도가 두 배라 흐리게 잡아도 진했다.

#### 자모를 그렸다 — `render/jamo.ts`

기계 위의 모든 자모가 한 굵기 · 진짜 원 · 직각으로 만들어진다. 시각 보정도 서체도
없다. **악보의 레인 이름은 여전히 견고딕**이고, 그 분리가 요점이다: 기계는 가공된 것,
기보는 조판된 것.

지름길이 아니다. 그려진 ㅇ은 완전한 고리이고 서체의 ㅇ은 아니다 — 옆이 얇고 위가
눌려 있고 다른 글자 옆에 서도록 그려져 있다. 턴테이블 위에서 네 가지 회전으로 놓이면
고리 쪽이 맞다. 그리고 획 구성이 훈민정음 제자 그대로라, 기하 작도가 현대 서체보다
글자의 출신에 가깝다.

**그 과정에서 잡은 것 셋:**

1. **`evenodd`가 글자를 갉아먹었다.** 한 자모는 여러 획으로 된 한 경로이고 획은
   교차한다(ㅂ의 세로가 허리를, ㅜ의 기둥이 바를). even-odd로 칠하니 **교차부마다
   구멍**이 뚫려 관절이 파인 글자가 나왔다. 지금은 모든 획을 같은 방향으로, 카운터만
   반대로 감고 `nonzero`로 칠한다. 검사는 눈이 아니라 **부호 있는 넓이**로 한다 —
   서브패스 넓이의 부호가 채울지 자를지를 정하는 유일한 값이다.
2. **사선이 박스를 넘었다.** 획은 자기 선에서 굵기의 절반만큼 법선으로 밀려 있으므로,
   꼭짓점을 모서리에 정확히 두면 *그려진 것*은 40단위 밖으로 나간다. ㅅ·ㅈ이 em을 넘어
   림에서 이웃을 침범했다. 지금은 꼭짓점을 안으로 당긴다. **검사가 잡았다**(모든 자모의
   모든 좌표가 0..EM 안).
3. **변환 문자열이 지수 표기를 냈다.** `sin(π)`는 1.2e-16이고 `1.2e-16`으로 찍힌다.
   SVG는 읽지만 사람도 순진한 파서도 두 개의 숫자로 읽는다 — 실제로 **제 테스트가
   먼저 걸려 넘어졌다.** 반올림해서 내보낸다.

겹자모는 **자기 변환을 가진 두 파트**로 돌려준다. 경로 문자열을 파싱해 숫자를 다시 쓰는
방법도 썼었는데, `A`는 좌표가 아닌 인자를 셋 갖고 있어서 호가 있는 글자를 누가 추가하는
날 조용히 깨진다.

#### 5차 실측

| | |
|---|---|
| 제스처 | 원판 드래그 붐→움 · 슬라이드 드래그 움→옴 · 림 글자 탭 옴→돔 · 스텝 탭 |
| 칸 = 킷 | 3/3 · 6/8 · 19/28 (세 킷 조합에서 일치) |
| 지수 표기 | 아트워크 전체 변환에서 **0회** |
| 검사 | 343개 (자모 16개 추가) |

#### 5차에서 남은 것

1. **자모 51자의 작도를 눈으로 검수하지 않았다.** 검사는 "그려진다 · 박스 안에 있다 ·
   감김이 맞다 · 가획이 획을 더한다"까지만 본다. **모양이 좋은지는 사람이 봐야 한다** —
   특히 ㅊ · ㅎ · ㄹ과 겹받침 열하나. `Deck`의 킷을 `All`로 두면 한 화면에 다 나온다.
2. **좁은 창에서 데크가 폭을 많이 먹는 것**은 그대로다.

### 16. Tool 05 — 6차 패스: 칸이 글자를 관통하고 있었다, 그리고 킷을 고정했다

사용자 지적 둘 + 지시 하나.

#### 칸이 자소와 겹쳤다 — 읽는 각만큼

살은 원판 자신의 회전(`spin + selected*step`)으로 돌리고 글자는 읽는 자리에서
(`read + spin + i*step`) 놓고 있었다. 둘이 **정확히 `read`만큼** 어긋나고, 네 항목짜리
휠에서는 그 어긋남이 정확히 한 칸이라 **모든 살이 글자를 정통으로 관통했다.**

지금은 `dividersOf(platter)`가 기하 모듈에 있고, 글자와 똑같이 계산해서 반 스텝만
더한다. 렌더러는 각도를 만들지 않고 받아 쓴다. **림에 맞춰야 하는 것은 림에서 계산할
것** — 이건 이 도구에서 "그림과 제스처가 같은 좌표를 쓰는지 확인하라"(4차)의 사촌이고,
이번엔 그림과 그림이 어긋났다.

검사 둘을 붙였다: 킷이 만들 수 있는 32음절 전부에서 (a) 각 살이 이웃한 두 글자의
정확히 중간이고 (b) 살과 글자의 최소 간격이 반 스텝보다 크다. 실측 최소 간격 **45.0°**.

#### 킷을 넷 · 둘 · 넷으로 고정했다

사용자 지시: 앞으로 **초성 ㄷ ㅂ ㅇ ㅊ · 중성 ㅡ ㅜ · 종성 ㅁ ㅂ ㅇ 공란**만 쓴다.

`ChoKit`/`JungKit`/`JongKit` 타입, `CHO_KITS`/`JUNG_KITS`/`JONG_KITS` 레코드, 문서의
세 필드, 패널의 세 `ChoiceRow`가 전부 사라지고 상수 배열 셋(`CHO_RIM`/`JUNG_RIM`/
`JONG_RIM`)이 됐다. **선택지가 하나뿐인 컨트롤은 컨트롤이 아니다**(규칙 4) — 그리고
"나중에 바꿀지 모르니 남긴다"는 존재하지 않는 도구의 로드맵과 같은 종류의 약속이다.

`rimFor`는 남겼다. 킷에 없는 자모가 든 문서가 오면 림 앞에 붙인다 — 지금은 도달할 수
없는 경로지만, 휠이 기계가 물고 있는 것과 다른 글자를 보여주는 쪽이 더 나쁘다.

**기본 문서의 하이햇이 바뀌었다.** 츳(ㅊㅡㅅ)의 ㅅ이 종성 킷에 없어서 로드하자마자
림이 자라던 참이었다 — `츱`(ㅊㅡㅂ)으로 옮겼다. ㅂ도 불파음이라 같은 악기 같은 길이다.
그 사고를 막는 검사를 붙였다: **기본 문서의 모든 자모가 킷 안에 있을 것.**

#### 자소 작도는 사용자가 넘겨주기로 했다

`render/jamo.ts`의 51자는 **자리를 지키는 것**이다. 사용자가 직접 제작해서 넘길
예정이므로 손대지 말 것 — 바뀌는 것은 `SHAPES`/`PAIRS` 두 테이블뿐이고, 나머지(감김
규칙 · 배치 행렬 · 캐시 · 검사)는 그대로 쓴다.

#### 6차 실측

| | |
|---|---|
| 살 ↔ 글자 최소 간격 | **45.0°** = 반 스텝, 32음절 전부에서 |
| 킷 | ㄷ ㅂ ㅇ ㅊ / ㅜ ㅡ / — ㅁ ㅂ ㅇ |
| 기본 레인 | 붐 · 둥 · 츱 (전부 킷 안) |
| 검사 | 347개 |

### 17. Tool 05 — 7차 패스: 소리가 전부 비슷하게 들렸다

사용자 피드백: **"실제 글자의 발음과 유사하지 않은 지점들이 있고, 닿자와 홀자 조합과
별개로 모든 소리가 비슷하게 들려."** 그리고 구체적으로 종성 ㅇ=울리는 장음 · ㅁ=닫히는
단음 · ㅂ=닫히고 끊기는 소리(붑!), 초성 ㅊ=짧고 높은 소리 · ㅇ=울리는 소리(웅웅~).

#### 먼저 재고, 그 다음에 고쳤다

렌더 버퍼로 열여섯 음절(초성 4 × 종성 4)을 재니 진단이 그대로 나왔다.

- **ㅁ과 ㅂ이 같은 길이였다** — 둠 67ms vs 둡 60ms. 7ms.
- **ㅊ에서는 받침이 안 들렸다** — 춤 167ms. ㅊ의 노이즈가 320ms짜리라 받침이 끝난 뒤에도
  계속 났다.
- **ㅇ 초성은 울리지 않고 둔탁했다** — 고역비 0.003, 피크도 가장 낮았다.

#### 근본 원인: 몸통이 초성과 무관했다

**몸통이 음절에서 가장 큰 소리인데 항상 같은 크기로 났다.** 츠와 으는 앞에 긁힌 자국만
다른 같은 톱니파다. 이게 "모든 소리가 비슷하게 들려"의 구조적 원인이었고, 어택을 아무리
다듬어도 안 고쳐지는 종류다.

`Attack`에 둘을 넣었다.

- **`voiced` (0..1)** — 이 음절이 얼마나 목소리인가. 믹서 설정이 아니라 입이 하는 일이다:
  ㅇ은 모음의 자리일 뿐이라 1.0, ㅅ은 성대가 거의 안 붙은 마찰이라 0.13, 파열음은 터진
  **다음에** 모음이 오므로 0.5~0.62.
- **`onset`** — 몸통이 들어오는 시간. 파열이 있으면 즉시(그 파열이 곧 onset), 없으면
  부풀어 오른다(ㅇ 55ms). **그게 웅이 울리느냐 퍽 하느냐를 가른다.**

그리고 `transient`의 `hum` 분기를 지웠다 — 비음의 murmur는 이제 몸통의 스웰이고, 둘 다
있으면 ㅁ·ㄴ이 두 번 난다.

#### 종성: 길이 말고 닫히는 자리

`Release`에 **`close`**를 넣었다. ㅁ은 양순이라 맨 앞에서 막히니 울릴 것이 남지 않아
꼬리의 필터가 닫히고(0.8), ㅇ은 연구개라 그 앞쪽이 통째로 관이라 열린 채 남는다(0.05).

그리고 **불파음에 클로저 텀프**를 붙였다. 조음 기관이 맞닿는 소리이고, 그게 붑!의
느낌표다. 처음에 45ms를 울리게 했더니 **붑이 붐보다 길어졌다**(115 vs 88ms) — 감쇠가
들리는 클로저는 클로저가 아니다. 18ms로 줄이고 불파 꼬리도 0.085→0.06으로 당겼다.

#### 마찰 길이를 조음 방식별로 갈랐다

`punch * (2 + air*6)` 하나로 계산하다 보니 격음 파찰음이 **320ms**가 나왔다. 지금은
파열 `punch*1.6` · 파찰 `0.05+air*0.045` · 마찰 `0.06+air*0.07` · 후음은 꼬리를 따른다.

#### 7차 실측

들리는 길이(ms), 받침만 바꿔서:

| 초성 | 공란 | ㅇ | ㅁ | ㅂ |
|---|---|---|---|---|
| ㄷ | 376 | 339 | 86 | **74** |
| ㅂ | 394 | 340 | 88 | **73** |
| ㅇ | 431 | 376 | 108 | **74** |
| ㅊ | 322 | 303 | 88 | **82** |

**ㅂ < ㅁ < ㅇ < 공란**이 네 초성 전부에서 선다. 춤 167→88ms.

어택 40ms 창의 고역비: 움 0.002 · 붐 0.077 · 둠 0.521 · 춤 **3.05**(40배).
음절 전체 목소리/노이즈 비: 웅 0.005 → 충 0.988(**200배**).
한 마디 렌더 10.6초 · 피크 0.55 · 클리핑 0.

#### 검사는 숫자가 아니라 관계를 본다

여섯을 더했다(총 353개). `voiced`·`onset`이 초성만의 것이고 종성이 안 건드린다 ·
방식별 목소리 서열(ㅇ>ㅁ>ㅂ>ㅊ>ㅅ) · 파열이 없는 초성만 부푼다 · 노이즈가 많을수록
목소리가 적다 · `close`가 종성만의 것이고 ㅁ>ㄴ>ㅇ이다 · 그리고 **길이 사다리에 비율
하한을 걸었다**(ㅁ/ㅂ > 1.8배, ㅇ/ㅁ > 2.5배) — 순서만 보면 7ms 차이도 통과하는데,
그게 정확히 이번에 사용자가 지적한 상태였다.

#### 7차에서 남은 것

1. **여전히 사람이 들어야 한다.** 검사는 관계만 보고, 상수는 음성학에서 왔다. 톤이
   마음에 안 들면 `audio/voice.ts`의 표 숫자만 고치면 되고 검사는 안 깨진다.
2. **중성이 둘뿐이라 음정 폭이 좁다**(ㅜ 55Hz · ㅡ 73Hz). 킷이 그렇게 정해졌으므로
   그대로 뒀다. 넓히려면 `VOWEL` 표의 두 값이다.

---

### 18. Tool 05 — 8차 패스: TTS를 **샘플이 아니라 계측기**로 썼다

**요청:** "여전히 소리가 맘에 들지 않아. tts같은 것에서 소스를 따와, 실제 음성과
비교해서 만들어볼 수 있어?"

**샘플을 쓰지는 않았다.** 녹음을 재생하면 자모가 라벨이 되고, 가획(둠/뚬/툼)도
`Voice`의 다섯 트림도 손댈 것이 없어진다 — 이 도구가 드럼머신이 아닌 이유가 통째로
사라진다. 대신 macOS `say`(Yuna)로 킷의 16음절을 받아 **자로 썼다.** 합성음과 같은
파이썬 계측(피크·RMS·밴드 에너지·자기상관·엔벨로프 온셋)으로 재고, **절대값이 아니라
비율**을 맞췄다. 튜닝 상수가 아니라 관계를 옮긴 것이다.

`scratchpad/tts/`에 WAV 32개와 `analyse.py`·`tts.json`이 있다. 저장소에는 안 넣었다 —
자산이 아니라 이번 측정의 기록이다.

**측정이 알려준 것 넷, 전부 추측으로는 못 나왔다.**

1. **몸통이 초성마다 다른 *시점*에 시작한다.** 실제 피크까지: 우 20ms · 붐/둠 70ms ·
   추 105ms. 나는 그것을 세기의 차이로만 모델링하고 있었다. 파찰음은 마찰이 먼저 나고
   모음이 **나중에** 온다. `Attack`에 `delay`가 생겼고(`BURST` 18ms · `AFFRICATE` 45ms ·
   `HISS` 55ms · 비음과 ㅇ은 0), `body()`가 `at + delay`에서 시작한다.
   **`voiceLength`도 그것을 알아야 한다** — 모르면 렌더된 추가 55ms 짧게 잘린다.
2. **파열음끼리는 훨씬 가깝고, 파찰음은 훨씬 멀다.** 실제 ㄷ/ㅂ은 1.8배인데 내 것은
   6.8배였고, 실제 ㅊ/ㄷ은 23배인데 내 것은 6배였다. `tint`가 히스로 가는 매핑을
   눌러(`0.32 + tint*0.55`) 지금 ㅊ/ㅂ이 **41배** — 실제와 같은 값이다.
3. **공명음은 다 같이 유성이다.** 녹음의 자기상관은 ㄷ·ㅂ·ㅇ이 전부 ~1.0이고 ㅊ만
   0.39~0.65로 떨어진다. 파열음마다 몸통을 반으로 줄이던 것은 음성학처럼 보이는
   **믹서 결정**이었다.
4. **포먼트는 절대 주파수다.** 이게 제일 컸다. 컷오프를 기음의 배수로 잡아 뒀는데,
   55Hz 킥에서 그건 157Hz다 — 톱니파를 자기 기음까지 깎으면 사인파다. 녹음의 고역
   함량이 내 것의 열 배로 나와서 잡았다. 같은 ㅜ가 높이 불러도 낮게 불러도 같은 ㅜ인
   것이 **포먼트가 절대적이기 때문**이고, 나는 물리를 거꾸로 알고 있었다. 지금은
   `max(pitch*2.4, 230 + open*1700)` — 절대값이되 기음 아래로는 안 내려간다.
   결과: 꼬리 밝기 0.009~0.016 → 0.045~0.094(실측 0.06~0.18), 그리고 **ㅜ 0.077 대
   ㅡ 0.242로 모음이 3배 갈라졌다.**

**그리고 표에 앞뒤가 안 맞는 줄이 하나 있었다.** 공란의 `close`가 0.15, ㅇ이 0.05 —
*아무것도 닫지 않는* 음절이 ㅇ보다 더 닫히는 것으로 적혀 있었다. 얼굴만 봐도 틀렸고
녹음도 그렇게 말했다(실제 부는 붕의 두 배로 밝다). 검사가 `c('') < c('ㄴ')`만 보고
있어서 통과하고 있었다 — 지금은 **모든 종성보다 작다**를 본다.

**닫힘은 페이드가 아니라 사건이다.** 필터를 꼬리 전체에 걸쳐 내리고 있었는데, 그건
모음이 시작하는 순간부터 입이 닫히기 시작한다는 뜻이다. 모음이 앞을 열어 두고 닫힘이
뒤를 가져간다 — 닫을 것이 많을수록 일찍 시작한다.

**남은 어긋남 하나는 안 쫓았다.** 60~150ms 창에서 부 0.068 · 붐 0.082 · 붕 0.086으로,
실제(붐 0.06 < 붕 0.09 < 부 0.17)와 공란의 자리가 다르다. 20% 차이이고, 그 옆에서
길이가 70 · 155 · 410 · 465ms로 갈라져 있다. **소리가 아니라 계측을 튜닝하기
시작하는 자리**라 멈췄다.

**측정 방법에서 배운 것:** 창을 길이의 백분율로 잡으면 길이가 다른 소리끼리 비교가
안 된다. 처음에 꼬리 밝기가 뒤집혀 보인 것이 그 때문이었고(공란은 720ms의 45%,
ㅁ은 220ms의 45% — 서로 다른 시점을 보고 있었다), **같은 절대 구간**으로 다시 재서야
표의 진짜 버그가 드러났다.

**실측 결과:** 킷 16음절 전부 살아 있고(피크 0.362~0.548) 클리핑 0 · 무음 0.
어택 밝기가 ㅇ 0.01 → ㅂ 0.21 → ㄷ 0.72 → ㅊ 8.6~9.6으로 **1000배** 벌어졌다.
검사 354개 통과, 인덱스 청크 542.88KB(gzip 166.60)로 변화 없음, 스케줄 드리프트
26스텝에서 0.000000ms.

**바뀐 파일:** `audio/voice.ts`(`delay` 필드 · `BURST`/`NASAL`/`HISS`/`AFFRICATE` ·
`RELEASE`의 `close` 열) · `audio/engine.ts`(`body()`의 시작 시각 · 포먼트 컷오프 ·
닫힘 타이밍 · 트랜지언트 대 몸통 비율 · `voiceLength`) · `audio/voice.test.ts`(불변식
셋을 실측에 맞게 다시 씀 — 옛 검사 둘은 옛 모델을 서술하고 있었다).

### 19. Tool 05 — 9차 패스: 레인 페이더, 그리고 사용자가 그린 자모

**요청 둘.** "각 트랙별로 볼륨을 줄였다가 올릴 수 있는 인디케이터를 만들어줘." ·
"자소의 디자인 svg데이터를 폴더에 넣었어. 획 두께를 맞춰두었으니, 크기 비례를 깨트리지
않고 적용해."

#### 페이더 — 문서에 적어 둔 것이 틀렸다

`CLAUDE.md`와 `README.md`가 오랫동안 **"가획이 에너지이므로 믹서 페이더가 없다"**고
말하고 있었다. 그건 두 가지를 하나로 본 것이다. 가획은 *한 소리가 얼마나 센가*이고,
레인 페이더는 *한 줄이 다른 줄들 사이 어디에 앉는가*다. 앞은 글자의 사실이라 여전히
컨트롤이 아니고, 뒤는 한 마디를 짜는 사람의 결정이다. **절반으로 내린 레인은 같은 뚬을
작게 낼 뿐이고, 페이더가 못 하는 일이 정확히 뚬을 둠으로 만드는 것이다.** 세 문서를
그렇게 고쳤다 — 지키지 못하게 된 문장을 남겨 두는 것이 이 저장소에서 가장 비싼 종류의
거짓말이다.

**모양은 세워 놓은 스텝 키다**(기계 등록부 11번). 같은 파인 슬롯, 같은 키. 스텝 키가
*이 레인이 여기서 난다*고 말하고 페이더 키가 *이 레인이 이만큼 난다*고 말하는데, 같은
종류의 사실 둘을 노브와 키로 나눠 그리면 한 격자에 두 언어가 생긴다.

**값은 캡의 위치가 아니라 채워진 양으로 읽는다.** 첫 판은 캡만 있었고, 34px 슬롯에서
그건 글자 옆의 짧은 대시였다 — 레인 셋이 다른 값이면 흩어진 자국 셋으로 보였고 어느
것도 값으로 안 읽혔다. 슬롯이 차오르면 여러 레인을 한눈에 훑을 수 있다.

**헤더 폭이 글자 크기의 배수가 아니게 됐다.** 셋 중 둘(× 마크와 페이더)은 활자 크기를
따라가지 않으므로, 순수한 비례는 작은 쪽 끝에서 셋을 같은 12px에 겹쳐 놓는다. 지금은
`max(lane*1.9, lane+42)`이고, Lane 16·32·64 전부에서 겹침이 없는 것을 렌더된 좌표로
확인했다(버그 유형 10 — 활자 레이아웃은 계산으로 끝내지 말 것).

**동작:** 드래그로 값, 탭으로 음소거·복구(직전 값을 기억한다). 0인 레인은
`hitsAt`에서 **아예 빠진다** — 0으로 우는 오실레이터가 아니라 없는 음이고, 렌더 피크가
정확히 0으로 나온다. `isEmpty`는 그래도 비어 있다고 하지 않는다: 음소거는 지우는 것이
아니고, 다 내려놨다는 이유로 Export가 거절하면 아무도 안 한 질문에 답하는 것이다.

**검증:** 합성 포인터로 드래그 전체를 **한 tick에** 흘려서 0.767→0.5→0.167→0으로
단조 하강하는 것을 확인했다(버그 유형 1이 걸리는 유일한 모양). 탭 음소거는
0.45→0→0.45. 레벨이 파일까지 가는지는 같은 마디를 두 번 렌더해서 봤다 — 피크
0.4345 / 0.2632 / 0(정확히), 길이는 셋 다 동일. 비율이 0.5가 아니라 0.606인 것은
버스에 웨이브셰이퍼가 있어서이고, 그게 맞다: 내리면 새추레이션도 같이 풀린다.
히스토리는 제스처당 한 번만 쌓인다.

#### 자모 — 여섯 개가 왔고, 규칙은 하나였다

`0827_DJ is my Type /design/SVG`에 여섯(ㅡ ㅜ ㅊ ㅂ ㅁ ㅇ). **ㄷ은 없다** — 같은
단위·같은 획 굵기로 대신 그려 두었고, 도착하면 바뀌는 것은 `ART`의 그 항목 하나다.

**"크기 비례를 깨트리지 말라"는 요청이 곧 구현 규칙이었다: 배율이 자모마다 다르면
안 된다.** 서로 다른 viewBox 크기가 곧 비례이기 때문이다 — ㅇ 55.47 대 ㅁ 50.64는
원형 오버슛, 가로획 8.07 대 세로획 9.0은 시각 보정, ㅡ·ㅜ의 54.8은 중성이라서. 각자를
자기 상자에 맞추면 그 숫자들이 전부 자기 자신으로 나뉘어 **한 가족이 여섯 개의 무관한
그림이 된다.** 그래서 배율은 ㅁ 하나에서 뽑아 전부에 같이 쓰고(`ART_UNIT`), 각 자모는
em 중심에 놓는다. 상자를 넘치는 것이 정상이다.

원본 경로는 **한 글자도 다시 쓰지 않았다.** `JamoPart.t`가 이미 있어서 좌표는 그대로
두고 변환만 얹는다 — `jamo.ts`가 예전에 "경로 안의 숫자를 명령별로 고치는 수술은
어느 날 누가 호를 추가하면 깨진다"고 적어 둔 그 이유다.

그리고 여기서 그리는 나머지 알파벳의 획 굵기를 0.2em에서 **9/50.64em으로 맞췄다** —
0.2는 맞출 것이 없던 시절의 어림수였고, 그대로 두면 킷 밖의 글자가 언젠가 옆자리
글자보다 굵게 선다.

#### 검사를 다시 썼다 — 그리고 진짜 버그가 하나 나왔다

일곱 개가 깨졌는데 **전부 "내가 어떻게 그렸는가"를 서술하는 검사**였다: `M`의 개수,
`A`의 유무, `t`가 없다는 것. 도면은 다른 프로그램에서 소문자 상대 명령과 3차 곡선으로
나왔으므로, 그런 검사는 글자가 아니라 **철자법**을 보고 있었다. 지킬 값이 있는 사실은
기하다 — 얼마나 큰가, 어느 쪽으로 감기는가. 그래서 경로 평탄화기(`render/pathgeom.ts`)를
쓰고 바운딩 박스와 부호 있는 면적으로 다시 물었다.

**그러자 ㅅ·ㅈ의 두 다리가 서로 반대로 감겨 있는 것이 나왔다.** `nonzero`에서 두 획이
만나는 꼭짓점의 쐐기가 서로 상쇄되어 **글자가 자기 뾰족한 끝에 홈이 파인 채로** 그려지고
있었다. 킷에 없어서 아무도 못 봤을 뿐이고, 하나라도 올리는 날 보였을 것이다. `slash`가
이제 자기 면적의 부호를 보고 필요하면 뒤집는다 — 호출부마다가 아니라 거기서 고쳤으니
나중에 대각선을 하나 더해도 다시 나오지 않는다.

**그리고 검사 하나는 내가 틀렸다.** "도면과 그린 것이 같은 방향으로 감긴다"를 단정했다가
ㅁ에서 깨졌는데, `nonzero`는 **패스 요소마다** 풀리고 자모 하나가 곧 패스 하나이므로
알파벳 전체가 같은 방향일 필요가 없다. 렌더러가 요구하지 않는 것을 약속한 검사였다.
지금 보는 것은 letter 안의 관계다: 가장 큰 컨투어가 채우고 카운터가 그보다 많지 않다.

**실측:** 검사 356개 통과, 인덱스 청크 542.85KB(gzip 166.57)로 변화 없음(도면은 지연
청크의 경로 문자열 몇 백 바이트다). 렌더 확인 — 카운터 전부 열림, ㅇ의 오버슛과 획
굵기 차이 살아 있음.

**바뀐 파일:** `render/jamoArt.ts`(새) · `render/pathgeom.ts`(새) · `render/jamo.ts` ·
`render/jamo.test.ts`(다시 씀) · `types.ts` · `geometry/sequence.ts`(+`levelOf`·
`setLevel`, `hitsAt`가 레벨을 같이 준다) · `geometry/grid.ts` · `audio/transport.ts` ·
`audio/render.ts` · `render/Sequencer.tsx` · `render/Stage.tsx` · `store.ts` ·
`Preview.tsx` · `geometry/sequence.test.ts`.

### 20. 퍼블리싱 — 저장소로 옮기고 GitHub Pages에 올렸다 (2026-08-29)

**끝났다. 사이트가 https://designedbyyeon.github.io/Tinkertype/ 에서 돌고 있다.**

**무엇을 옮겼나.** `260820_main`에서 웹에 필요한 것만 골라 왔다 — `src/` 전부,
`public/`, `index.html`, `package.json`/`package-lock.json`, `tsconfig*.json` 넷,
`vite.config.ts`, `vitest.config.ts`, `.oxlintrc.json`, `.gitignore`,
`.claude/launch.json`, 그리고 문서 셋. 뺀 것은 `node_modules`·`dist`(생성물),
루트의 `ChosunBg.TTF`(3.9MB 중복본 — 진짜는 `src/shared/media/type/hangul/fonts/`에
있다), 그리고 `Typography Webtool/`(버려진 별개 저장소의 클론). 레퍼런스 폴더
여섯은 나중에 따라왔고 `.gitignore`에 걸려 있다(머리말 참조).

옮긴 자리에서 `npm ci` → `npm run build` → `npm test`를 돌려 확인했다. 371개 전부
통과하고 인덱스 청크는 547KB(gzip 171KB)로 옮기기 전과 같다.

**`.gitattributes`에 `binary`를 못박았다.** 저장소가 받은 초기 커밋에
`* text=auto`가 있었다. 서체·wasm·`.task`는 NUL 바이트가 있어서 git이 알아서
바이너리로 보지만, 그 판정에 3.9MB 서체를 맡길 이유가 없다. 확장자로 못박았다.

**배포는 `.github/workflows/deploy.yml` 하나다.** main에 푸시 → `npm ci` →
`npm test` → `npm run build` → Pages 업로드. 테스트를 빌드 앞에 둔 것은 깨진
기하나 소리 표가 사이트에 도착하지 않게 하려는 것이고, 0.9초짜리라 값이 싸다.
`base: './'`가 이미 있어서 `/Tinkertype/` 서브경로용으로 고친 것은 하나도 없다.

**1차 실행이 실패했고, 원인이 코드가 아니었다.** `build.test.ts`의
"leaves no NaN, whatever the chamfer is asked to cut"이 5초 기본 타임아웃에 걸렸다.
여기서 2327ms인 검사인데 2코어 러너에서는 그걸 넘긴다. **비용은 기하가 아니라
`expect`였다** — KIOSK를 16번 압출해 정점 좌표를 *하나하나* 단정하고 있었고,
`expect` 하나를 만드는 값이 숫자를 읽는 값의 마흔 배다. 평범한 루프로 훑어
**처음 어긋난 인덱스 하나만** 단정하도록 바꿔서 2327ms → 58ms, 스위트 전체가
2.5초 → 0.86초가 됐다. 커버리지는 같다 — NaN을 심어서 인덱스 7을 짚고 실패하는
것을 확인했다.

**타임아웃 숫자를 키우지 않았다.** 그러면 진짜로 멈춘 검사까지 같이 숨는다. 지금
가장 느린 검사가 304ms라 러너가 다섯 배 느려도 여유가 크다.

**교훈은 CLAUDE.md의 검증 방식으로 올렸다** — 여기서 371개가 0.9초인 것은 CI에서도
0.9초라는 뜻이 아니고, 검사가 무엇을 재는지와 별개로 *어떻게* 재는지가 비용이다.

**같이 정리한 것.** 러너가 띄운 "Node.js 20 is deprecated" 경고 — 액션들을 현재
메이저로 올렸다(checkout@v7 · setup-node@v7 · configure-pages@v6 ·
upload-pages-artifact@v5 · deploy-pages@v5, Node 24). 입력 이름이 그대로인 것은
각 액션의 `action.yml`을 받아서 확인했다.

**실측으로 확인한 것.** 배포된 사이트를 브라우저로 열어 메인 페이지와 Plastic
Type(입체)을 확인했다 — 콘솔 오류 0, 자산 전부 200(three 청크 · Pretendard ·
Big Shoulders · Gothic A1), 한글이 입체로 정상 렌더된다.

---

## 작업 방식 (지금까지 실제로 통한 것)

- **디자인 결정은 만들어서 보여주고 고르게 한다.** 설명하지 않는다. 옵션을
  실제로 렌더해 스크린샷으로 나란히 놓는다.
- **폰트·자산은 넣기 전에 재고, 그 심사를 테스트로 남긴다.** 눈으로 브라우저와
  대조하는 것이 최종 판정이다 — 자동 검사가 틀린 것을 두 번 잡았다.
- **추측하지 말고 갈라서 잰다.** 성능 문제의 원인이 두 도구에서 정반대였다
  (정밀도 vs 검색 범위).
- **측정 순서를 오염시키지 말 것.** React 배치 상태를 같은 tick에 읽으면 안 되고,
  전역 상태를 움직이는 세터(`applyAxes`)는 재는 일과 한 함수에 있어야 한다.
- **하드웨어에 붙은 코드는 산술과 갈라 둔다.** 도구 04의 제스처 수학은 카메라도
  모델도 DOM도 모르는 파일에 있고, 그래서 합성 손으로 단위 테스트가 된다. 검사하는
  것은 특정 숫자가 아니라 어떤 손에나 참이어야 하는 불변식이다 — 한 사람 손에
  맞춘 상수는 그 사람의 상수일 뿐이다.
- **큰 문서를 스크립트로 고치기 전에 앵커 수를 세어 볼 것.** `str.index` 두 개로 만든
  역방향 슬라이스가 README를 한 번 지웠다(12번). 잘라낼 구간의 앵커는 **출현 횟수를
  먼저 세서 1인지 확인**할 것. 2026-08-29부터는 git이 있어서 되돌릴 것이 생겼지만,
  그건 사고를 되돌리는 장치이지 사고를 막는 장치가 아니다 — **커밋되지 않은 편집은
  git도 못 지킨다.**
- **디자인은 만들어서 보여주고 고르게 한다 — 그리고 레퍼런스를 달라고 한다.** tool 05의
  1차 디자인은 "구리다"는 한 마디로 끝났고, 폴더에 들어온 레퍼런스 세 장이 그 자리에서
  방향을 정했다. 읽어낸 것은 취향이 아니라 **등록부**였다: 속이 찬 덩어리 · 반전 한 번 ·
  눈금이 텍스처 · 한 단 낮은 패널 · 가장 큰 활자는 읽기값.
- **모호한 지시는 물어본다 — 두 해석이 정반대 작업일 때만.** "가로모임꼴만"이 레퍼런스와
  어긋났고, 물어보니 "말을 잘못했어, 세로모임꼴만"이었다. 추측했으면 배치 코드를 두 번
  썼다.
- **소리는 스크린샷이 안 찍히므로 버퍼를 읽는다.** tool 05의 검증은 전부 렌더된
  샘플을 숫자로 본 것이고, 그 방법이 아니면 못 찾을 버그가 셋 있었다. 파라미터가
  산술적으로 맞는데 결과가 하나의 회색인 실패는 매체를 가리지 않는다.
- **CI는 이 기계가 아니다.** 여기서 0.9초인 스위트가 2코어 러너에서 타임아웃으로 죽었다
  (20번). 그리고 원인은 검사가 *무엇을* 재는지가 아니라 *어떻게* 재는지였다 —
  좌표마다 `expect` 하나가 마흔 배였다. **실패를 봤을 때 먼저 물을 것은 "코드가
  틀렸나"가 아니라 "여기서와 거기서 무엇이 다른가"다.**
- **눈으로 "원래 그런 디자인"처럼 보이는 버그가 있다.** 투명해진 미리보기 카드가
  그랬다. 그래서 정리할 때는 눈 말고 **대조표**를 한 번 돌린다 — `var()` 이름 대
  토큰 선언, export된 문자열 대 `var(--`, 선언된 export 대 참조 수.
